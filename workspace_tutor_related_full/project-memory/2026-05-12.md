# 2026-05-12 Aquarius deployment and routing memory

## Release v1.1.0 deployed
- Harrison 要求把版本号改成 `v1.1.0` 并部署到服务器。应用版本已从 `1.0.0` 升到 `1.1.0`：`package.json`、`package-lock.json`、workspace mirror 的 `package.json`，以及 UI 中侧栏版本和 Settings 版本文案都改为 `v1.1.0`。
- 已同步版本号到 `/tmp/aquarius-deploy-clean` 部署镜像，并从部署镜像提交 `92c0c8e Release Aquarius v1.1.0`；随后 merge 远端 `origin/main` 的 3 个 UI 提交，生成 merge commit `d3c074b`，并成功 `git push origin main`。
- GitHub `main` 当时为 `d3c074bbbd06e253eb658ffdc010ede3cb73a8f0`。Vercel 前端 `https://aquarius-seven.vercel.app/` 已轮询返回 `v1.1.0`，资源版本 `style.css/app.js?v=1778524463`。Render 后端 `https://aquarius-5ss0.onrender.com/health` 返回 ok。
- 验证：deploy mirror `npm run check` 通过；`git diff --check` 通过；GitHub raw `package.json` 显示 `"version": "1.1.0"`。注意：deploy mirror 根目录有一个未跟踪历史孤儿 `app.js`，不是正式入口，忽略它。

## Production bug fix: API base and JSON errors
- Harrison 截图显示线上 lesson 打开失败，错误包括 `Unexpected token 'T', "The page c"... is not valid JSON`，DevTools 有 `/api/section` 404。
- 根因：生产 `config.js` 里 `apiBase: ''`，前端在 Vercel 静态域名上使用 `window.location.origin`，于是请求了 `https://aquarius-seven.vercel.app/api/section`，但真实后端在 Render。
- 已提交并部署 `864e263 Fix production API base`：生产非 localhost 时 `apiBase` 指向 `https://aquarius-5ss0.onrender.com`；`index.html` 的 `config.js` query bump 到 `1778524701`；`app.js` 增加 `readApiJson(res, label)`，preview/lesson fetch 先检查 HTTP status，避免 404 HTML/text 被当 JSON parse。
- 已提交并部署 `5a30d69 Use rendered content TOC builder`：把不存在的 `buildTocFromRenderedLesson()` 调用改为 `buildTocFromContent(learnExplainContent)`，修 ChapterOverview render enhancement warning。

## Section routing rule correction
- Harrison 明确最终规则：点击左侧 sidebar 里的章节/大章节条目，必须永远先进入 `Study sequence / Subsections` overview 页面，不能从 sidebar 直接跳进 lecture 讲解页。这条优先级高于之前任何“无小节就直接开讲解”的实现想法。
- 没有小节的章节也必须用同一个 overview/subsection 形式。例如 `B.4 Cramer's Rule`、`B.8 Appendix: Useful Mathematical Formulas`：sidebar 点击先显示 overview 页面，右侧 Subsections 列表里放一个 self-card；只有点击这个 self-card 才打开实际 lecture。
- 有真实小节的章节只展示真实 child cards；只有当 `/api/section mode=overview` 明确返回 `hasPrelude:true` 且有非空 `lesson` 时，才额外显示父章节 self-card/parent prelude card。`B.2` 是允许有 parent prelude 的例子；`B.3`、`1.1` 这类没有 parent prelude 的章节，不能为了凑大章讲解而照抄第一节或生成假的父章节卡片。
- `B.8` 特别规则：sidebar 仍先进 overview/subsection 形式；点击 `B.8` self-card 后，继续保留它原来的 formula appendix / textbook-only 行为，例如 `isB8TextbookOnlySection`、`getB8TextbookOnlyMarkdown`，不要当成普通生成 lesson。
- 已部署相关修复提交：`0d85236` 曾修“无 prelude 不显示父卡”；`09904c7` 后续确认对 B.4/B.8 直接进讲解的方向错误，被废弃；`27ec895 Restore subsection overview routing` 恢复 sidebar 永远先进 overview，并给无小节章节 self-card fallback。
- 当前正确代码意图：`shouldOpenSectionAsChapterOverview()` 对任何真实 section id/title 返回 true；`getOverviewLessonEntries()` 在无 child subsections 时插入 parent/self entry；`loadChapterOverviewPrelude()` 只在 `hasPrelude && lesson` 时给有 child subsections 的章节加入 parent card。
- 验证记录：线上 B.4 sidebar 点击已人工视觉验证，会显示 `B.4 Cramer's Rule` overview 页面和单个 `B.4 Cramer's Rule Start ->` 卡片。B.8 走同一入口逻辑；以后若再动 routing，必须专门回归 B.4、B.8、B.2、B.3/1.1 四类样本。

## 1.4-2 impulse demo hydration fix
- Harrison 反馈 `1.4-2` 讲解 demo 有 bug。定位到 active lesson cache 里有两个 `kc-interactive-demo`：第一个 `Impulse as a Limit of Unit-Area Pulses` 是 Agent 生成的 unit-area pulse demo，但数据只有 `type/tool/spec/title`，没有 `demo_type`；前端 `hydrateInteractiveDemos()` 只会渲染白名单 demo type，因此这个 demo 会保持空白容器。第二个 `Interactive Check: locate first, then sample` 有 `demo_type:"impulse_sifting"`，原本可识别。
- 已在 `tutor-openclaw-ui/app.js` 新增 `inferChapterOneDemoType()`：如果 demo 没有显式 `demo_type`，但 title/spec/content 同时命中 impulse 与 unit-area/pulse/rectangular/triangular/gaussian/exponential 语义，就推断为 `impulse_unit_area_limit`。
- 已新增 `impulse_unit_area_limit` Chapter1 demo renderer：提供 `Sharpness alpha` slider 与 `Pulse shape` select（Rectangular/Triangular/Exponential/Gaussian），canvas 显示单位面积脉冲随 alpha 变窄变高，readouts 强调总面积保持 1。
- 资源版本 bump 到 `app.js/style.css/logo v=1778524702`；同步主工程、workspace mirror、部署镜像和部署 workspace mirror。部署提交：`9401619 Render impulse limit demo`，已 push GitHub main。
- 验证：主工程与 deploy mirror `npm run check` 通过；`git diff --check` 通过；`/api/section mode=lesson 1.4-2` 返回 `cached:true` 且 lesson 内有 2 个 `kc-interactive-demo`；本地脚本解码 demo 后确认第一个 inferred 为 `impulse_unit_area_limit`，第二个为 `impulse_sifting`。

## Wikimedia lesson image audit
- Harrison 怀疑 `1.12 Summary` 里的 Wikipedia 图不对，并要求检查其他涉及 Wikipedia/Wikimedia 的图。
- 审核结论：`1.12` 当前 active lesson 的 `Superposition_principle_block_diagram_for_a_MIMO_system.png` 是多输入多输出系统的 superposition block diagram，语义可作为 input-system-output 参考，保留。
- 已修正错配图：`1.3-1` discrete-time signal 不再用 Fourier/DTFT/DFT taxonomy 图，改为 `Digital.signal.discret.svg`；`1.12` parent prelude 不再用 `Broadcasting1.JPG` 做时间轴/幅度轴分类图，改为 `Digital.signal.discret.svg`；`B.6-1` 和 `B.7-6` 的 matrix transpose 不再用 `GaussianScatterPCA.svg`，改为 `Matrix_transpose.gif`。
- 以后 lesson-image QA 规则：Wikipedia/Wikimedia 来源不等于正确，必须核对图片实际内容是否和当前知识点一致；矩阵转置不能用 PCA 图，离散时间不要用 Fourier transform taxonomy 图。

## 2026-05-12 1.4-2 impulse sifting controls and demo backlog
- Harrison 反馈 `1.4-2` 的 `Interactive Check: locate first, then sample` 中拖动 `k` 和 `b` 没反应。根因不是 delta sampling 数学错，而是 UI 默认选择 `delta(t-a)`，却同时显示只对 `delta(kt-b)` 有意义的 `k/b` sliders；在 `delta(t-a)` 模式下拖 `k/b` 本来不会改变 support/scaling，造成“控件坏了”的体验。
- 已修前端 `impulse_sifting` renderer：默认 form 改为 `delta(kt-b)`；`k` 默认 `2.5`、范围 `1..4`；`b` 默认 `4`；当 form 为 `delta(kt-b)` 时隐藏 `a`，显示 `k/b`，其他 form 则显示 `a`、隐藏 `k/b`；坐标轴右端扩到 `6`，保证默认 `b/k` 和 slider 范围可见。资源版本 bump 到 `app.js?v=1778528924`。
- 讲解数学规则保持：`delta(k t-b)` 的 support 是 `t=b/k`，scale 是 `1/|k|`；demo 目标是让学生先设 argument 为 0，再带入周围函数，并看到 slope factor。
- 当前 active `aquarius_visual_latex_v2` lesson cache 扫描结果：37 个 `kc-interactive-demo` block 中，前端可 hydrate 7 个，另有 30 个“课件要求 demo 但没有对应 renderer”。已排除 debug/backup 文件。
- 缺 renderer 清单：`1_1-2` untitled；`1_2-1` untitled；`1_2-2` Time-Scaling Slider；`1_2-3` Mirror the signal；`1_2-4` Explore y(t)=x(at-b)；`1_3-2` Sampling Time versus Quantizing Amplitude；`1_3-3` Shift-Test Demo for Periodicity；`1_3-4` Running Energy and Average Power Classifier；`1_4-1` Move and flip the unit step；`1_4-3` untitled complex exponential demo；`1_5-2` Time Reflection Symmetry Test；`1_7-1` Superposition Tester；`1_7-2` Delay-Then-System vs System-Then-Delay Tester；`1_7-4` Causality Time-Shift Tester；`1_7-5` untitled React + Canvas demo；`1_7-8` BIBO Test Bench；`1_8-3` DC Motor Parameter Response Demo；`1_11-3` Explore g(at+b)；`1_11-4` Rectangular approximation of signal energy；`1_12` Does the Shift Match?；`1_12` Energy vs Power；`b_2-2` Opposite Rotations Create Sine and Cosine；`b_3` Exponential Time Constant Sketcher；`b_3-1` Decay and Growth Controlled by a；`b_3-2` Interactive Envelope-Controlled Sinusoid；`b_5-1` untitled clearing-fractions demo；`b_5-3` Repeated-Factor Term Ladder；`b_6-2` Matrix Multiplication Conformability Demo；`b_7-1` untitled matrix locator demo；`b_7-5` Point-by-Point Multiplication Demo。
- Harrison 要求“做”之后，已修前端 hydration 策略：`hydrateInteractiveDemos()` 不再对未知 demo 直接 `return` 留空壳，而是先推断 demo family，再分发到可用 renderer 或 brief fallback。新增 family 包括 `signal_transform`、`energy_power`、`sampling_quantization`、`system_property`、`exponential_envelope`、`matrix_locator`、`parameter_response`、`pointwise_multiplication`、`matrix_conformability`、`brief`。
- 已新增通用交互 renderer：时间移位/缩放/反射波形 sandbox；能量/平均功率分类器；采样与量化切换器；系统性质双路径 tester；指数/包络 sketcher；矩阵 entry locator；DC motor 参数响应；MATLAB 点乘/维度 mismatch demo。低价值或信息不足的 demo 会显示可读 brief card，不再空白。
- `B.6-2 Matrix Multiplication Conformability Demo` 的数据实际在 `demo.demo_spec.controls`，不是旧 renderer 期待的 `demo.controls`；本次做了控件字段映射，把 `m/n/p/Override rows of B` 映射到 `rowsA/colsA/colsB/rowsB`，避免真实缓存里有数据但前端不认。
- 资源版本 bump 到 `app.js/style.css?v=1778531200`；已同步主工程、workspace mirror、deploy mirror 与 deploy workspace mirror。验证：deploy mirror 和桌面工程 `npm run check` 通过；`git diff --check` 通过；9100 临时服务 `/health` 返回 ok，首页引用新资源版本。

## 2026-05-12 1.3-2 analog/digital lesson cache repair
- Harrison reported 1.3-2 page order jumping 2 -> 3 -> 2 and a mismatched Wikipedia/PWM image on the quantization page. Root cause was corrupted active lesson markdown, not frontend rendering: the cache duplicated `## 2` and `## 3`, mislabeled `x(t)` as amplitude-axis content, mislabeled `x[n]` as quantization content, and inserted `PWM, 3-level.svg` as a web_reference_image even though the page topic was analog-to-digital quantization.
- Fixed the active 1_3-2 lesson cache sequence to exactly: `## 1. Time Axis: Continuous-Time versus Discrete-Time`, `## 2. Amplitude Axis: Analog versus Digital`, `## 3. Analog-to-Digital Conversion: Quantization`; removed the wrong PWM Wikipedia image and its source block; kept the sampling/quantization interactive demo under the quantization section.
- Regression rule: for classification lessons, `x(t)`/`x[n]` belongs to time-axis notation only; analog/digital belongs to vertical amplitude value sets; quantization is a vertical amplitude mapping operation. Never use an external/Wikipedia image unless its actual visual content matches the current section title and caption.

## 2026-05-12 B.2-2 opposite-rotations demo fix
- Harrison reported the `B.2-2 Opposite Rotations Create Sine and Cosine` demo rendered as a static brief/empty shell instead of an interactive demo. Root cause: the active demo title/data was inferred as generic `sinusoid`, and the hydration path had no dedicated renderer for this Euler conjugate-rotation demo, so it fell through to the brief fallback.
- Fixed frontend hydration by adding an `opposite_rotations` family and a dedicated `renderOppositeRotationsDemo()` renderer. It shows `e^{j phi}` and `e^{-j phi}` as conjugate phasors on the unit circle, supports the phi slider and trail toggle from the cache controls, and updates cosine/sine readouts for `(e^{j phi}+e^{-j phi})/2` and `(e^{j phi}-e^{-j phi})/(2j)`.
- Regression rule: B.2-2 must not render as the generic brief fallback. It must show a real canvas/unit-circle phasor demo where moving phi changes the two opposite rotations and the cosine/sine values.
