# 日志记录 - 2026-03-18

### Tutor Agent Demo 进度检查（手机端 Prompt -> Skill -> Nano Banana 2）

- 目标确认：验证“用户输入原始 prompt（不做 refine）→ 触发 `image-animation-generation` skill → 调用 `google/gemini-3.1-flash-image-preview`（Nano Banana 2）→ 返回图片到前端”的链路可用性。
- `openclaw status` 检查：Gateway 服务在本机运行正常（端口 18789）。
- 当前前端文件位于 `tutor-openclaw-ui/`：`index.html`、`style.css`、`app.js`、`ws-bridge.js`。
- `ws-bridge.js` 本地服务（9000）可启动，但调用 `/api/tutor` 时与 Gateway WS 协议不匹配：
  - 报错现象：`invalid request frame` / close code `1008`。
  - 根因：桥接脚本使用了不符合当前 Gateway 协议要求的原始 JSON-RPC 帧格式，缺少正确 connect 握手流程。
- 直接测试 skill 脚本：
  - 命令：`python3 -u ~/.agents/skills/image-animation-generation/scripts/gen_image.py "请用一张图讲解DFS原理"`
  - 结果：脚本成功返回文本说明（exit 0），说明 API 通路可用；但本次返回内容未包含 Markdown 图片链接，前端无法稳定渲染图片。

### 当前结论

- **尚未完全实现“手机端输入后稳定返回图片”**。
- 目前是“链路部分打通”：
  - Skill 脚本能被单独执行；
  - 但 Web 前端通过 ws-bridge 到 OpenClaw 的调用仍有协议层阻塞；
  - 且生图结果格式还需要保证为前端可渲染的图片链接/图片字段。

### 阶段性进展 (更新于 12:20)

1. **链路重构打通**：废弃了 `ws-bridge.js` 里手写的残缺 WS 协议，直接改用 `child_process.spawn` 调用 `gen_image.py`，实现用户 Prompt 100% 直通。
2. **ngrok 映射**：启动了 ngrok 使得前端可以通过公网 URL (`https://unsinkable-dyan-staidly.ngrok-free.dev`) 被手机端访问演示。
3. **强制双轨输出(图+文)**：因为像“讲解傅立叶变换原理”这种非明确要求画图的 prompt 会导致模型只输出文字，在 skill 内增加了强约束和 auto-retry 机制，确保无论是概念解释还是绘图指令，**都会强制返回文字解释 + 至少1张生成的教学配图**。
4. **统一命名空间**：在描述中排除了 “nano banana” 等容易混淆的称呼，确认直接调用 OpenRouter 的 `google/gemini-3.1-flash-image-preview`。
5. **前端大改版**：采用 `frontend-design` 技能的指导，按照 hyperknow 官网风格重构了前端。从一个单调的聊天框变成了具有氛围渐变、Glass拟态面板、大 Hero 区域和双栏工作区的产品级 Landing Page。
6. **前端开发模型偏好确立**：制定了专属 AgentSkill `frontend-model-default`，并写入 `MEMORY.md` 级长期记忆：以后但凡做前端页面任务，在用户未显式指定的情况下，自动默认切换使用 `openrouter/google/gemini-3.1-pro-preview`。

### 下一步（明确行动项）

1. 等待用户对 hyperknow 风格前端的验收反馈。
2. 如需进一步迭代，继续利用 Gemini 模型做网页细节打磨。

### 协作方式偏好（用户新增）

- 用户要求：从现在开始执行任务时，按“约每 2 分钟”节奏做一次简短暂停汇报：
  - 已完成什么；
  - 下一步要做什么。
- 特殊规则：如果当前单个任务未完成，先把该单个任务做完，再进行这次暂停汇报。
### 会话补充记录（下午）

- **前端模型切换报错排查结论**：Control UI 在某些路径下会把未完整限定的 Gemini 模型名错误拼接成 `quan2go/gemini-3.1-pro-preview`，触发 `model not allowed`。
- **稳定修复要点**：
  - 保留并使用别名：`gemini -> openrouter/gemini-3.1-pro-preview`。
  - 清理错误 alias：`gemini-3.1-pro-preview -> openrouter/openrouter/gemini-3.1-pro-preview`（双前缀）以避免 UI 继续命中错误映射。
  - 当前环境下前端默认模型技能应使用 `openrouter/gemini-3.1-pro-preview`（与本机模型表一致）。

- **长期记忆检索服务修复**：此前因 OpenAI embeddings 配置导致 401；已切换 `memorySearch.provider=local` 并补齐本地依赖后恢复健康。

- **文件操作事故与修正（重要）**：
  - 用户要求删除的是 `/Users/chenghaoxiang/workspace/tutor-openclaw-ui`（外部 workspace 的旧目录）。
  - 先前误删了 `/Users/chenghaoxiang/.openclaw/workspace/tutor-openclaw-ui`，后已恢复。
  - 正确目标已移动到 `/Users/chenghaoxiang/workspace/.trash/...`（可恢复删除）。
  - **流程教训**：涉及删除时必须先复述并二次确认“绝对路径”再执行。

