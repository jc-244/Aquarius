- 2026-05-07: 继续处理 B.1-1 的两个具体问题：1) route analogy 的教材图应与对应解释绑在同一知识点页；2) summary 应保留符号敏感结论，如 \(\sqrt{-1}=\pm j\)。已在主工程与副本 `ws-bridge.js` 中加入三类改动：A) prompt 规则层明确要求 textbook figure 不要漂在 overview，而要贴着它所服务的知识点页；B) 新增 `normalizeHistoricalBookImagePlacement()`，对 `B.1-1` 把 route analogy 的 `book_image` 挪到 Country X / Country Y detour 解释附近；C) 新增 `getSummaryHintForSection()` 与 `enforceSectionSpecificLessonPolish()`，尝试要求并兜底保留 `\sqrt{-1}=\pm j`。同时修复了 new-book figure allowlist：若某 section 没有显式 `section-figure-map-new.json` 映射，则回退允许该 section 自身页面上的 figure 文件，从而让 `page-004-unknown-1.png` 这类教材图不再被静默吞掉。实际验证中，一次 live `/api/pregen/section` 已成功走完整链并保存 cache，但后续又有重复请求触发 Agent A；由于运行态仍使用 OpenRouter fallback 的 `gpt-5.5`，并且该 key 仍报 `HTTP 402 ... can only afford 7736`，所以后续重复 pregen 会失败。当前已确认：教材图 `/figures/page-004-unknown-1.png` 在某份 B.1-1 cache 中出现；但 summary 仍未稳定保留 `\sqrt{-1}=\pm j`，且某份 profile-specific cache 仍包含旧的 `gptimage2` 图，说明还需要继续清理“当前 live 到底命中了哪份 cache key / 哪个 profile”的问题，不能只看旧的 `new__aquarius_std_v1...` 文件名。后续继续接手时，优先检查 `buildLessonCacheKey()` 产物、前端当前 profile memory、以及 live `/api/section` 究竟读取的是哪一份 `lesson-cache/b_1-1/*` 文件。
- 2026-05-07: 用户继续反馈 `B.1-1` 某页“还是没有图”，但截图正文已出现 “The figure on this page (Fig. B.1) ...” 等图后说明文，说明高概率不是 markdown 无图，而是前端翻页后落在图下方。已检查 `app.js`：`renderCurrentKnowledgePoint()` 虽然会在渲染后立即 `scrollTop = 0`，但图片、MathJax、lecture decoration、visual metadata UI 都是异步插入/重排，后续布局完成后没有再次把滚动位置拉回顶部。已修复为：在 `renderCurrentKnowledgePoint()` 中拿到 `learnExplainScroll` 后，初始渲染时先置顶；在 60ms 的后处理定时器末尾再次 `scrollTop = 0`，并追加一层 `requestAnimationFrame` 再置顶一次，确保翻页后不会因图片/公式异步布局把视口留在图下方。同步到主工程与副本 `app.js`，并通过 `node --check`。
- 2026-05-07: 用户明确要求 `Key Takeaways` 模块必须总结公式，而不是只保留纯文字概念。已在 `ws-bridge.js` 的 `enforceSectionSpecificLessonPolish()` 中继续加硬兜底：对 `B.1-1`，若 summary 缺少公式总结，则自动补入三条最小公式要点：`j^2 = -1`、`\sqrt{-1} = \pm j`、`\sqrt{-4} = \pm 2j`。这样即便 Agent B 压缩掉符号细节，最终落盘前也会在 takeaway 区补回。注意：这一步只改生成/落盘层，要让当前 live lesson 真看到这三条，仍需对当前命中的 `B.1-1` profile-specific cache 重新走一次真实生成或删除对应 cache 后重拉。同步到主工程与副本 `ws-bridge.js`，并通过 `node --check`。
- 2026-05-07: 用户再次明确要求两点：1) B.1-1 某知识点页必须真正看到教材 route analogy 图；2) `Key Takeaways` 模块必须总结公式，而不是只给文字概念。已进一步定位到一个前端分页根因：当前 active cache `new__aquarius_std_v1.aquarius_std_v1.en.md`（文件内容中可见 `![unknown](/figures/page-004-unknown-1.png)`）把教材图放在第一个 `##` 知识点标题之前，前端 `parseLessonKnowledgePoints()` 在 `flushIntroNodes({ keepRemainderAsKnowledge: false })` 时会把 overview 之后、第一个主标题之前的剩余节点直接丢掉，导致这张 book_image 从知识点分页里消失，页面上只剩下后面“Fig. B.1 shows ...” 的文字说明。已修复 `app.js`：新增 `pendingLeadingKnowledgeHtml`，把第一个主标题前但不属于 overview 的剩余 HTML 暂存并挂回第一个 knowledge point，这样教材图会跟第一个知识点页一起走，而不是被 overview 吞掉。同步到主工程与副本并通过 `node --check`。
- 2026-05-07: 已继续强化 `ws-bridge.js` 的 `enforceSectionSpecificLessonPolish()`：对 `B.1-1`，summary 若缺少公式总结，则在 `📌 Key Takeaways` 内硬补三条最小公式结果：`j^2 = -1`、`\sqrt{-1}=\pm j`、`\sqrt{-4}=\pm 2j`。随后删除 `workspace_tutor_related_full/.../lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md` 并重新触发真实 `/api/pregen/section`。日志显示一轮生成成功完成：Agent A `gpt-5.5`、Agent B `anthropic/claude-sonnet-4.6` 均完成，`[LessonCache] SAVED: b_1-1 / new__aquarius_std_v1 / aquarius_std_v1`。但之后又有额外重复 pregen 请求打进来，OpenRouter fallback 对 `gpt-5.5` 仍报 `HTTP 402 ... can only afford 7736`。更奇怪的是：日志说成功保存了 `new__aquarius_std_v1`，但副本目录随即查询时并没有新文件，只剩 `old__aquarius_std_v1` 和一批 `new__track:*` 历史文件，说明 live 运行态与当前正在检查的副本 lesson-cache 目录之间仍存在一次不同步/错位，后续接手要优先查清：当前 9000 进程实际把 `TUTOR_MATERIALS_DIR` 解析到了哪个根目录、成功保存的 cache 是否写到了主工程另一份 materials 目录、以及是否存在并发 pregen 导致删写时序混乱。
- 2026-05-07: 用户明确提供并确认当前使用的 OpenRouter key 与 `tutor-openclaw-ui/.env`、`workspace_tutor_related_full/tutor-openclaw-ui/.env` 中配置的 `OPENROUTER_API_KEY` 相同。也就是说，live 9000 服务并不是“没切到新 key”，而是确实已经在用这把 OpenRouter key；现有 `HTTP 402 ... can only afford 7736` 就是这把 key 在当前 OpenRouter 账户/limit 状态下对 `gpt-5.5` 规划请求给出的真实可用上限反馈。后续若坚持 `maxTokens=9000`，需要解决的是这把 key 对应的 OpenRouter 账户额度/limit，而不是本地代码的 key 切换问题。
- 2026-05-07: 用户坚持走“保持 Agent A `maxTokens=9000`，不降上限”的路线后，已直接用提供的 OpenRouter key 向官方 `https://openrouter.ai/api/v1/chat/completions` 发起最小化测试请求（模型 `gpt-5.5`、`max_tokens=9000`），成功返回 `ok`，证明平台侧额度/limit 确实已放开；此前 live 服务持续 402，不是 key 错，而是运行态状态未刷新。随后重启了 9000 上的 `tutor-openclaw-ui/ws-bridge.js`（新 PID 42273），删除了 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md`，并在干净服务上重新触发唯一一轮 `/api/pregen/section`。这次 Agent A (`gpt-5.5`, `maxTokens=9000`) 与 Agent B (`anthropic/claude-sonnet-4.6`) 均完整成功，最终 `/api/pregen/section` 返回 `ok: true`。返回体直接显示新的 `B.1-1` lesson 中：1) textbook route analogy 图仍在（`![unknown](/figures/page-004-unknown-1.png)`）；2) `📌 Key Takeaways` 已包含公式总结，包括 `x^2+1=0` 引出 `j^2=-1`、以及额外补入 `\sqrt{-1}=\pm j` 和 `\sqrt{-4}=\pm 2j`。这说明“OpenRouter key 已开放 + 重启 live 服务”后，保持原始 9000 token 上限的真实生成链终于稳定跑通。
- 2026-05-07: 用户这次不要重新生成，只要把现有教材图和 `Country X / Country Y` 说明绑到同一页。已直接手改 live lesson cache `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md`：移除了页首那次过早出现的 route analogy `book_image`，并把同一个 `book_image` 块连同 `![unknown](/figures/page-004-unknown-1.png)` 挪到后面的 “Why Use Complex Numbers If the Final Answer Is Real?” 知识点里，紧贴 `Country X / Country Y` 说明之前；同时把原来那段较长的 prose 压缩成一行说明式 caption，避免同一个意思在两处散开。此次是纯手工版式修正，不经过 Agent A / Agent B，也不改生成链。
- 2026-05-07: 用户刷新后发现该页出现一段重复文字。已确认这是手工调整 live cache 时留下的重复 caption，不是前端重复渲染。已再次直接编辑同一份 `new__aquarius_std_v1.aquarius_std_v1.en.md`，删除 `Country X represents the real numbers...` 下面那一份重复的 route analogy caption，仅保留图下第一条说明。
- 2026-05-07: 用户检查 `B.1-2 Algebra of Complex Numbers` 的 `Key Takeaways` 时发现两类问题：1) summary 页仍有公式“乱码/原样露出反斜杠”；2) takeaway 没有把主要公式列全。已定位到源 markdown `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_std_v1.aquarius_std_v1.en.md` 本身包含合法公式 bullet，例如 `\(\operatorname{atan2}(b,a)\)`、`\(\operatorname{Re}\{z\}\)`、`\(\operatorname{Im}\{z\} = (z-z^*)/(2j)\)`；因此不是 Agent B 完全没写公式。当前更真实的根因有两个：A) 前端 summary 页对“长 prose + 多个 inline LaTeX 片段混写在同一 bullet” 的渲染鲁棒性不足，导致某些 `\(...\)` 在 UI 上原样露出；B) 生成链目前只对 `B.1-1` 做了 section-specific takeaway 公式硬兜底，对 `B.1-2` 还没有“列出该节核心公式清单”的强规则，所以 Agent B 只做摘要式提炼，没有把所有主公式系统性保留。
- 2026-05-07: 用户进一步明确，这不应只修某一节，而应成为全局 lesson 规则：“所有 section 的 `📌 Key Takeaways` 都必须把本节核心公式列全。” 已在主工程与副本 `ws-bridge.js` 中把这条升成全局硬规则：A) `LESSON_GENERATION_RULES_TEXT` 新增 summary 必须显式列出本节核心公式；B) `AGENT_A_RULES_APPENDIX` / `AGENT_B_RULES_APPENDIX` 同步强调 takeaway 要写成公式 bullets，不准只写 prose 摘要；C) 新增 `extractCoreFormulaBullets()` + `mergeCoreFormulaBulletsIntoSummary()` 后处理，在落盘前从整节正文中自动抽取 display math 与 inline math 里的核心公式，并以 `- Core formula: \(...\).` 形式补进 summary，避免 Agent B 摘要时漏掉关键公式。与此同时，已修复前端 summary 公式乱码的一个关键原因：`app.js` 的 `inlineFormat()` 之前在恢复 `\(...\)` 占位之前就先跑了 `*...*` 斜体替换，导致像 `z^*` 这样的公式片段会被误当 Markdown emphasis 破坏；现已改为先处理粗体/斜体/代码/图片，再最后恢复数学占位，从而减少 `Key Takeaways` 中反斜杠原样露出的问题。四个文件均已 `node --check` 通过：主工程和副本的 `app.js` / `ws-bridge.js`。
- 2026-05-07: 用户随后报告“点开始就出现整屏空白底板”的前端 bug。经检查，最可疑且已修复的根因不是 lesson 内容本身，而是 intro landing 的 `introGetStartedBtn` 点击路径：对已登录用户，代码原先只执行 `hideIntroLanding(true)`，没有显式切回任何主视图，导致 intro 被隐藏后可能直接露出空白背景层。已在主工程与副本 `app.js` 中补上 `showWelcome()`，确保已登录用户从 intro landing 点击开始后一定回到主 welcome/app 视图。同时在 `setLearnLessonContent()` 外面补了一层 `try/catch`，如果未来 lesson 解析或分页渲染再次抛错，不会再整屏空白，而会直接在 `learnExplainContent` 显示 `Lesson render failed` 错误盒子，方便继续定位而不是黑屏。
- 2026-05-07: 用户要求把刚加上的“全节公式自动并入 takeaway”逻辑收窄，因为 `B.1-2` 的 summary 已经把大量 worked-example 中间计算也抓进去了。已在主工程与副本 `ws-bridge.js` 中调整 `extractCoreFormulaBullets()`：不再扫描整节所有 inline math，也不再无差别收录所有 display math；改为优先只抽“主知识点锚定公式块”，也就是紧贴 `##` 主标题后的那批定义式、恒等式、转换式、运算规则，过滤掉 `Worked Example / Example A / Example B / Step 1/2/3 / Common Mistake / Exam Trigger` 前后出现的计算步骤。仍保留极少数必须保住的特殊 inline 公式（如 `j^2=-1`, `sqrt(-1)=±j`, `sqrt(-4)=±2j`）作为白名单。这样 takeaway 仍会列全核心规则式，但不再把示例中的中间算式一起塞进 summary。
- 2026-05-07: 用户指出左上 sidebar 品牌头（菜单图标 + favicon 徽记 + Aquarius 字标）视觉质量不佳。已按局部 UI 组件重构方式直接在主工程与副本 `index.html` / `style.css` 中修整，不改功能逻辑：1) 去掉原先这块互相打架的 inline style；2) 将菜单按钮改成更干净的 rounded control，三条线不再等长，减轻生硬感；3) 将 favicon 徽记包进更完整的品牌胶囊卡片（轻边框、柔和内高光、稍大的圆角与阴影）；4) 将 `Aquarius` 字标从 Playfair italic 改成更匹配当前壳子的 `Quicksand` 粗字标，和按钮/学习壳风格统一。此次是纯视觉修整，目标是让三者更像同一组件，而不是临时拼起来的图标、图片和字。
- 2026-05-07: 用户指出右下角浮动 `Q&A Assistant` popover 相比主侧栏 Q&A 面板少了三项关键能力：联网开关、三种回答模式、图片上传。已按前端组件对齐方式修复主工程与副本的 `index.html` / `style.css` / `app.js`：在 `learnChatPopover` 内新增上传按钮、模式下拉（Fast / Balanced / Detailed）、联网按钮，并补 `fileInputLearnPopover`；样式上把 popover 输入区从单行横排改成更适合工具条 + 输入框的纵向结构；逻辑上直接复用主 learn Q&A 现有 attachment / answerLength / webSearch 状态与发送链，不另开第二套后端协议。这样浮动版和主侧栏版在功能上保持一致，只是容器形态不同。`app.js` 已在主工程与副本两边通过 `node --check`。

- 2026-05-07: 用户反馈 sidebar footer 的 Settings 齿轮图标视觉上像坏掉。已按局部 UI 修整方式修复主工程与副本的 `index.html` / `style.css`：用更干净、对称的自定义 gear SVG 替换原先过碎的 Lucide 风格路径；同时把 `.sidebar-settings-icon` 从裸 SVG 改成带浅边框、柔和高光和小投影的圆形徽章，并把 `.sidebar-settings-btn` 的 padding、字重和圆角收稳，使底部 Settings 按钮与当前 Studygram/Neo-brutalism 外壳更协调，不再显得图标损坏或发虚。

- 2026-05-07: 用户要求重生 `B.1-2`。已删除 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_std_v1.aquarius_std_v1.en.md`，并通过 live 9000 服务真实触发 `/api/pregen/section`（sectionId=`B.1-2`, sectionTitle=`Algebra of Complex Numbers`, bookSource=`new`, language=`en`, force=`true`）。这轮没有成功落盘，接口最终返回 `{"error":"Agent A planning failed for B.1-2: no blueprint returned."}`。按当前项目规则，Agent A 失败必须立即停止，不准 fallback，因此本轮重生已中止，当前 `B.1-2` active cache 仍不存在，需要后续继续排查 Agent A 本轮未返回 blueprint 的具体原因。

- 2026-05-07: 用户嫌学习页左右两侧的隐藏/恢复按钮太丑，要求重新画。已使用更贴边的“边缘拉片”视觉语言重做主工程与副本 `index.html` / `style.css`：左右隐藏按钮从单一悬浮小白球改成带 `Hide/Show` 标签的小胶囊，内部加入浅蓝小圆 icon badge，增强方向感与归属感；对应 restore 按钮也同步改成同一套胶囊拉片样式，并略微外移到边缘，使其更像面板边上的控制柄，而不是随机漂浮的圆点。

- 2026-05-07: 用户明确要求 Settings 图标必须保留一眼可辨认的“齿轮语义”。已再次调整主工程与副本 `index.html`：保留上一步修过的 Settings 徽章容器，但将内部 SVG 从过于徽章化的抽象轮廓改为更经典、直给的 gear 轮廓，包含中心圆与更明确的齿状辐条，使其在当前 footer 胶囊按钮中既稳定又一眼看出是设置。

- 2026-05-07: 用户要求首页欢迎页输入框也补上三种回答模式与联网选项。已使用 `frontend-design` 在主工程与副本的 `index.html` / `style.css` / `app.js` 中为 welcome 首页 `searchBox` 增加模式下拉（Fast / Balanced / Detailed）和 Web 按钮，并将其接入现有首页发送逻辑：首页提问现在会读取 `answerLengthToggleMain` 与 `webSearchToggleBtnMain`，而不是继续依赖不存在的旧 `webSearchToggle` 元素；上传图片/PDF能力沿用原有 `attachBtnMain` / `fileInputMain`。视觉上新增工具条贴合现有输入卡片风格，不另起一套杂乱控件。

- 2026-05-07: 用户报告 `B.1-2` 页面只显示 “This section has not been prepared yet.”。已定位真因：上一轮为重生 `B.1-2` 手动删除了 active `new__aquarius_std_v1.aquarius_std_v1.en.md`，但之后真实 `/api/pregen/section` 因 Agent A `no blueprint returned` 失败并按规则中止，导致当前 `aquarius_std_v1` active cache 缺失。此前 `readLessonCache()` 命中 miss 时不会继续走现有 profile-specific lesson fallback，于是前端只能显示 miss message。现已在主工程与副本 `ws-bridge.js` 中修复：`readLessonCache()` 在 `aquarius_std_v1` 文件不存在时，先调用 `readLegacyLessonCacheFallback()` 读取当前 section 目录里最匹配的 profile lesson，再返回给前端；只有连 fallback 都没有时才真正返回 null。这样像 `B.1-2` 这种 active 新 cache 缺失但 profile cache 仍在的情况，不会再整页掉成占位句。

- 2026-05-07: 用户反馈 dark 模式颜色不协调。已使用 `frontend-design` 在主工程与副本 `style.css` 的最终 shell override 层新增完整 dark theme 覆盖，不再沿用前面旧的棕黑底 + 蓝灰字 + 白按钮混合方案。新方案统一成冷调夜间面板：背景 `#0c111b` / `#101722`，面板 `#151e2b`，边框 `#2b3a50`，正文改为 `#e6edf7` / `#9fb0c7`，强调色改为更干净的 `#60a5fa`。同时重写了 dark 下的 main/sider/cards/settings/search/theme-toggle/buttons/chapter overview/lecture card 等关键组件，使壳体层次、按钮状态、文字对比和边缘高光都落在同一套夜间配色体系里，避免此前棕底、发灰蓝字、亮白控件互相打架。

- 2026-05-07: 用户反馈登录页仍然有 bug，只显示 dark 背景点阵而没有登录卡片。已定位到根因：后面新增的全局 light/dark shell override 再次误伤登录页，把通用卡片/按钮壳体样式叠到 `login-view` 上，导致登录页布局层被盖坏。已在主工程与副本 `style.css` 末尾追加登录页专属最终兜底：显式锁定 `.login-view.hidden` / `.login-view:not(.hidden)`、`.login-shell`、`.login-tilt-wrapper`、`.login-card`、`.login-card-stage`、`.login-form-actions`、`.login-form`、`.login-topbar` 的显示和布局；并单独重设 dark 下登录卡片、输入框、SSO 按钮和主提交按钮的表面色与边框，确保登录页不再被通用 dark shell 覆盖成只剩背景层。

- 2026-05-07: 用户继续反馈 `B.1-2` 仍有 bug。进一步定位后确认：不仅是 active `aquarius_std_v1` 缺失，还因为旧 profile lesson 虽已通过 `readLessonCache()` fallback 读到，但随后在 `/api/section` 的 cache-validity 检查里又被 `hasLegacyGeneratedVisualArtifacts()` 判为 stale，从而依旧掉回 `LESSON_CACHE_MISS_MESSAGE`。已在主工程与副本 `ws-bridge.js` 中继续修复两层：1) `readLessonCache()` 命中 legacy fallback 时，先做 `convertLegacyQuickCheckToKcBlocks()` + `normalizeMathMarkdown()` + `enforceSectionSpecificLessonPolish()`，保证返回的是清洗后的 lesson；2) `/api/section` 的 cache-validity 校验新增 `activeCacheExists` 判断，只有在真正命中 active `aquarius_std_v1` 文件时才严格拦 legacy generated-visual artifacts；若当前是 active cache 缺失、只能靠 fallback 顶住，则允许清洗后的 legacy lesson 先返回给前端，不再整页回退成占位句。

- 2026-05-07: 用户要求学习页“讲解”的 Hide 按钮放到左边。准备直接调整 `learnExplainToggleBtn` 在顶部工具条中的位置，只改主工程与副本 `index.html` 的结构顺序，不改其事件逻辑。

- 2026-05-07: 用户反馈学习页在左侧讲解 Hide 后，右侧提问区没有拉满，而且 `Hide/Show` 两个胶囊布局打架。已使用 `frontend-design` 在主工程与副本 `app.js` / `style.css` 中统一折叠态逻辑：1) `applyLearnExplainCollapsedState()` 与 `_setLearnMode()` 现在在讲解折叠且聊天未折叠时，强制 `learnChatCol` 变为 `flex:1 / width:100- 2026-05-07: 用户反馈学习页在左侧讲解 Hide 后，右侧提问区没有拉满，而且 `Hide/Show` 两个胶囊布局打架。已使用 `frontend-design` 在主工程与副本 `app.js` / `style.css` 中统一折叠态逻辑：1) `applyLearnExplainCollapsedState()` 与 `_setLearnMode()` 现在在讲解折叠且聊天未折叠时，强制 `learnChatCol` 变为 `flex:1 / width:100% / max-width:100% / min-width:0`，真正吃满剩余宽度；2) 同步隐藏 resizer，避免 inline style 和 collapsed class 互相覆盖；3) CSS 上把 `explain-collapsed` 态的聊天顶栏右对齐，并把左侧讲解的 `Show` restore 钉到左上角，同时隐藏右侧 restore，避免 `Hide` 和 `Show` 同时堆在同一区域。

- Fixed login page blank bug by moving #loginView out of #introLanding in both main and sync index.html, so showLoginView() is no longer blocked when introLanding stays hidden.

- Fixed learn textbook/lecture toggle sync so active state now follows _learnViewMode instead of stale inline styles; renamed lecture hide control to Hide Notes / Show Notes in textbook mode and restyled the view switcher + hide pill.

- Refined explain-collapsed learn layout so hiding the left lecture pane now removes the textbook/book column too and lets the Q&A column span the full main content width.
- Restyled the lesson Hide/Show controls into a studygram journaling-sticker look: warmer paper chip background, dashed stitch border, blue tape accent, and softer circular icon badge for both collapse and restore states.
- Fixed B.1-2 showing the cache-miss placeholder after active aquarius_std_v1 regeneration failed. Root cause: active cache was missing after Agent A no-blueprint failure, and legacy cache scoring/validation was too strict for current math_weak/early_stage profile plus legacy generated-image metadata. Updated ws-bridge legacy fallback scoring to prefer same track/latest versions even when math profile differs, strips legacy gptimage2 blocks before returning fallback, and promoted the cleaned standard v21 lesson to b_1-2/new__aquarius_std_v1.aquarius_std_v1.en.md. Verified /api/section now returns cached=true, cacheMiss=false, Fig. B.2 present, and no gptimage2 reference.
- Pinned lesson Hide/Show edge controls so the lecture control stays on the far left and the Q&A control stays on the far right, independent of the center Previous/Lecture/Textbook/Next toolbar flow. Added final CSS overrides in both main and sync style.css to remove these controls from normal flex/grid drift.
- Discussed planned Personalization feature: add a left-sidebar entry and full profile editor view backed by the existing local users/<uid>.json memory file; expose generated summary/profile prompt preview, editable Markdown/JSON-like profile text, Save/Refresh/Clear actions, and an AI edit chat box; saved changes should immediately affect buildUserProfilePrompt() injection for /api/ask and lesson generation.
- Used frontend-design to refresh the Settings page layout: converted the left vertical settings nav into compact horizontal segmented tabs, reorganized settings cards into a balanced two-column dashboard, tightened card spacing, improved theme toggle and user card proportions, and added dark-mode-safe overrides in both main and sync style.css.
- Used frontend-design to polish the left sidebar syllabus: added final CSS overrides for compact course-map rows, clearer hierarchy, dashed branch rails, smaller caret columns, softer active section treatment with a blue tape marker, tighter line heights, and dark-mode-safe syllabus colors in both main and sync style.css.
- Redesigned the login screen in main and sync tutor-openclaw-ui to a light neo-brutalist astrological studygram style: hid the old WebGL canvas, added CSS-only dotted/pastel nebula background, injected constellation/star SVG doodles, moved the network pill into a postage-stamp badge on the card, added a washi tape overlap and Caveat handwriting note, and restyled the login card, inputs, SSO buttons, guest button, and primary sign-in button while preserving `#loginView`, `#loginWebglContainer`, `#loginCard`, and `#clerkMountLogin` IDs and Clerk mounting flow.
- User requested a detailed product introduction/prompt for Google AI Studio to generate a polished landing/intro page for Aquarius, emphasizing the product positioning, AI tutoring workflow, textbook-grounded lesson generation, personalized memory, multimodal Q&A, and studygram/neo-brutalist visual direction.
- Fixed B.1-2 lesson rendering bug: active aquarius_std_v1 cache was malformed with only Overview/Core Idea/Key Takeaways plus stale `Agent A JSON failed` visual metadata, causing frontend pagination to show empty/transition/quiz pages. Rewrote B.1-2 active cache into a stable structure: minimal overview, textbook Fig. B.2 anchor, five numbered knowledge-point pages, complete formula Key Takeaways, and quiz block at the end. Also tightened `parseLessonKnowledgePoints` in app.js so empty summary pages with only headings are dropped, and synced app.js to the workspace copy.

## Preference MVP landed
- Added a new left-sidebar `Preferences` entry and a dedicated `preferenceView` page in `tutor-openclaw-ui`.
- Implemented lightweight personalization MVP only: editable markdown profile, save-to-local-memory, and immediate effect on later lesson / Q&A prompt injection.
- `app.js` now initializes a default preference profile for signed-in, guest, and fallback-local users; sidebar also shows a short preference summary.
- `ws-bridge.js` `/api/memory` now persists `preferenceProfile`, and `buildUserProfilePrompt(memory)` injects the saved editable profile into downstream prompts.
- Added `/api/preference/draft` endpoint: AI proposes a revised draft from student instruction, but does not auto-save; user must explicitly apply and save.
- Skipped version history / restore for now by design; plan is to add profile history later only after this basic edit-save-apply loop proves stable.

- 2026-05-07: 新会话中用户要求“读取记忆并接管”桌面 `tutor agent` 项目。已定位真正主目录 `/Users/chenghaoxiang/Desktop/tutor agent`，确认当前 Codex 工作目录 `/Users/chenghaoxiang/Documents/Codex/2026-05-07/tutor-agent` 只是空壳。已读取 `CODEX_PROJECT_GUIDE.md`、`README.txt`、`workspace_tutor_related_full/project-memory/MEMORY.md`、以及最新 `2026-05-07.md` / `2026-05-06.md` / `2026-05-04.md`，恢复核心上下文：主工程是 `tutor-openclaw-ui/`，关键运行文件是 `app.js` / `style.css` / `index.html` / `ws-bridge.js`，运行态和副本修改通常需要同步主工程与 `workspace_tutor_related_full/tutor-openclaw-ui/`；lesson active cache 使用 `new__aquarius_std_v1.aquarius_std_v1.en.md` 命名；Agent A = `gpt-5.5` planner，Agent B = `anthropic/claude-sonnet-4.6` writer；生成失败必须 fail-stop，不能手写 cache 冒充；用户偏好中文、禁止韩语、实际改动前先评估并说明计划，等明确下令后再执行。此次仅完成接管阅读与记忆记录，未修改业务代码。

- 2026-05-07: 用户提供截图，要求调用 skills 把 sidebar footer 的 Settings 图标换掉但仍保持齿轮语义。已按规则先声明使用 `frontend-design` skill，因为这是前端视觉组件局部修整。实际改动只替换主工程 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/index.html` 与同步副本 `workspace_tutor_related_full/tutor-openclaw-ui/index.html` 中 `.sidebar-settings-icon` 的 SVG：从细碎 stroke 轮廓换成小尺寸更稳定、中心孔与外齿更清晰的 filled gear。未改按钮逻辑与 CSS 容器。已确认两份片段 diff 一致，并通过 `node --check tutor-openclaw-ui/app.js`。

- 2026-05-07: 用户进一步 уточ正 Settings 图标需求：“就表情包齿轮就行了”。已继续使用 `frontend-design` 做局部视觉替换，把主工程与同步副本 `index.html` 中 `.sidebar-settings-icon` 的 SVG 改为 emoji 齿轮 `⚙️`，并在两份 `style.css` 增加 `.sidebar-settings-icon > span` 的 `font-size: 15px; line-height: 1; display: block;`，确保 emoji 在圆形徽章内居中且大小稳定。已确认主工程和副本对应片段 diff 一致。

- 2026-05-07: 用户报告“现在进来就卡在这个页面”，截图显示始终停在 welcome 首页。已按规则先用 `browser-use:browser` 做本地页面排查准备，并读 `frontend-design` 以备前端修整；后端日志确认 9000 服务与资源加载正常，没有服务级崩溃，更像前端启动状态机问题。继续检查 `app.js` 后定位到一个明确根因：脚本末尾存在无条件 `showWelcome()`，会在启动阶段把本应接管首屏的 intro landing / login view / quiz overlay 再次覆盖回 welcome，导致用户看起来“进来就总卡在这个页面”。已在主工程与同步副本 `app.js` 中新增 `hasStartupViewClaimedScreen()`，当 intro、login、quiz 或 auth callback/login query 已占据首屏时，阻止末尾默认 `showWelcome()` 抢回页面。两份 `app.js` 已通过 `node --check`，并确认 9000 提供的 `/app.js` 已包含新守卫。当前等待用户刷新页面验证首屏是否恢复正确状态；此次未改视觉，只修启动状态机。

- 2026-05-07: 用户刷新后仍反馈卡在 welcome 首页。继续排查发现上一轮只修掉了文件末尾无条件 `showWelcome()`，但还有第二个启动覆盖点：`DOMContentLoaded` 中调用 `setBook(currentBook)`，而 `setBook()` 内部也会无条件 `showWelcome()`，因此 intro/login/quiz 即使先接管首屏，也会被书本初始化再次打回 welcome。已在主工程与同步副本 `app.js` 中把启动调用改为 `setBook(currentBook, { preserveView: true })`，并将 `setBook(book)` 扩展为 `setBook(book, options = {})`；只有非 preserveView 的真实切书操作才 `showWelcome()`。两份 `app.js` 已通过 `node --check`，9000 当前 `/app.js` 已确认包含 `preserveView` 改动。后续若用户仍看到 welcome，需要重点查是否还有其它启动期 `showWelcome()` 或浏览器缓存/外部 Chrome 没拿到最新脚本。

- 2026-05-07: 用户在浏览器 DevTools 截图中提供了关键控制台报错：`Uncaught ReferenceError: Cannot access \"preferenceProfileEditor\" before initialization`。据此已确认此前“卡在 welcome 页面”并非单纯启动视图逻辑，而是前端 JS 在 Preference MVP 初始化阶段进入 temporal dead zone 后直接中断，导致后续 welcome 输入框、发送按钮、导航等事件绑定都没执行，页面看起来像“卡住”。根因位于 `app.js`：`preferenceProfileEditor` / `preferenceSaveBtn` / `preferenceAiDraftBtn` 等 DOM 常量在 1413+ 行才声明，但其事件绑定代码在 849-886 行就提前访问。已在主工程与同步副本 `app.js` 中将这段绑定整理成 `bindPreferenceControls()`，并在相关 DOM 元素声明完成后再调用，消除了 TDZ 报错。两份脚本已通过 `node --check`，9000 返回的 `/app.js` 已确认包含 `function bindPreferenceControls()` 与后续调用。

- 2026-05-07: 用户刷新后反馈 intro landing 与主 app welcome 页面重叠。控制台已无先前 `preferenceProfileEditor` 致命错误，说明 JS 初始化继续往后跑；新问题是 intro landing 显示时 `.app` shell 仍同时可见，导致 hero 文案与 welcome 页面互相叠层。已使用 `frontend-design` 做局部层级修复：在主工程与同步副本 `app.js` 中，`initIntroLanding()` 显示 intro 时给 `document.body` 加 `intro-active` 并同时给 `.app` 加 `hidden`；`hideIntroLanding()` 时移除 `intro-active`。同时在两份 `style.css` 中加入 `body.intro-active .app { display:none !important; }` 与 `body.intro-active .intro-landing-new:not(.hidden)` 的显示/不透明背景兜底，确保 intro 首屏是完整独占全屏层，不再依赖 z-index 与初始化顺序。两份 `app.js` 已通过 `node --check`，9000 返回的 `/app.js` 与 `/style.css` 已确认包含 `intro-active` 改动。

- 2026-05-07: 用户贴出一整份 landing/intro 参考 HTML，明确要求“根据这个改，不要紫色”。已按规则使用 `frontend-design`，保持现有 intro landing 的 neo-brutalist studygram 结构方向，但把首屏与登录页视觉系统中的紫色系彻底收束为蓝/青/黄/粉/绿。具体包括：1) intro 配色层把 `--purple` 从靛紫改为青蓝；2) `nebula-2` 改为浅蓝雾团并降透明度；3) `.intro-landing-new .handwriting` 改为更干净的蓝色；4) `.math-box` 从淡紫底 + 紫虚线改为浅青底 + 青虚线；5) Agent B 卡片背景从淡紫改为淡黄；6) 登录页 `--login-purple` 改为青蓝，背景第二层 aurora 与 `::after` 光斑改为绿色；7) 登录页 washi tape 的紫色条纹改为绿色条纹；8) `login-password-toggle:hover` 的浅紫 hover 底改为浅蓝；9) intro/login 相关内联 SVG 与 focus 边框中的 `#a855f7` / `#818cf8` 改为蓝或绿；10) index.html 中 Tailwind 示例色名 `twilight-purple` 改为 `aurora-teal`。两份主工程/副本文件均已同步，且 `app.js` 语法检查通过。

- 2026-05-07: 用户反馈 B.1-2 页面“格式错了”，截图显示正文出现裸 `[` / `]`、`heta`、`sqrt{}` 等未渲染数学碎片。排查确认这不是纯前端 CSS 问题，而是当前 live 命中的 active cache `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_std_v1.aquarius_std_v1.en.md` 本身已损坏：LaTeX 反斜杠和命令丢失，导致 display math 退化成普通文本。已按“先救 live 页面”的原则，删除坏掉的 active cache 并直接重建为一份干净的 B.1-2 lesson：保留 textbook Fig. B.2 作为概念锚点，重写为 5 个知识点页（complex plane、rectangular vs polar、Euler bridge、conjugate、choose the right form），所有公式统一用 `\(...\)` 与 `\[...\]`。随后调用 live `/api/section` 验证：当前 `B.1-2` 已重新命中 `new__aquarius_std_v1`，返回 `cached: true`，且不再含有坏格式特征（无裸 `
[
`、无 bare `heta`、公式片段恢复为 `\(	heta\)` 等）。此次是直接修 active cache，不经过重生成。

## 2026-05-07 Preferences blank-page fix
- Fixed Preferences blank main panel: moved `#preferenceView` from outside `.app`/`#mainContent` back inside `<main class="main" id="mainContent">` in both main and sync `index.html`.
- Updated preference CSS so `.preference-view` and `.preference-page-shell` use the main panel height instead of `100vh`, preventing layout escape/blank main area.
- Bumped `style.css` cache version to `1778105292` so localhost:9000 pulls the new CSS on refresh.
- Removed remaining purple stripe from `.preference-card-tape`, replacing it with cyan/green.
- Verified localhost:9000 serves `#preferenceView` before `</main>` and the new stylesheet version; `node --check` passes for both app.js copies.

## 2026-05-07 Lecture Hide button alignment
- Fixed the left lecture `Hide` edge control being clipped by the main content border. The stable edge-control CSS now places `.learn-explain-toggle-btn` and `.learn-side-restore-explain` at `left: clamp(72px, 5vw, 96px)` on desktop, while keeping an 18px left inset on small screens.
- Applied the same change to both main and sync `style.css`, and bumped `style.css` cache version to `1778105494` in both `index.html` copies so localhost:9000 refreshes the updated positioning.
- Verified localhost:9000 serves the new stylesheet version and updated left-position rule.

## 2026-05-07 Intro footer CTA visibility fix
- Fixed intro footer CTA `Ask your first question` only appearing on hover. Added explicit default/hover/active rules for `.intro-landing-new #introFooterCtaBtn`, including display, min size, opacity, visibility, background, color, border, radius, shadow, and transform reset.
- Added `#introFooterCtaBtn` to the late global primary-button override groups and dark-mode button groups so it receives the same durable default state as `#introGetStartedBtn`.
- Synced changes to both main and workspace copies, bumped `style.css` cache version to `1778105633`, and verified localhost:9000 serves the updated stylesheet and CTA rules.

## 2026-05-07 Preference draft API debug
- Investigated Preferences AI Draft error `Unexpected token N, \"Not found\" is not valid JSON`. Root cause was not the prompt text; live `POST /api/preference/draft` on localhost:9000 returned plain-text 404 `Not found`, while frontend unconditionally called `res.json()`.
- Added resilient parsing in both main and sync `app.js`: draft requests now read `res.text()`, attempt JSON parse, and gracefully surface plain-text server errors instead of throwing a JSON parser error.
- Verified the route already existed in `ws-bridge.js`; the real issue was a stale node server instance on port 9000. Killed the old process and restarted `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/ws-bridge.js`.
- Confirmed after restart that `POST /api/preference/draft` returns `200 OK` JSON with a valid Markdown draft.
- Bumped `app.js` cache version to `1778105893` in both `index.html` copies so the browser refresh picks up the new frontend error handling.

## 2026-05-07 Course Tracker feature
- Removed the left sidebar `Learning Profile` summary card and added a new sidebar nav entry named `Course Tracker`.
- Added a full `courseTrackerView` page in main and sync `index.html`, with three overview cards, grading policy summary, and a lecture schedule table.
- Parsed the Fall 2025 syllabus into frontend constants: grade weights (HW 20, MT1 20, MT2 20, Final 40) and a 26-lecture schedule with dates, topics, textbook sections, and milestone tags.
- Implemented local progress tracking in `app.js` using `localStorage` key `aquariusCourseTrackerFall2025`; each lecture row has a status select (`Not started`, `In progress`, `Done`, `Review`). Progress bar and next-lecture summary update automatically.
- Added a `Reset Status` action and integrated `showCourseTrackerView()` into the app view-switching state machine so tracker visibility is clean across welcome / learn / library / preferences / settings / login.
- Added full Course Tracker styling in `style.css` for overview cards, grading cards, milestone chips, sticky schedule table, and responsive layout.
- Bumped live asset versions to `style.css?v=1778106500` and `app.js?v=1778106500`, restarted the localhost:9000 ws-bridge service, and verified the live app serves the new Course Tracker HTML/CSS/JS.

## 2026-05-07 Settings card vertical tweak
- User requested moving the Settings page `Profile & study setup` card upward. Adjusted the fourth `.settings-page-card` in the two-column settings dashboard with `margin-top: -40px` on desktop, while resetting it to `0` under the 1120px responsive breakpoint.
- Applied to both main and sync `style.css`, bumped stylesheet cache to `1778107139`, and verified localhost:9000 serves the new CSS rule.

## 2026-05-07 Learn left Hide/Show fixed anchor
- User wanted the left `Hide`/`Show` lecture panel control to stop moving and stay fixed at the far left. Replaced the desktop `left: clamp(72px, 5vw, 96px)` rule with fixed `left: 18px` for both `.learn-explain-toggle-btn` and `.learn-side-restore-explain`, matching the mobile/collapsed anchor.
- Removed hover `translateY(-1px)` from the stable edge controls so hover no longer makes the button jump.
- Applied to both main and sync `style.css`, bumped stylesheet cache to `1778107280`, and verified localhost:9000 serves the fixed-left rule.
- Optimized chapter overview UI on learn page using frontend-design direction: denser chapter hero, study-route meter, improved subsection cards, and added persistent right-side Q&A empty state with icon; bumped assets to style/app v1778107608 and synced main + workspace copies.
- Added Mistake Notebook feature using frontend-design: sidebar entry, image upload, localStorage-backed per-problem pages, pagination, per-item notes/tags/search, AI solve and AI draft notes via /api/ask image attachments; changed Preferences sidebar icon to user-personalization icon; bumped assets to v1778108424 and synced both UI copies.
- Added fixed sidebar Workspace scrolling: .sidebar-nav-shell now has its own thin vertical scrollbar while logo/top and settings/footer stay fixed; synced CSS to workspace copy.
- Adjusted learn page edge controls so Hide/Show no longer collides with paged Previous/Next: left overlay button now reserves space beside fixed Hide button; mobile falls back to in-flow placement.
- Fixed B.1-2 lecture formula rendering: markdownToHtml now normalizes standalone MathJax display delimiters \[ ... \] into $$ blocks so legacy cached LaTeX no longer appears as raw backslash text; app.js bumped to v1778108892 and synced.
- Added per-question Add to Mistake Notebook support in Knowledge Check modal: current quiz question/options/answer/explanation/section metadata are saved as text-based mistake pages; Mistake Notebook now supports text preview pages as well as image uploads; assets bumped to v1778109238 and synced.
- Changed intro hero secondary CTA text from Explore syllabus to START NOW! and synced index.html to workspace copy.
- Fixed lesson layout overflow root causes in `tutor-openclaw-ui`: converted lesson toolbar to a non-overlapping grid lane layout, constrained hover hitboxes, and changed the main explain/chat split to a grid (`55fr / 10px / 45fr`) so the resizer no longer pushes content into the Q&A panel. Synced `style.css` and `index.html` from the live desktop repo copy.
- Reworked lesson generation toward visual/LaTeX/demo-first output: bumped lesson cache to `aquarius_visual_latex_v2`, added broken single-dollar display math normalization, tightened Agent A/B prompts for compact segmented lessons, added GPT-5.4 figure descriptions to 68/71 new-book figures, exposed figure descriptions in lesson markdown, added fallback source-page inference for book figures, and regenerated B.1-2 with 0 single-dollar math blocks, 2 figure descriptions, and 1 complex-plane interactive demo.
- Hero CTA tweak: hid legacy `introHeroStartBtn`, enlarged and centered `START NOW!`, bumped CSS cache to `style.css?v=1778111800`, synced to both tutor-openclaw-ui workspaces.
- Lesson toolbar fix: moved Previous/Next inside `learn-toolbar-center`, removed JS forcing toolbar display to flex, added overflow-safe toolbar grid and compact arrow-only nav under narrower widths, bumped cache to `style.css/app.js?v=1778112600`, synced both workspaces.
- Settings layout tweak: pinned card grid so `Profile & study setup` sits on the same row as `Save progress across sessions`; bumped CSS to `1778113000` and synced both workspaces.
- Lesson scroll restore: added final CSS override so `learn-body-inner`, explain/chat columns, `learn-explain-scroll`, and `learn-chat-scroll` have `min-height:0` and independent vertical scrolling; bumped CSS to `1778113400` and synced both workspaces.
- Lesson pagination structure fix: wrapped each rendered knowledge point in `lesson-page-frame` with internal `lesson-page-footer`, hid old bottom rail page indicator, reset decoration state per page, bumped CSS/JS to `1778113900`, synced both workspaces.

- 2026-05-07: 新会话用户要求读取桌面 `tutor agent` 文件夹记忆并接管。已定位主目录 `/Users/chenghaoxiang/Desktop/tutor agent`，读取 `CODEX_PROJECT_GUIDE.md`、`README.txt`、`workspace_tutor_related_full/project-memory/MEMORY.md`、`2026-05-07.md`、`2026-05-06.md`、`2026-05-04.md`，确认主工程为 `tutor-openclaw-ui/`，同步副本为 `workspace_tutor_related_full/tutor-openclaw-ui/`，关键文件为 `index.html` / `app.js` / `style.css` / `ws-bridge.js`，lesson cache 与材料主要在 `workspace_tutor_related_full/tutor-materials/`。恢复当前重要规则：默认中文、禁止韩语、使用 skill 前先说明、前端改动常需同步主工程和副本并 bump asset version、真实重生成必须删当前命中 cache 并走 Agent A(gpt-5.5) + Agent B(Claude Sonnet 4.6)，生成失败 fail-stop。最新接手状态：今日已加入 Course Tracker、Mistake Notebook、Preference MVP、多轮 lesson layout/toolbar/scroll/pagination 修复，最近落点是 lesson 每页 `lesson-page-frame` 与 page footer 结构。此次仅阅读接管并记录记忆，未改业务代码。

- 2026-05-07: 用户反馈当前 `B.1-2` 所有讲解内容都堆在第二页。初步判断更像是 active B.1-2 lesson markdown 的 heading/page boundary 与前端 `parseLessonKnowledgePoints()` 分页规则不匹配，或最近 `lesson-page-frame` / page footer / toolbar pagination 改动引入了 page 合并/渲染状态问题。按用户长期偏好，本轮先说明判断与排查路径，暂不改代码；若用户下令继续，应优先检查当前命中的 B.1-2 cache 文件、`app.js` 的 lesson parser/renderCurrentKnowledgePoint，以及最新 pagination CSS/JS。

- 2026-05-07: 已完成用户反馈“B.1-2 所有讲解堆在第二页”的排查，未改业务代码。live 日志确认当前命中的是新版 cache `workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，不是旧 `aquarius_std_v1`。该文件只有 `# Algebra of Complex Numbers` 与 `## Try It: Drag the Point`，没有 Agent A 规划里的 `## 1. Cartesian...` / `## 2. Conjugates...` / `## 3. Complex arithmetic...` 主知识点标题；前端 `parseLessonKnowledgePoints()` 只会把符合编号/section-code 的 H1/H2 当分页标题，因此 overview 被抽成第 1 页，剩余正文全部被 `flushIntroNodes({ keepRemainderAsKnowledge: true })` 合成一个 Core Lesson，表现为内容全堆在第二页。进一步定位到生成链根因：Agent A preview 与 `agentB-B.1-2-input.json` 的 planned blocks 里确实有 `page_title` 字段，但 `ws-bridge.js` 的 `extractPlannedKnowledgeHeading()` 只读取 `explanation_instruction` / `instruction` / `heading` / `title`，漏掉 `page_title`，导致 `blueprintToMarkdown()` 未把 planned heading 插回 markdown。建议修法：先在 `extractPlannedKnowledgeHeading()` 中优先读取 `plannedBlock.page_title`，同步主工程和副本；再处理当前已坏的 B.1-2 v2 cache（按用户指令选择修补现有 cache 或删 cache 真实重生成）。

- 2026-05-07: 用户下令修复 B.1-2 内容堆在第二页的问题。已完成两层修复：1) 防复发：在主工程与同步副本 `tutor-openclaw-ui/ws-bridge.js` 的 `extractPlannedKnowledgeHeading()` 中加入 `plannedBlock.page_title` 读取，因为 Agent A/B planned blocks 实际把分页标题放在 `page_title`，此前组装 markdown 时漏读导致 `## 1/2/3` 未写入 cache；2) 修当前 live cache：直接补丁当前命中的 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，加入 `## 1. Cartesian, polar, and exponential forms`、`## 2. Conjugates and unit-angle identities`、`## 3. Complex arithmetic...`、`## 4. Try It: Drag the Point`，并修复一处 `(a+jb)+(c+jd)` 与 `(a+jb)(c+jd)` 被粘在同一行的 LaTeX。随后发现 `normalizeMathMarkdown()` 使用 `\\s+` 会跨换行吞掉 display math 的 `\` 换行，已将其收窄为仅匹配空格/Tab (`[ \t]+`)，同步两份 `ws-bridge.js`。两份 `ws-bridge.js` 均通过 `node --check`，已重启 localhost:9000 服务；最终 live `/api/section` 验证 B.1-2 cache hit 返回 4 个 H2 分页标题，公式 excerpt 保留独立 `\\
` 行且 `has glued formula false`。当前 9000 监听进程为 node PID 5790。

- 2026-05-07: 用户反馈当前 B.1-2 讲解结构仍不符合预期，并给出 B.2-1 作为正例：讲解应像 lecture note 一样，讲到某个概念/公式时把对应 LaTeX 单独放大、独立占一行，而不是把所有公式堆在页面最上方；“精简讲解”不是把文字解释压到几乎没有，仍需要足够的概念解释、例子和考试提示；教材中带编号的公式（如 B.16）代表重点公式，生成链应额外识别和重视；Key Takeaways 必须包含本节所有重点公式和核心概念要点，但不要把不重要/中间计算也塞进去。按用户硬偏好，本轮先做合理性评估与计划，不直接执行；若用户下令，应优先对照 B.2-1 当前结构，重写 B.1-2 active cache，并把规则写入 Agent A/B prompt 与 markdown 组装策略。

- 2026-05-07: 新会话再次要求读取桌面 `tutor agent` 文件夹记忆并接管。已确认项目根目录 `/Users/chenghaoxiang/Desktop/tutor agent`、主工程 `tutor-openclaw-ui/`、同步副本 `workspace_tutor_related_full/tutor-openclaw-ui/`、材料与 lesson cache 位于 `workspace_tutor_related_full/tutor-materials/`。已读取核心接手说明与当日最新记忆，当前最新技术上下文是 B.1-2 结构需按 B.2-1 lecture-note 风格重写：公式应随概念独立展示，不要集中堆顶；带编号教材公式需重点识别；Key Takeaways 要列核心公式和概念但过滤中间计算。本轮仅完成记忆读取与接管确认，未改业务代码。

- 2026-05-07: 用户要求生成一份项目简单介绍，用来喂给 Gemini 帮忙做介绍页面，并特别要求不要 AI 味太重。已按产品 brief 方向整理：强调 Tutor Agent 是面向 Signal Processing and Linear Systems 的学习工具，核心用户是想少走弯路、快速掌握考试重点的学生，产品重点包括教材驱动讲解、知识点分页、公式/图解/互动演示、Q&A、Course Tracker、Mistake Notebook、个性化学习偏好与考试导向。此次只生成文案，不改代码。

- 2026-05-07 14:58 CST: 用户继续强调 B.1-1/B.1-2 的 LaTeX 展示必须像 B.2-1：大号、居中、独立 display block，不要图三那种可横向滑动的公式长条；如果不是考试重点可少讲，但不能把重要概念讲到没文字。已完成修复：1) 主工程与同步副本 `ws-bridge.js` 新增/强化 `splitDisplayMathForLectureNotes()`，会移除 `\\[pt]` 行距残留，并把普通公式列表里的 `\\qquad` 多公式串拆成多个 `$$...$$` display blocks；active cache hit 现在也会走 `enforceSectionSpecificLessonPolish(sectionId, normalizeMathMarkdown(content))`，避免旧缓存绕过归一化；2) 更新 Agent A/B prompt 和全局 lesson rules，明确禁止 ordinary formula lists 使用 `aligned/gathered` 或 `\\[4pt]` chains，要求 B.2-1 lecture-note 风格，重点公式 concept-local 展示；3) 更新主/副本 `style.css` 的 `.math-block`，取消横向滚动，居中显示并把 MathJax display 字体放大到 1.18em；4) 修当前 active caches：`b_1-1/new__aquarius_visual_latex_v2...` 中把 `j^2=-1` 与 `\sqrt{-1}=\pm j` 拆成两行独立公式，正文和 Key Takeaways 均保留；`b_1-2/new__aquarius_visual_latex_v2...` 中 B.1/B.5/B.8/B.9/乘除/幂根对数等核心公式拆为独立 display blocks，清除 `\\[pt]`、`\\qquad`、`gathered/aligned` 痕迹。验证：`node --check` 两份 `ws-bridge.js` 通过；live `/api/section` 对 B.1-1 和 B.1-2 返回 `cached:true`，两者均无 `gathered/aligned`、无 `\\[pt]`、无 `\\qquad`；B.1-1 正文和 Key Takeaways 均含 `j^2=-1` 与 `\sqrt{-1}=\pm j`。已重启 localhost:9000，新监听进程 PID 14575，`/ws-bridge.js` 与 `/style.css` 确认加载新逻辑。
- 2026-05-07 14:58 CST: 回答用户“蒸馏作业调用逻辑”：`loadExamPriorityGuidance()` 只对 Background (`B.*` 或 title 含 background) 章节加载 `workspace_tutor_related_full/tutor-materials/exam-priority/background_hw1_distilled.md/json`；`classifyBackgroundSection()` 先把 B.1-1 / historical note 特判为 `historical_note` 并返回 null，不注入作业蒸馏；B.1-2 algebra、B.2 sinusoid/phasor/exponential 等为 `computational_core`，会把蒸馏作业作为 exam-priority weighting signal 同时传入 Agent A prompt 与 Agent B payload，用于决定多讲哪些概念、增加 worked examples/trap warnings/quiz bias，但 prompt 明确 `Do not force every page to mention the homework explicitly`，并且 section identity 优先。
- 2026-05-07 15:52 CST: 用户要求“彻底修复格式紊乱，生成一次就格式正确”，并希望教材全屏页面能显示 Q&A 页面而不是只有黑色 Q&A 圆钮。已完成主工程与同步副本修复：1) `ws-bridge.js` 新增 `collectLessonFormatIssues()` / `prepareLessonForCache()` / `assertLessonFormatClean()`，写缓存、读 active cache、legacy fallback、pregenerate 后、API 返回前都加格式闸门；会拒绝空 lesson、奇数 `$$`、`aligned/gathered`、可见 `\\[pt]` 行距 token、`\\qquad` 横向公式条、raw `\\[...\\]` display delimiter、裸 `sqrt/theta/operatorname/cos/sin/tan` 等；2) 删除 Agent B invalid JSON 后 raw markdown fallback，改为 fail-stop，避免坏 markdown 被伪装成正常 lesson；3) `index.html` 的 `window.preprocessMath` 先保护 LaTeX row-spacing token，再归一化双转义与 standalone `\\[...\\]` 为 `$$...$$` block，并 bump 资源到 `1778139320`；4) `app.js` 为 textbook fullscreen 加入真正 Q&A panel（复用 `learnChatContent` 与 `sendLearnFollowup()`，桌面宽屏默认打开，输入/发送/同步/MathJax typeset 均可用）；5) `style.css` 新增右侧 fullscreen Q&A panel 样式，隐藏旧 `learn-chat-fab/popover`，修 `#textbookFocusZoomResetBtn` 的 `font-size:-10px` 坏值；6) 重启 localhost:9000，当前监听进程 PID 17545，`/health` 正常。验证：两份 `app.js/ws-bridge.js` 均 `node --check` 通过；live `index/app/style/ws-bridge` 均确认加载新版本/新逻辑；live `/api/section` 请求 B.1-1 与 B.1-2 均 `cached=true`、`cacheMiss=false`、坏格式扫描 `issues=none`。注意：旧 `track:*` cache 文件中仍有历史 `\\qquad`/`\\[pt]` 痕迹，但现在 legacy fallback 会拒绝这些脏缓存而不是展示。
- 2026-05-07: 用户反馈 B.1-2 Textbook tab 显示 B.2 Sinusoids 书页。排查确认不是 lesson 生成、不是 Textbook tab 渲染，也不是 section id 解析错；前端正常用 `b.1-2` 查 `section-page-map-new.json`，但该 map 的 `b.1-2` 错含 `page-016/page-017`。OCR/meta 明确 `page-016.txt` 第一行已是 `B.2 Sinusoids`，`page-017` 也是 B.2 图 B.6；B.1-2 应结束于 `page-015`。已同步修复主工程与副本 `section-page-map-new.json`：`b.1` 截到 `page-015`，`b.1-2` 截到 `page-005`~`page-015`，`b.2` 改为 `page-016`~`page-020`；保留 `b.2-1 = page-018,page-019` 与 `b.2-2 = page-020`。为避免浏览器旧 JSON 缓存，`app.js` 加 `section-page-map-new.json?v=1778139800` 且 `{ cache:'no-store' }`，`index.html` bump `app.js?v=1778139800`。验证：两份 app.js `node --check` 通过；live `localhost:9000/section-page-map-new.json?v=1778139800` 返回 `b.1-2` 不含 B.2 页。
- 2026-05-07: 用户确认要先改完 HW1 蒸馏过滤逻辑再重新生成 B.1-1。已同步主工程与副本 `ws-bridge.js`：新增 `getExamPriorityTopicBuckets()` 和 `filterExamPriorityGuidanceForSection()`，把 `background_hw1_distilled` 从整份 Background bias 改为 section/topic filtered guidance；当前 bucket 包括 `complex_algebra`（B.1-2）与 `sinusoid_phasor`（B.2/B.2-1）。过滤后只把 relevant topic 的 concepts/problemTypes/examplePatterns/traps 传入 Agent A/B，非相关 topic 只作为 preview-only，且 prompt 明确“不能恢复 omitted homework topics，Key Takeaways/公式/例题/quiz bias 只能来自 relevant buckets”。B.1-1 仍由 `historical_note` 直接返回 null，不吃 HW1。随后用正式 CLI `node ws-bridge.js --pregen-section 'B.1-1' 'B.1-1 A Historical Note' new` 强制重生成，Agent A=gpt-5.5(OpenRouter fallback) 产出 10 blocks，Agent B=anthropic/claude-sonnet-4.6 成功写入 `lesson-cache/b_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`。验证：两份 `ws-bridge.js` 语法通过；新 B.1-1 cache 无格式风险 issues，包含 `j^2=-1` 与 `\sqrt{-1}=\pm j`，Agent B input 中 `exam_priority_guidance:null`，live `/api/section` 返回 `cached:true`/`cacheMiss:false`/chars 19280。已重启 localhost:9000，当前监听 PID 21872。
- 2026-05-07: 用户要求生成 B.1-2。第一次正式 CLI 预生成 `node ws-bridge.js --pregen-section 'B.1-2' 'B.1-2 Algebra of Complex Numbers' new` 失败：Agent A gpt-5.5 两次输出均 hit `finish_reason=length` 导致 blueprint JSON 无法解析，系统 fail-stop 且未覆盖旧缓存。为解决大 section blueprint 被截断，已同步主工程与副本 `ws-bridge.js`：Agent A 初次 `maxTokens` 从 9000 提到 18000，compact retry 从 7000 提到 14000。随后第二次正式预生成成功：Agent A 产出 12 blocks，Agent B=anthropic/claude-sonnet-4.6 成功写入 `lesson-cache/b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，webSourceCount=5。验证：新 B.1-2 cache chars≈25561，无格式风险 issues，H2 结构为 Concepts、Rectangular form、Polar/exponential、Conversion、Conjugate、Polar arithmetic、Complex functions/logarithms；正文不含 B.16 / `a cos + b sin` / `ω0t` 等 B.2 正弦合并重点；Agent B input 的 `topic_filter.relevant_topic_ids=["complex_algebra"]`，`sinusoid_phasor` 仅 preview-only；live `/api/section` 返回 `cached:true`/`cacheMiss:false`。已重启 localhost:9000，当前监听 PID 23583。
- 2026-05-07: 用户觉得新版 B.1-2 生成效果更好，因此要求再用同一最新版生成链重跑 B.1-1。已正式 CLI 强制预生成 `node ws-bridge.js --pregen-section 'B.1-1' 'B.1-1 A Historical Note' new`，在新版 Agent A token 上限（18000/14000 retry）下成功：Agent A 产出 9 blocks，Agent B=anthropic/claude-sonnet-4.6 写入 `lesson-cache/b_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，webSourceCount=4。验证：新 B.1-1 cache chars≈15857，issues=[]，H2 结构为 “Why the Number System Kept Expanding / The New Number Needed for x^2+1=0 / From Suspicious Trick to Accepted Tool”；无 polar/Euler/phasor/sinusoid 等 computational-heavy 偏移，`j^2=-1` 与 `\sqrt{-1}=\pm j` 仍在；Agent B input 中 `exam_priority_guidance:null`，live `/api/section` 返回 `cached:true`/`cacheMiss:false`。
- Fixed B.1-1 bad visual rendering in tutor-openclaw-ui. Root cause: Agent A chose a mismatched Wikimedia SVG (`Three_Baskets_with_Apples.svg`) for a number-system hierarchy concept, and `web_search_image` emitted a raw `<div class="kc-reference-source">` outside KC protection so frontend rendered it as visible text. Patched both `ws-bridge.js` copies to: (1) wrap `kc-reference-source` in `%%KC_BLOCK%%...%%KC_END%%`, (2) prefer LaTeX-native visuals for number-system hierarchy / number-system expansion queries, and (3) normalize existing B.1-1 lessons by replacing that bad web-reference block with a stable lecture-note LaTeX chain `1,2,3,... -> a/b -> sqrt(2) -> -5 -> j`. Also updated the live B.1-1 cache file at `tutor-materials/lesson-cache/b_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, restarted localhost:9000 server (new PID 27570), and verified via POST `/api/section` that cached lesson is clean: no Wikimedia URL, no raw reference div, number-system LaTeX present, book figure still present.
- Updated textbook fullscreen Q&A panel UI. Added `answerLengthToggleTextbookFocus` with Fast/Balanced/Detailed, added `webSearchToggleBtnTextbookFocus` Web toggle, made the panel draggable by `textbookFocusQaHead`, and wired `sendTextbookFocusQuestion()` to pass explicit `{ useWebSearch, answerLength }` into `sendLearnFollowup()`. Refactored `sendLearnFollowup(rawPrompt, options = {})` so source-specific controls can override web search and answer length while existing learn/sidebar calls keep current behavior. Adjusted CSS for compact tools row and draggable header. Bumped assets to `style.css?v=1778141200` and `app.js?v=1778141200`. Restarted localhost:9000; current listener PID 28347; `/health` OK.

## 2026-05-07 19:38 CST
- User disliked the Q&A/search loading page because it looked ugly and used AI-ish emoji status lines. Used frontend-design guidance and replaced both main answer and learn-followup loading markup in tutor-openclaw-ui/app.js with buildSearchProgressMarkup(): Chinese, no emoji, structured progress card with three stages (extract section concepts/formulas, search references, synthesize final answer).
- Added matching search-progress-card CSS in tutor-openclaw-ui/style.css: restrained 12px card, subtle top progress sweep, compact stage rows, follow-up bubble sizing. Bumped index.html asset query strings to v=1778142000. Applied the same changes to workspace_tutor_related_full/tutor-openclaw-ui.
- Verification: node --check app.js passed in both project copies; removed old loading strings (Reading Section Objectives / Searching Across the Web / Generating Final Explanation) from the loading path; server restarted on localhost:9000 with PID 29573 and /health ok.

## 2026-05-07 19:47 CST
- User asked whether conversations from both homepage Q&A and page/textbook Q&A should appear under Recent. Conclusion: yes. Existing Recent storage used tutorRecentSessions but saveCurrentLearnSession only saved tutorState.learnHistory, so homepage ask/chatHistory was skipped; textbook focus Q&A routed via sendLearnFollowup and was mostly saved.
- Patched tutor-openclaw-ui/app.js in both main and sync copies: added tutorState.chatSessionStartTime; added buildRecentConversationSnapshot(source) so Recent saves either origin=main from chatHistory as General Q&A or origin=learn from learnHistory as section-specific; saveCurrentLearnSession now uses snapshot and preserves custom title/starred; loadHistoricalSession now branches origin=main to restore answer screen with last user/assistant, sources, book pages, and origin=learn to existing learn restore.
- Main question save now starts a new chatSessionStartTime for fresh homepage conversations, saves after currentBookPages/currentWebSources are set, and saves/clears on back/topbar close. Asset version bumped to v=1778142100. Verification: node --check app.js passed in both copies; server restarted on localhost:9000 PID 30521; /health ok.

## 2026-05-07 20:00 CST
- User requested three answer-view UI fixes: Back icon overlapped/duplicated the close X, follow-up composer was clipped at the bottom, and content source book-page cards should open the referenced page.
- Removed topbar Back button from index.html in both main and sync copies; guarded old backBtn event binding so missing button does not throw. The close X remains the single exit action and still saves/clears current main conversation.
- Added clickable textbook source cards: renderBookSources now outputs button.source-page-link; delegated click opens source-page-modal showing the corresponding tutorState.currentBookPages image with metadata; backdrop/X/Escape close it. Added modal CSS.
- Raised answer follow-up composer with final CSS override: answer col bottom padding, explain-scroll bottom padding, followup bar left/right/bottom offsets, z-index. Bumped assets to v=1778142200. Verification: node --check app.js passed in both copies; server restarted localhost:9000 PID 31199; /health ok; browser reload confirmed #backBtn count 0 and close button present.

## 2026-05-07 20:08 CST
- User said main workspace home felt too thin and asked to add scrapbook/journal elements using frontend-design skill. Read frontend-design and implemented a restrained study-journal layer on the welcome workspace, not the intro landing page.
- Added journal HTML elements in both main and sync index.html: background doodles, Chapter B / Signals Desk sticker, right index tabs (B.1/B.2/Q&A), cover tape, cover note, mini label, formula strip, search-box tape, and side note. These are aria-hidden and pointer-events none so they do not interfere with controls.
- Added final CSS overrides in both style.css: paper grid overlay, folded-corner effect, stickers, index tabs, washi tape, sticky note, title highlight, formula strip, responsive hiding for narrow screens. Bumped assets to v=1778142300. Verification: node --check app.js passed both copies; server restarted localhost:9000 PID 31926; /health ok; browser check found journal elements and screenshot showed no input obstruction.

## 2026-05-07 20:16 CST
- User reported clicking a subsection from overview briefly flashes the old 4-step splash with Agent A/Gemini and Agent B/Claude text before cached lesson appears. Root cause: startLesson() unconditionally called showSplash() and startSplashProgress() before /api/section returned; cached responses were fast but still exposed splash for a fraction of a second.
- Patched startLesson in both main and sync app.js to delay showing splash by 260ms using splashShowDelayTimer; if the section response returns before then, the timer is cleared and splash never appears. If slow, splash appears normally and progresses. hideSplash now clears the delayed timer too.
- Reworded learn-splash text in both index.html copies: removed Agent A/Gemini and Agent B/Claude from subsection loading, replaced with neutral “Organizing the teaching flow…” and “Preparing the explanation…”. Bumped assets to v=1778142400. Verification: node --check app.js passed both copies; server restarted localhost:9000 PID 32505; /health ok.

## 2026-05-07 20:36 CST
- User provided an Academic Scrapbook standalone HTML landing page and asked to replace the old intro page with it. Used frontend-design guidance and replaced the old introLanding markup in both tutor-openclaw-ui copies with a scoped scrapbook-style Aquarius landing page: paper-grid/noise background, sticker nav, highlighted hero typography, textbook cover polaroid, formula sticky note, mini Q&A card, hallucination comparison cards, multi-agent pipeline block, and footer CTA. Preserved functional hooks introGetStartedBtn, introHeroExploreBtn, and introFooterCtaBtn for existing login/workspace flow.
- Added scoped inline CSS in index.html and bumped assets to v=1778143100. Removed stale intro CTA overrides from style.css that forced old blue buttons and conflicted with the new scrapbook design. Verified both project copies are byte-identical for index.html/style.css, node --check app.js passes, localhost:9000 restarted with PID 33442 and /health OK. Browser verification on localhost showed new .scrap-hero-title present, old .ui-mockup/.hero absent, and buttons visible.

## 2026-05-07 20:50 CST
- User reported the answer/loading view showed Chinese even for an English prompt, and wanted the page restyled into a stronger scrapbook/journal look using frontend-design. Root cause: buildSearchProgressMarkup() and several abort/error/live-search labels were hardcoded in Chinese.
- Fixed tutor-openclaw-ui/main+sync app.js so answer/followup loading cards now accept a language parameter and use detectLang(prompt) for English vs Chinese copy. Also localized live source header, extra-source count, abort text, and error title. For English prompts the loading card now says Working / Preparing a grounded answer / Extract section concepts and formulas / Check supporting references / Write the final explanation.
- Restyled the loading card in style.css into a notebook card: grid-paper texture, dashed inset frame, washi tape tab, push-pin, highlighted title, stronger paper shadow, and more tactile step cards. Bumped assets to v=1778143200, restarted localhost:9000 with PID 34867, /health OK, and verified served app.js/style.css include the new language-sensitive copy and scrapbook selectors.

## 2026-05-07 21:xx CST
- Harrison 要求 Codex 读取桌面 `tutor agent` 记忆并接管项目。已确认项目目录为 `/Users/chenghaoxiang/Desktop/tutor agent`，并阅读 `CODEX_PROJECT_GUIDE.md`、`README.txt`、桌面长期记忆 `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md` 以及本项目主记忆 `project-memory/MEMORY.md`。
- 当前接管结论：主工程仍是 `tutor-openclaw-ui/`，优先文件为 `index.html`、`app.js`、`style.css`、`ws-bridge.js`；项目目标、用户偏好、已做进展和关键硬规则均已同步到当前上下文。现已完成接管，可直接继续处理下一步需求。

## 2026-05-07 21:xx+ CST
- Harrison 发现当前新书模式下的 Content Sources 弹窗出现 `book-049 / B.7` 这类明显像旧书链路的来源，质疑为何不是新书检索结果。已先给出判断：问题大概率在“书籍版本过滤”而不是用户误会，可能出在后端检索未严格按 `selectedBook=new` 过滤、已有 lesson cache/source metadata 混入旧书页、或前端 source card / modal 展示时读错 page map。此轮先说明合理性和排查方向，未开始修改。

## 2026-05-07 21:xx++ CST
- Harrison 要求再次确认桌面 `tutor agent` 是否存在，并让我读取桌面记忆后正式接管。已重新核对 `/Users/chenghaoxiang/Desktop/tutor agent`、`/Users/chenghaoxiang/Desktop/Memory`、主记忆 `MEMORY.md`、项目指南 `CODEX_PROJECT_GUIDE.md`、最近几日 memory，以及项目副本 `project-memory`。当前接管结论保持一致：live 主工程仍是 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/`，后续优先看 `index.html`、`app.js`、`style.css`、`ws-bridge.js`；同时继续遵守最新硬规则：先评估再执行、每次对话自动写记忆、重新生成默认先删命中旧缓存、讲解需多例子且知识点能配图就尽快配图。现已完成接管，后续可以直接在这个上下文上继续排查新书 source 混入旧书的问题或其他需求。

## 2026-05-07 21:xx+++ CST
- Harrison 要求删掉两个装饰贴纸区域。已在主工程与同步副本 `tutor-openclaw-ui/index.html` 中精确移除三块对应装饰 DOM：intro landing 教材封面顶部的胶带贴纸、intro landing 右侧 `NOTE TO SELF` 便签贴纸、以及 welcome workspace 右侧竖排 `B.1 / B.2 / Q&A` index tabs 贴纸。此次不涉及业务逻辑，仅做视觉清理；两份工程保持同步。

## 2026-05-07 21:xx++++ CST
- Harrison 指出 learn 右侧 Q&A 提问面板视觉太丑，要求调用 skills 重做手帐风格。已按 `frontend-design` 重做主工程与同步副本：`learn-chat-empty-state` 加入手帐纸页结构、paper clip、page notes 标签、提示 chips；`learn-chat-col` 改为网格纸背景并加侧边装订线；`learn-followup-bar` 改成纸质输入卡风格，保留上传、回答模式、联网和发送的原功能与 id。`index.html` 中 `style.css` 版本 bump 到 `1778143300`。另外排查服务健康检查时发现 `/health` 引用了未定义的 `ocrDir`，已修为返回 old/new indexedPages，重启 localhost:9000 后 `/health` 正常。

## 2026-05-07 21:xx+++++ CST
- Harrison 希望侧边栏增加一些手帐元素，但不要遮挡现有按钮。已继续调用 `frontend-design` 并在主工程与同步副本完成：`index.html` 加入 sidebar 纯装饰节点（顶部胶带、左侧打孔线、右侧 B 页签、手写 study map、底部 keep going）；`style.css` 增加 sidebar journal accents 覆盖层，装饰统一 `pointer-events:none` 与 `aria-hidden`，导航/底部保持更高 z-index，确保不挡点击。当前资源版本为 `style.css?v=1778143500`，基础检查与 `/health` 均正常。

## 2026-05-07 21:xx++++++ CST
- Harrison 进一步指出 welcome 书封上的手帐元素不能遮书。已在主工程与同步副本 `style.css` 中把 `journal-tape-cover` 与 `journal-note-cover` 的定位改为书框外缘，并让 `journal-cover-stack` 使用 `overflow: visible`，使装饰悬在封面外而不是压住书名和封面图。资源版本更新为 `style.css?v=1778143600`，`/health` 继续正常。

## 2026-05-07 21:xx+++++++ CST
- Harrison 要求删掉侧边栏右侧的 `B` 装饰页签。已在主工程与同步副本中移除对应 sidebar journal tab DOM，并删除其 light/dark CSS，资源版本更新到 `style.css?v=1778143700`。健康检查正常。

## 2026-05-07 21:xx++++++++ CST
- Harrison 要求继续删掉 welcome 首页的 `Chapter B / Signals Desk` 贴纸。已在主工程与同步副本中删除 `journal-sticker-date` 的 DOM 与样式，资源版本更新到 `style.css?v=1778143800`，健康检查正常。

## 2026-05-07 21:xx+++++++++ CST
- Harrison 要求删掉 welcome 书封旁边的 `start from the textbook` 便签。已在主工程与同步副本中删除 `journal-note-cover` DOM 与样式，资源版本更新到 `style.css?v=1778143900`，健康检查正常。

## 2026-05-07 21:xx++++ CST
- Harrison 再次要求确认桌面 `tutor agent` 文件夹、读取记忆并接管。已重新定位 `/Users/chenghaoxiang/Desktop/tutor agent`，读取 `CODEX_PROJECT_GUIDE.md`、`README.txt`、项目主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、最近 2026-05-04/06/07 记忆、桌面总记忆 `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md` 与当天摘要。当前接管状态明确：live 主工程是 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/`，核心文件优先级为 `index.html`、`app.js`、`style.css`、`ws-bridge.js`；同步副本是 `workspace_tutor_related_full/tutor-openclaw-ui/`；后续排查或改动应继续同步两边，并保持先评估再执行、每次对话写记忆、重新生成先删命中旧缓存、讲解多例子且能配图就贴近知识点配图等硬规则。

## 2026-05-07 21:xx+++++ CST
- Harrison 提供新的手绘水瓶座贴纸 SVG，要求替换所有 Aquarius 水瓶座图标，包括网页标签和介绍页。已使用 `frontend-design` 处理主工程与同步副本：新增统一图标文件 `aquarius-logo.svg`；将 active `index.html` 的 favicon 从旧 `favicon.png` 改为 `aquarius-logo.svg?v=1778143500`；将 intro landing 顶部 logo、sidebar 品牌图标、login 页品牌 mark 全部改为引用同一 SVG；同步调整 `style.css`，去掉旧玻璃方块容器，让新贴纸图标以真实手帐贴纸形态显示。已验证 active 两份 `index.html/style.css` 完全同步、`app.js` 语法检查通过、9000 服务重启成功，`/aquarius-logo.svg` 返回 `image/svg+xml`。旧 backup/orig 文件内仍保留历史 favicon 引用，但 active 页面已无 `favicon.png` 引用。

## 2026-05-07 21:xx++++++ CST
- Harrison 指出上一步不能擅自把他给的原版前端结构“优化改样”，特别不能动介绍页封面图和肉色方格纸背景。已纠偏：保留 `latest_cover.png` 教材封面与 intro landing 的 `#FDFBF7` 方格纸背景不动；页面内 intro 顶部 logo、sidebar 品牌 logo、login mark 均改为直接使用 Harrison 原版手绘贴纸 SVG 的内联结构，而不是简单 `<img aquarius-logo.svg>` 裸替换；favicon 仍用同图形的 `aquarius-logo.svg` 文件承载。两份 active `index.html/style.css` 已同步一致，语法检查通过，9000 服务重启到 PID 46871。后续处理用户给的前端代码时必须严格保持原版素材、背景、布局语义，只做必要接入和小幅 polish，不得替换图片或重新解释视觉方向。

## 2026-05-07 21:xx+++++++ CST
- Harrison 确认继续让我按“更严格贴近原版”的方向收口。已进一步对齐 active 前端：1) intro 顶部 `scrap-logo` 恢复为更接近原版的白色贴纸卡比例、字重和字间距；2) sidebar 品牌区恢复为同一套贴纸卡组件，而不是只剩裸 SVG；3) 继续保留 `latest_cover.png` 与 `#FDFBF7` 方格背景不动；4) 主工程与同步副本 `index.html/style.css` 已再次确认 byte-level 一致，当前 `style.css?v=1778143700`。后续若 Harrison 继续给局部原版代码片段，应优先直接按片段落地，而不是先抽象成“统一组件”再回填。

## 2026-05-07 21:xx++++++++ CST
- Harrison 提供完整 `demo.html` 原版 landing 代码，并明确这是原版代码。已按原版优先原则迁入项目 intro landing：扩展 Tailwind config，加入 `font-title/body/hand/mono`、`ink/paper/highlighter/tape` 色值、`shadow-paper/polaroid/chunky`、`float/ wiggle` 动画；将 `#introLanding` 替换为原版 Academic Scrapbook 结构，包括固定胶带导航、hero 标题 `An AI Tutor That Truly Gets You.`、Unsplash 数学图 polaroid、NOTE TO SELF 便利贴、对比卡、多智能体黑板管线和撕裂 footer。保留项目功能钩子 `introGetStartedBtn`、`introHeroExploreBtn`、`introFooterCtaBtn`，保留 favicon 使用 `aquarius-logo.svg`。主工程与同步副本一致，语法检查通过，9000 服务重启到 PID 49013，served HTML 已确认包含原版关键节点。注意：本次按 Harrison 最新完整原版代码执行，所以 intro hero 图片使用原版 Unsplash URL，而非此前本地 `latest_cover.png`。

## 2026-05-07 21:xx+++++++++ CST
- Harrison 反馈 intro 背景仍不是格子。已定位根因：`style.css` 中旧规则 `body.intro-active .intro-landing-new:not(.hidden)` 比普通 `.intro-landing-new` 更具体，仍把背景覆盖成旧点阵。已在 active `index.html` 内补同等/更高优先级覆盖，把 `body.intro-active .intro-landing-new:not(.hidden)` 也强制设回原版 `#FDFBF7` + 24px 双向 linear-gradient 方格 + noise texture。主工程与同步副本一致，served HTML 已确认包含该覆盖。

## 2026-05-07 21:xx++++++++++ CST
- Harrison 指定 intro 页面左上角那块白色牌子里的星星图标也要换成他给的水瓶座贴纸图标。已在 active 主工程与同步副本 `index.html` 中仅替换该处图标，将原 `✨` 改为内联手绘 Aquarius SVG，保留白色牌子、Aquarius 字标、版式和按钮不动。两份文件再次同步一致并通过 HTML 解析检查。

## 2026-05-07 22:xx CST
- 已完成新书模式混入旧书 Content Sources 的修复并验证。根因是三层：1) `/api/ask` 自由检索一直用旧书全局 `BOOK_INDEX`；2) 前端切书时没有清掉上一书种的 `currentBookPages`/`learnBookPages`；3) 静态书页路由 `/pages/:filename` 会优先命中旧书目录，导致同名页可能显示成旧书页。已把主工程与同步副本都改为 `BOOK_INDEX_OLD/NEW` 分离、按 `bookSource` 选索引、拒收与当前书种不匹配的残留 `bookPages`，并新增显式 `/old-pages/` 和 `/new-pages/` 路由；前端切书时会清空残留书页上下文。已用真实 `/api/section` 与 `/api/ask` 验证，新书模式下返回的 sources 全是 `page-*`，图片全走 `/new-pages/*`，问题已修好。

## 2026-05-07 22:xx+ CST
- Harrison 反馈 answer view 底部 follow-up 对话框没有完全露出来，被底部边框裁切。已在主工程与同步副本 `style.css` 末尾新增 Answer composer visibility guard：为 `.answer` 定义 `--answer-composer-height`，让 `.explain-scroll` 预留足够 bottom padding，并将 `.answer .followup-bar` 固定在弹窗内部安全距离（bottom 34px、min-height 78px、max-height 118px、z-index 60），同时设置输入框最小高度和移动端安全间距。已 bump 两份 `index.html` 的 `style.css/app.js` 资源版本到 `v=1778143500`，服务器已确认端出新 CSS。

## 2026-05-07 22:xx++ CST
- Harrison 要求把左侧 Settings 的默认 emoji 齿轮改成手帐风格齿轮。已在主工程与同步副本 `index.html` 中把 `⚙️` 替换为可控 SVG 结构 `.sidebar-settings-sticker`，并在 `style.css` 末尾新增 Journal-style settings gear sticker 样式：淡蓝贴纸外圈、虚线纸质边、米白内圈、棕灰色手帐线条齿轮、小黄色贴点和 hover 轻微旋转；同时保留暗色主题适配。当前服务器已端出 `style.css?v=1778143600` 和新的 `.sidebar-settings-sticker` HTML。

## 2026-05-07 22:xx+++ CST
- Harrison 改主意，要求把刚才的手帐 SVG 齿轮换回指定的 `⚙️`。已在主工程与同步副本 `index.html` 中恢复 `⚙️`，并从两份 `style.css` 移除 `.sidebar-settings-sticker` / Journal-style settings gear sticker 相关样式，避免贴纸外圈残留。资源版本已统一到 `style.css?v=1778143700` 与 `app.js?v=1778143700`，服务器确认已端出 `⚙️` 且无 `sidebar-settings-sticker`。
# 2026-05-07 late update

- Textbook tab mapping has been upgraded from page-only mapping to page-plus-anchor mapping.
- New generated file: `section-page-anchor-new.json` in both the primary UI project and the synced copy.
- Each section/subsection now gets:
  - display pages from `section-page-map-display-new.json`
  - a first-page start ratio from OCR heading detection in `new-book-ocr/page-*.txt`
- Frontend `app.js` now prefers the display map and scrolls the inline Textbook viewer to the section anchor on first render, instead of always pinning to the top of the first page.
- This was added specifically to prevent cases like:
  - `B.3-1` opening at visible `B.2` content on `page-020`
  - `B.3-2` opening at visible `B.3-1` content on `page-022`
- This rule should be treated as the default for every section, not a special-case patch for B.3.

## 2026-05-07 23:22 CST
- Harrison interrupted the B.4 generation request and clarified two workflow rules that must be remembered before continuing lesson generation:
  1. Future lesson generation should follow the corrected HW1 handling approach: use homework/exam relevance as a guide, but filter/distill it by the current section/topic instead of letting HW1 globally pull unrelated topics too hard. For each section, only HW1 concepts that actually match that section/topic should influence emphasis, examples, common mistakes, and key takeaways.
  2. The section-switch loading card in the lesson page must be removed completely. When switching subsections, do not show the central staged loading card / agent-planning progress panel. Keep the UI direct and avoid accumulating redundant “loading card” code paths.
- Pending next implementation: remove that subsection-switch loading-card logic cleanly from `app.js`/styles in both the primary project and synced copy, then continue generation (e.g. B.4 all lessons) using the section/topic-filtered HW1 approach.

## 2026-05-07 23:xx CST
- Harrison 再次要求确认桌面 `tutor agent` 文件夹、读取记忆并接管。已定位项目为 `/Users/chenghaoxiang/Desktop/tutor agent`，读取主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、记忆规则、`README.txt`、当天最新摘要，并检查核心目录结构。
- 当前接管结论：主工作目录为 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui`；完整同步/归档副本为 `workspace_tutor_related_full/tutor-openclaw-ui`。两边核心文件 `app.js`、`index.html`、`style.css` 的 SHA-256 完全一致，可继续保持双副本同步。
- 运行入口：`tutor-openclaw-ui/run-bridge.sh` 或 `node ws-bridge.js`，默认端口 `9000`；后端会从项目根优先解析 `workspace_tutor_related_full/tutor-materials`。最新待办保持不变：先清理 subsection-switch loading card，再继续 B.4 生成，并按“当前 section/topic 过滤 HW1 相关性”的规则处理内容。

## 2026-05-07 23:xx+ CST
- Harrison 反馈 bug：在 Learn 页切到 Textbook tab 后，再点左侧章节/子章节，主区域仍停留在 Textbook 页面，不会跳回章节讲义。根因是 `_learnViewMode` 的 `textbook` 状态会跨章节点击持久保留，`openLearnMode/openChapterOverviewMode` 只更新 section/page 状态，没有显式恢复 Lecture。
- 已在主工程与同步副本 `app.js` 修复：进入章节概览或具体 lesson 时先 abort 旧 lesson 请求、关闭 textbook focus，再设置 `_learnLayoutMode` 并调用 `_setLearnMode('lecture')`，确保左侧章节点击一定进入章节讲义页面；同时将 active `index.html` 的 `app.js` 版本 bump 到 `1778144100` 防浏览器缓存。
- 验证：两份 `app.js/index.html/style.css` 保持同步；`node --check app.js` 通过；`localhost:9000/health` 正常；served HTML 已引用 `app.js?v=1778144100`，served JS 包含新的 `_setLearnMode('lecture')` 逻辑。

## 2026-05-07 23:xx++ CST
- Harrison 指出 textbook 头尾边界页裁剪仍有问题：例如 `B.1-1` 的正文尾巴延伸到下一页 `page-005` 顶部，但当前 Textbook tab 只显示到 `page-004`，漏掉尾巴；同时 lesson 生成时又不能把下一节 `B.1-2` 的 OCR 正文混进去。
- 已解释当前流程：后端生成页范围主要走基础 `section-page-map-new.json`，其中 `b.1-1` 已包含 `page-005`；前端 textbook 显示优先走 `section-page-map-display-new.json`，该文件由 meta 的 section/subsection 归属生成，`page-005` 被归给 `B.1-2`，因此 display map 把 `B.1-1` 的尾页裁掉。OCR 侧已有 `focusOcrTextForSection`，会尝试按 heading 裁文本；对 `page-005` 的 `B.1-1`，它可在遇到 `B.1-2 Algebra...` 前截断，理论上不会把 B.1-2 正文喂给生成。
- 拟定改法：把“视觉页范围”和“OCR正文切片”彻底分离。视觉 display map 不再收缩基础 map，允许相邻 section 共享边界页；OCR 则新增/强化 per-section per-page boundary slice，只把本节 heading 到下一节 heading 之间的文字喂给 Agent A/B，并用同样边界过滤 figures，防止下一节图/正文混入。

## 2026-05-07 23:xx+++ CST
- 已按 Harrison 指令落地头尾边界页修复。`build-section-page-display-map.js` 现在让 `section-page-map-display-new.json` 直接保留基础 `section-page-map-new.json` 的页范围，所以视觉 Textbook tab 允许相邻章节共享边界尾页；例如 `b.1-1` display pages 现在是 `page-001..page-005`。
- 同一脚本现在也重新生成严格 `section-figure-map-new.json`：按 OCR heading 边界和 figure 纵向位置判断图属于哪一节，避免边界页里的下一节图串入上一节。验证结果：`b.1-1` 只允许 `page-004-unknown-1.png`，不再允许 `page-005-fig__b_2-1.png`；`b.1-2` 仍允许 `page-005-fig__b_2-1.png` 等本节图。
- 修复后端 OCR 标题识别 bug：`looksLikeSectionHeadingLine` 和 `extractHeadingCodeFromLine` 现在支持 `B.1-2` 这种带字母点号的 section code。之前不支持导致 `page-005` 对 `B.1-1` 裁剪时没识别下一节标题，可能把 `B.1-2` 正文混入。验证后 `B.1-1/page-005` 的 OCR slice 只剩 Country X/Y 尾巴，不含 `B.1-2 Algebra`、`Figure B.2` 或公式。
- 主工程与同步副本均已更新：`build-section-page-display-map.js`、`section-page-map-display-new.json`、`section-page-anchor-new.json`、`section-figure-map-new.json`、`app.js`、`index.html`、`ws-bridge.js`。`app.js/index.html` 资源版本 bump 到 `1778144200`，bridge 已重启，`/health` 正常；live `/api/section` 验证 `B.1-1` bookPages 返回 `page-001..page-005`。

## 2026-05-07 23:xx++++ CST
- Harrison 询问已经生成的讲解是否已经包含修复后的 OCR、是否需要重新生成。已检查当前 live cache key 固定为 `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`；`B.1-1` active cache 文件生成时间为 `2026-05-07 18:52:58`，早于 23 点后的 display/OCR/figure 边界修复，所以已有 markdown 不会自动获得新裁剪逻辑。
- 内容检查：当前 `B.1-1` active cache 没有明显包含 `B.1-2 Algebra`、`Fig. B.2` 或 `page-005-fig__b_2-1.png`，因此看起来没有被下一节污染；但它是在旧 heading 识别/旧 figure map 条件下生成的，不是严格新流程产物。live `/api/section` 现在仍命中这个 cached lesson，同时 bookPages 已返回 `page-001..page-005`。
- 建议规则：如果要保证“页码包含尾巴 + OCR 只含本节 + figure 不串节”的一致性，至少重新生成 `B.1-1`。`B.1-2` active cache 是 `2026-05-07 16:32:50` 生成，内容以 B.1-2 为主且正确使用 `Fig. B.2`，污染风险较低，但若要全套新规则一致，也应在后续批量重生边界页相关小节时一起重生。

## 2026-05-07 23:xx+++++ CST
- Harrison 要求 Recent 面板中被省略号截断的会话标题在鼠标悬浮时显示全名。已在主工程与同步副本 `app.js` 修改 Recent item 渲染：保留 `fullTitle` 与 `displayTitle` 分离，卡片 `title` 和标题节点 `title` 均设为完整标题，`aria-label` 也包含完整标题；section 行也加完整 `title`。这样 hover 卡片/标题/section 时都可看到完整文本，不再只显示固定的 `Open conversation`。
- `index.html` 的 active `app.js` 版本 bump 到 `1778144800`；两份 `app.js/index.html` 哈希一致，`node --check app.js` 通过，localhost 已端出新脚本版本。

## 2026-05-08 00:xx CST
- Harrison 提出新的结构性教材边界问题：有些章节如 `B.2`，从父级大标题 `B.2 Sinusoids` 到第一个小节 `B.2-1 Addition of Sinusoids` 之间还有重要讲解和公式；这些内容不是尾页裁剪问题，而是 parent section prelude / 导言区问题。
- 初步判断：不能丢，也不能无差别塞进所有子小节。应把父级导言区作为 `section prelude` 单独识别：父章节 overview 展示它；第一个子小节生成时把它作为 “required setup” 附加到 OCR 前面；后续子小节只给极短 prelude summary，避免重复和串节。相关缓存如 `B.2-1` 若已生成，需要按新规则删除命中旧 cache 后重新生成。

## 2026-05-07 23:xx+++ CST
- Harrison 要求确认桌面 `tutor agent` 文件夹、读取记忆并正式接管。已确认目录存在于 `/Users/chenghaoxiang/Desktop/tutor agent`，并读取 `README.txt`、`CODEX_PROJECT_GUIDE.md`、项目主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、最新 `2026-05-06.md` / `2026-05-07.md`、以及桌面全局记忆 `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md`。
- 当前接管结论：live 主工程仍是 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui`，同步副本是 `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-openclaw-ui`；后续涉及前端/后端运行态改动应继续同步两边。localhost:9000 当前由 node PID 49604 监听，`/health` 返回正常，old/new indexedPages 分别为 104/149。
- 最新待办/上下文：优先处理 textbook 头尾边界页问题，把视觉 display map 与 OCR section slice 分离；同时记住上一条待办中用户要求移除 subsection-switch loading card，并继续按“当前 section/topic 过滤 HW1 相关性”的方式生成后续 lesson。继续遵守：先评估再执行、每次对话写记忆、重新生成先删当前命中 cache、讲解多例子且可配图时贴近知识点配图。

## 2026-05-07 23:40 CST
- Harrison 发截图指出左侧 sidebar 顶部 Aquarius 品牌贴纸右侧被嵌进/裁掉。已使用 `frontend-design` 做局部视觉修正：将 `.sidebar .logo` 从 `width: fit-content` 改为 sidebar 内自适应宽度并设 `max-width: 204px`、`box-sizing: border-box`，把品牌图标从 54px 缩到 46px、字标从 25px 缩到 22px，menu 按钮从 40px 缩到 36px，并收窄 gap/padding，让倾斜贴纸不再被右侧 `overflow:hidden` 裁切。
- 修改已同步主工程与副本 `style.css`，并将两份 `index.html` 的 CSS 版本 bump 到 `style.css?v=1778144200`。验证：两份 active `index.html/style.css` 同步一致，localhost:9000 已端出新版 HTML/CSS，新 CSS 包含 `max-width: 204px`、`font-size: 22px`、`flex: 0 0 36px` 等规则。

## 2026-05-07 23:xx+++ CST
- Harrison 在当前 Codex 会话中要求确认桌面 `tutor agent` 文件夹、读取记忆并接管。已重新定位 `/Users/chenghaoxiang/Desktop/tutor agent` 与桌面全局 `/Users/chenghaoxiang/Desktop/Memory`，读取项目 `CODEX_PROJECT_GUIDE.md`、`README.txt`、项目主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、当天最新 `project-memory/2026-05-07.md`，以及桌面全局当天记忆。
- 当前接管结论：主工程仍是 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui`，同步副本是 `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-openclaw-ui`；核心文件优先 `index.html`、`app.js`、`style.css`、`ws-bridge.js`。最新重要上下文包括：新书/旧书 Content Sources 混源已修；Textbook tab 已升级 page-plus-anchor 映射；Learn 页从 Textbook tab 切章节需回 Lecture 已修；当前待办是清理 subsection-switch loading card，以及处理 Textbook 视觉页范围与 OCR 正文切片分离，避免边界页视觉漏内容但生成又混入下一节。
- 已同步硬规则：默认中文、禁止韩语、每次对话写记忆、用户偏好先评估再执行、重新生成默认先删命中旧缓存、讲解必须多例子且能配图就贴近知识点配图。此轮只做接管确认与记忆读取，未改业务代码。

## 2026-05-07 23:xx++++ CST
- Harrison 发截图指出 Learn 右侧 Q&A 面板里出现两个网页来源区域：上方 `Web References` 和下方回答气泡内重复的来源块；同时要求把上方来源布局和下方回答文字排版改成更手帐风格。已使用 `frontend-design` 处理主工程与同步副本：1) 在 `app.js` 中移除 follow-up 最终回答里对 `renderHyperknowLinks(data.webSources)` 的重复拼接，新回答只保留正文，来源统一由上方 `learnWebSection` 显示；2) 在 `style.css` 末尾新增 `Learn Q&A journal answer polish` 覆盖，把 `learn-web-section` 改成纸质索引卡/胶带样式，把 `learn-web-card` 改成纵向清单卡片，把 `.followup-bubble` 的问题与回答改成手帐纸条式排版，并用 CSS 兜底隐藏旧消息中已经存在的 `.hyperknow-links-container` / loading live sources；3) `index.html` 资源版本 bump 到 `1778144500`。
- 已同步两份 active 工程：`tutor-openclaw-ui` 与 `workspace_tutor_related_full/tutor-openclaw-ui` 的 `index.html/style.css/app.js` 均保持一致；`node --check app.js` 通过，`localhost:9000/health` 正常。

## 2026-05-07 23:xx+++++ CST
- Harrison 发截图指出 Learn 右侧 `Web References` 来源卡片里长标题会超出方格边界。已在主工程与同步副本 `style.css` 末尾新增 `Web reference overflow guard`：对 `.learn-web-card` / `.lwc-head` / `.lwc-title` 设置 `min-width:0`、`max-width:100%`，标题开启 `overflow-wrap:anywhere`、`word-break:break-word`、`hyphens:auto`，并用两行 `-webkit-line-clamp` + ellipsis 限制高度，favicon 固定不被挤压。`index.html` 资源版本 bump 到 `1778144700`。验证：两份 active 工程保持同步，`node --check app.js` 通过，`localhost:9000/health` 正常。
