
## 2026-05-08 early CST
- Harrison 要求在左侧增加一个用于收集用户建议的入口，原本想叫 `Advice of this web`，已建议并采用更自然的名称 `Feedback Board`。功能目标：用户可以给 Aquarius/Tutor Agent 提建议，Harrison 后续自行查看并据此修改；同时它是公开讨论区，大家能看到别人给的建议并在下面讨论。
- 已在主工程与同步副本实现完整轻量版：左侧 sidebar 新增 `Feedback Board` 按钮；新增 `feedbackView` 页面，包含左侧建议发布卡和右侧公开讨论列表；前端 `app.js` 支持加载建议、发布建议、在建议下回复、刷新列表，所有用户输入都通过 `escapeHtml` 渲染；样式 `style.css` 使用手帐建议墙风格，纸张网格、胶带、图钉、回复纸条，并支持 dark theme。
- 后端 `ws-bridge.js` 新增 JSON 持久化接口：`GET /api/feedback`、`POST /api/feedback`、`POST /api/feedback/:id/replies`；数据保存在 `tutor-openclaw-ui/users/feedback-board.json`，字段有长度清理，最多保留 300 条建议和每条 200 条回复。已重启 localhost:9000 到新进程并实测发建议/回复接口成功；随后删除 smoke test 数据，当前 board 为空干净状态。
- 资源版本更新为 `style.css?v=1778144900` 和 `app.js?v=1778144900`。验证：主工程与 `workspace_tutor_related_full/tutor-openclaw-ui` 的 `index.html/style.css/app.js/ws-bridge.js` hash 完全一致；`node --check app.js/ws-bridge.js` 通过；`/health` 正常。

## 2026-05-08 01:25 CST
- Harrison 明确父章节（如 `B.2`）行为：点击父章节时仍然展示 overview 页面和 “Choose your next piece” 小节卡片，但如果 OCR 中父章节大标题到第一个小节标题之间存在实质前言内容，就要像小节一样生成一段父章节讲解插在卡片上方；是否需要生成由 OCR 自动判断。期望示例：`B.1` 不需要，`B.2` 需要，`B.3` 不需要。
- 已在 `tutor-openclaw-ui` 主工程与同步副本实现：`ws-bridge.js` 增加父章节前言 OCR 裁剪与判断，入口只接受真正父章节标题行，排除页眉（如 `B.2 Sinusoids 19`）和子标题（如 `B.2-1`）；实测分类为 `B.1=false`、`B.2=true`、`B.3=false`。
- 父章节前言生成使用独立 lesson cache variant：`new__parent_prelude__aquarius_visual_latex_v2...`，避免误命中旧的全章 `b_2` 缓存；已生成并验证 `b_2/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，内容包含 `B.2` 前言公式/周期/相位讲解，未混入 `B.2-1` 或 `B.2-2` 小节正文。
- 前端 `app.js` 新增 overview prelude loader/render path：父章节页面先显示 `Preparing chapter notes...`，后端若返回前言 lesson 就渲染在 `Choose your next piece` 上方；若无实质前言则保持原 overview 卡片页。`style.css` 增加 prelude 区块样式；`index.html` 资源版本更新到 `1778145100`。`node --check app.js/ws-bridge.js` 通过，`localhost:9000` 已重启并 `/health` 正常。
- Harrison 进一步澄清：所谓 `B.1` 不生成，指的是 `B.1` 大标题到 `B.1-1` 标题之间那段父章节前言因为字数少、重点弱或只是概览，所以不要生成 `B.1` 父章节讲解；不是说重新生成包含所有 `B.1-*` 小节的整章 lesson。曾按“重新生成 B.1 整章讲解”的字面意思启动了一次 `B.1` active lesson 生成，随后已删除误生成的 `b_1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，并确认 `/api/section mode=overview B.1` 返回 `hasPrelude:false`。
- Harrison 最终定义“生成/重新生成某章整章讲解”：先判断该父章节前言（父标题到第一个小节标题之间的 OCR）是否需要生成；需要就生成父章节 `parent_prelude` 讲解，不需要就跳过父章节；然后生成该父章节下所有小节的讲解。以后每一章都按这个含义执行。多窗口并行生成时必须避免冲突：同一个 section 不重复删/生成；不同 section 可并行，但写/删缓存前要确认目标文件没有被另一个窗口刚写好。
- 已按新定义完成 `B.1`：父章节 `B.1` overview 判定 `hasPrelude:false`，不生成父章节讲解；重新生成小节 `B.1-1 A Historical Note` 和 `B.1-2 Algebra of Complex Numbers`，active cache 分别为 `b_1-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`（约 16.9KB）和 `b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`（约 28.5KB）。通过 `/api/section mode=lesson` 验证两者均 `cached:true`。
- Harrison 要求生成 `B.2` 章节讲解。按“整章生成”定义检查：`B.2` 父章节前言 `hasPrelude:true`，`b_2/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 已存在且 overview `cached:true`；小节只有 `B.2-1 Addition of Sinusoids` 和 `B.2-2 Sinusoids in Terms of Exponentials`，两者 active cache 已存在并通过 `/api/section mode=lesson` 验证 `cached:true`。由于用户说“生成”而非“重新生成”，未删除或重跑已有 cache，避免多窗口冲突。
- Harrison 反馈 `B.2` 页面看不到父章节前言讲解。确认后端确实返回 `hasPrelude:true` 且 lesson cache 命中，问题是前端展示顺序/首屏高度：旧 `Chapter Map` hero 卡片在最上方太高，前言被插在 hero 下方，导致看起来像没显示。已修改 `app.js/style.css`：当 overview 有 `preludeHtml` 时给 shell/hero 加 compact class，将 hero 缩成轻量头信息，父章节前言讲解会更快出现在首屏上方；资源版本更新至 `1778145200`，并同步主工程与 `workspace_tutor_related_full/tutor-openclaw-ui`。
- Harrison 再次反馈仍看不到 `B.2` 前言，截图显示“大 Chapter Map hero + Preparing chapter notes...”。核实 `localhost:9000` 实际已返回 `1778145300/5400` 新静态文件，且 `/api/section overview B.2` 秒回 `hasPrelude:true,cached:true,lessonLen=34654`。最终判定截图来自旧前端 bundle/旧页面内存：新版本在 `preludeLoading` 阶段也不再显示大 hero。已进一步改为：有 `preludeHtml` 或 `preludeLoading` 时完全不渲染 hero，只显示前言面板/加载面板和小节卡；`loadChapterOverviewPrelude` 加 12s 超时与可见错误提示，避免永远停在 Preparing。资源版本更新至 `1778145400` 并同步。
- Harrison 第三次反馈：新版已没有大 hero，但仍停在 `Preparing chapter notes...`，说明静态文件已更新但前端异步替换没执行或被渲染增强步骤打断。已修 `app.js`：`renderChapterOverviewContent` 的每个 enhancement step 加 try/catch，避免某个装饰器报错阻断后续 `loadChapterOverviewPrelude`；`loadChapterOverviewPrelude` 改为拿到 lesson 后优先只替换 `#chapterOverviewPreludeMount` 局部节点，不再重渲染整页，missing/error 也局部替换。资源版本更新至 `1778145500`。后端 `/api/section overview B.2` 继续验证 `hasPrelude:true,cached:true,lessonLen=34654`。
- Harrison 反馈 `B.2` 前言虽然显示了，但没有分页，且内容里出现两个编号 `2.`。已修前端：父章节前言不再作为 overview 长文塞进 `chapterOverviewPreludeMount`，而是用 `setLearnLessonContent()` 走小节同一套 lesson 分页/上一页下一页；`Choose your next piece` 小节选择卡片通过 `extraHtml` 挂在父章节前言第 1 页下方。新增 `lesson-page-extra` 样式。已修缓存：`b_2/new__parent_prelude__aquarius_visual_latex_v2...en.md` 中重复的 `## 2. Compressing Cosine Plus Sine Into One Sinusoid` 从 2 个清到 1 个。资源版本更新至 `1778145600` 并同步。
- Harrison 随后指出父章节前言页没有翻页按键。原因是父章节前言虽然已经进 lesson renderer，但 `_learnLayoutMode` 仍属于 overview，`_setLearnMode()` 把 `lecturePrevOverlayBtn/lectureNextOverlayBtn` 和 bottom page indicator 都隐藏了。已加 `overview_lesson` 布局模式：保持 overview 的单栏布局与隐藏右侧列，但视为 `lesson-like`，会显示翻页按钮、页码和 lecture 分页控件。资源版本更新至 `1778145800` 并同步。
- Harrison 建议父章节/大章节前言也应该有试题测试。截图显示 `B.2` 父章节前言第 5/5 页空白，原因是 cache 里只有 `.kc-quiz-plan`，没有小节讲解会追加的 `.lesson-test-banner`/`startTestBtn`，quiz page 被解析出来但没有启动测试 UI。已抽出 `buildLessonTestBannerHtml()` 复用小节测试入口，并在 `loadChapterOverviewPrelude()` 给父章节前言 lesson 也追加测试 banner；版本更新至 `1778145900`。现在大章节前言最后一页应显示 “Ready to test your knowledge?” 和 Start Pre-generated Challenge。

## 2026-05-08 memory read
- Harrison 要求“读记忆”。已读取 Tutor Agent 项目主记忆 `MEMORY.md`、今日项目记忆 `project-memory/2026-05-08.md`、以及全局记忆 `/Users/chenghaoxiang/Desktop/Memory/memory/2026-05-08.md`。当前接管重点：继续遵守中文回复、每次对话写记忆、重新生成先定位并替换命中缓存、讲解多例子且能配图就配图；项目最新完成项包括 Feedback Board 左侧入口/公开讨论区，以及 B.2 父章节前言独立缓存逻辑。
- Harrison 追问是否读到“多窗口并行”规则。已检索并确认今日项目记忆中有明确记录：生成/重新生成整章时，不同 section 可以多窗口并行，但同一个 section 不能重复删/生成；任何写/删缓存前都要确认目标文件没有被另一个窗口刚写好，避免并行冲突。
- Harrison 指出“今天的记忆还有好多遗漏”。已完整重读今日项目记忆与全局今日记忆，并补齐先前漏掉的关键上下文：生成/重新生成某章整章讲解的正式定义是“先判断父章节前言是否需要 parent_prelude，需要则生成，不需要跳过；然后生成该父章节下全部小节”；`B.1` 已按此定义完成，父章节 `hasPrelude:false`，小节 `B.1-1` 与 `B.1-2` 已生成 `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` active cache 并通过 `/api/section mode=lesson` 验证 `cached:true`。

## 2026-05-08 B.3 full chapter generation
- Harrison 要求生成 `B.3` 整章讲解。已按今日定义执行：先请求 `/api/section mode=overview B.3`，确认父章节 `hasPrelude:false`，因此不生成父章节 `parent_prelude`；随后确定 `B.3` 下只有两个小节：`B.3-1 Monotonic Exponentials`（pages `page-020,page-021`）和 `B.3-2 The Exponentially Varying Sinusoid`（page `page-022`）。
- 生成 `B.3-1` 第一轮被 lesson 格式校验拦下，错误为 `horizontal_formula_strip`，根因是生成稿中残留 `\qquad` 被判定为横向公式长条。已在主工程与同步副本 `ws-bridge.js` 的 `normalizeMathMarkdown()` 中增加小型兜底：将剩余 `\qquad` 规范化为较窄的 `\quad`；两份 `node --check ws-bridge.js` 通过，并重启 `localhost:9000` 到 PID `68474`，`/health` 正常。
- 重跑后 `B.3-1` 与 `B.3-2` 均成功生成 active cache：`b_3-1/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`（2026-05-08 02:06:06，约 23.8KB）和 `b_3-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`（2026-05-08 02:09:43，约 24.9KB）。验证 `/api/section mode=lesson` 返回两者均 `cached:true`；`B.3-1` book pages 为 `page-020,page-021`，`B.3-2` 为 `page-022`。
- 注意：生成接口返回的 webSources 里曾出现泛化/跑偏来源（如 Wikipedia/AI、Organ-on-a-chip、Duga radar），但最终落盘 lesson cache 通过 `rg` 检查未包含这些来源标题，接口读取 cache 时 `webSources=0`，正文未见污染。

## 2026-05-08 textbook/lesson page-map audit
- Harrison 发现 `B.3-1` Textbook tab 少了尾巴：`page-021` OCR 末行断在 “Continuing in this”，真正尾巴在 `page-022` 前 6 行，而 `B.3-2` 从 `page-022` 第 9 行才开始。已确认这是 page map 问题，不是截图/前端误会。已把主工程与同步副本的 `section-page-map-new.json` 和 `section-page-map-display-new.json` 中 `b.3-1` 改为 `page-020,page-021,page-022`，并重生 `B.3-1` active lesson cache。
- Harrison 进一步澄清：要全量走一遍 `Textbook` 映射和喂给讲解生成的映射，不能让生成输入多或少。已确认当前架构：前端 Textbook tab 和生成链都读取 `section-page-map-new.json`；生成链在 `agentA_plan()` 内会对每页 OCR 调 `focusOcrTextForSection()` / `cropOcrTextToSectionBoundaries()`，因此允许 Textbook/section map 做整页重叠，同时实际喂给讲解的 OCR 会裁到当前 section 标题边界，避免把下一节正文喂进去。
- 已写脚本按“同父章节相邻小节”规则全量审计新书 section page map：如果下一小节标题在其第一页第 6 行之后，则上一小节需要包含该页尾巴；如果下一小节标题在页首（如第 3 行），上一小节不应包含该页。修正结果：保留/补齐 `b.2-1 -> page-020` 与 `b.3-1 -> page-022` 等必要重叠；删除过度重叠 `b.5-2 -> page-031`、`b.8-3 -> page-055`、`b.8-7 -> page-057`、`b.8-8 -> page-058`。修正后全量边界审计 `bad=0`，`section-page-map-new.json` 与 `section-page-map-display-new.json` 无 mismatch，四份 JSON 均 parse OK。
- 已重启 `localhost:9000` 到 PID `71173`，`/health` 正常。接口抽验：`B.2-1` 返回 `page-018,page-019,page-020`，`B.3-1` 返回 `page-020,page-021,page-022`，`B.5-2` 返回 `page-027,page-028,page-029,page-030`，`B.8-3` 返回 `page-054`。另外重生了 `B.2-1` active lesson cache，使其按修正后的 page map 重新生成。
- Harrison 追问刚才生成的讲解是否全都要重新生成。结论：不需要全量重生，只需要重生 page map 改动影响到“生成输入”的小节；本轮已经重生了 `B.2-1` 和 `B.3-1`。刚生成的 `B.3-2` page map 未变，不需要重生。后续若要清理历史缓存，受 page map 删除过度重叠影响且应择机重生的旧小节是 `B.5-2`、`B.8-3`、`B.8-7`、`B.8-8`。

## 2026-05-08 full memory report
- Harrison 要求读取今天所有记忆并汇报。已完整读取今日项目记忆 `workspace_tutor_related_full/project-memory/2026-05-08.md` 和全局今日记忆 `/Users/chenghaoxiang/Desktop/Memory/memory/2026-05-08.md`，并确认当前 `localhost:9000 /health` 正常。汇报重点包括：Feedback Board 已完成；父章节前言与整章生成定义已确立；B.1/B.2/B.3 的生成状态；B.2 前言 UI 多轮修复；B.3 生成与格式兜底；全量 textbook/lesson page map 审计；哪些缓存已重生以及哪些历史缓存可择机重生。

## 2026-05-08 Background regeneration
- Harrison 要求把所有 Background 章节重新生成。已按今日“整章生成”定义执行：父章节先判断是否有 OCR parent prelude，有则生成 `parent_prelude`，没有则跳过；再生成所有小节 active lesson cache。`B.2` 父章节 `hasPrelude:true`，已生成 `b_2/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`；`B.1/B.3/B.5/B.6/B.7` 父章节均判定 `hasPrelude:false`，不生成父章节前言；`B.4` 与 `B.8` 作为无小节章节生成 lesson。
- 本轮实际完成并验收 active cache：`B.1-1`、`B.1-2`、`B.2` parent prelude、`B.2-1`、`B.2-2`、`B.3-1`、`B.3-2`、`B.4`、`B.5-1` 到 `B.5-6`、`B.6-1`、`B.6-2`、`B.7-1` 到 `B.7-7`、`B.8`。全清单检查缺失数 `none`，文件均为 `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`（`B.2` 为 `new__parent_prelude__...`），mtime 均为 2026-05-08 凌晨本轮新生成。
- 并行经验：可以多进程/多 worker 跑不同 section，但本地 `ws-bridge`/模型链路在并发 4 时容易出现 5 分钟左右 `fetch failed`；其中部分请求虽然前端超时但后端已写好缓存（如 `B.5-2/B.5-4/B.5-5/B.5-6/B.7-3/B.7-7`），所以后续应以“返回结果 + 缓存文件存在/mtime/size”双重验收。稳妥并发建议为 2，最多谨慎到 3；同一个 section 仍然不能重复并行生成。

## 2026-05-08 takeover in Codex desktop
- Harrison 问是否能看到桌面 `tutor agent` 文件夹，并要求读取记忆后接管。当前 Codex 默认工作区 `/Users/chenghaoxiang/Documents/Codex/2026-05-08/tutor-agent` 是空目录，真正项目位于 `/Users/chenghaoxiang/Desktop/tutor agent`。已读取 `README.txt`、`CODEX_PROJECT_GUIDE.md`、`workspace_tutor_related_full/README_FULL_SCAN.txt`、`project-memory/MEMORY.md`、`project-memory/2026-04-21-memory-rule.md`、以及今日项目记忆 `project-memory/2026-05-08.md`。
- 当前接管状态：主项目是 `tutor-openclaw-ui/`，核心脚本包括 `app.js`、`style.css`、`ws-bridge.js`、`index.html` 和根目录 `tutor_craft.py`；教材/缓存资产在 `workspace_tutor_related_full/tutor-materials/`。后续默认用中文沟通，绝对不出现韩语；每次对话都要写入当日记忆；“重新生成”默认先定位实际命中缓存并删除旧缓存后重生；讲解要多举例，能配图/交互就优先配图/交互。
- 今日最新项目状态已接上：Feedback Board 已完成；父章节前言与整章生成定义已确立；Background 全部章节已重新生成并验收 active cache；新书 Textbook/lesson page map 已全量审计，`bad=0`；并行生成建议并发 2，最多谨慎到 3，同一个 section 不能重复并行删/生成，写/删前要确认目标缓存没有被其他窗口刚写好。

## 2026-05-08 deployment request
- Harrison 现在要把 Tutor Agent 项目部署到自己的服务器上，并特别强调一定不能泄露 API key。已先说明部署合理性与安全边界：API key 不进 Git、不进前端、不进日志、不在聊天中明文展示；优先通过服务器端 `.env` 或服务环境变量保存，部署代码时排除本地 `.env`、日志、运行期用户数据和临时缓存。等待 Harrison 明确确认服务器信息与执行方式后再实际部署。
- Harrison 提醒“检查我的记忆，里面写了部署方式”。已检索项目记忆与全局记忆中的部署相关记录：2026-04-21 结论是 Vercel 只能托静态前端，`ws-bridge.js` 后端依赖持久进程、spawn Python、本地文件系统和长请求，不能跑在 Vercel；可选“前端 Vercel + 后端 VPS”或“全部 VPS”。2026-04-27 实际走过 GitHub + Vercel + Render，出现过 API key 明文进 GitHub 导致 OpenRouter 泄露报警/强制作废、lesson-cache/generated 未上云导致缓存/图片失效、云端必须用环境变量 `OPENROUTER_API_KEY` 等问题。当前若部署到 Harrison 自己服务器，推荐全部 VPS：服务器跑 `node ws-bridge.js`，前面用 Nginx/HTTPS，API key 只放服务器 `.env` 或服务环境变量，禁止进 Git 和前端。
- Harrison 中断前一轮后明确：仍想走 2026-04-27 成功部署的流程，即 GitHub + Render 后端 + Vercel 前端，但要求不要暴露任何 API key。当前应按安全版流程执行：先做本地 secret scan，去除代码中的硬编码 key/fallback，确保 `.env` 不进 Git；Render 只通过 Environment Variables 配置 `OPENROUTER_API_KEY`/必要服务 key；Vercel 只托静态前端且不放模型 key；lesson-cache/generated/tutor-materials 作为资源上云时要完整，但不能混入 `.env`、日志或明文密钥。
- Harrison 下令“开始”。已执行安全版 GitHub/Vercel/Render 前置部署：桌面真源不是 Git 仓库，旧 `/Users/chenghaoxiang/.openclaw/workspace` 仓库有大量无关脏改，因此采用临时干净克隆 `/tmp/aquarius-deploy-clean` 同步桌面真源到 GitHub 仓库 `MrParamecium/Aquarius`。修复点：两份 `ws-bridge.js` 中 `SERPER_API_KEY` 硬编码 fallback 已改为只读环境变量；新增/整理 `.gitignore`、`.dockerignore`、`package.json`、`Dockerfile`、`vercel.json`、`.env.example`；确认 `.env`、日志、users、node_modules 不在最新提交树中；`node --check tutor-openclaw-ui/ws-bridge.js/app.js` 通过；secret scan 最新提交只剩 `OPENROUTER_API_KEY` 等环境变量名读取，没有明文 key。已提交并推送 `15001f4 Prepare safe cloud deployment` 到 GitHub main。GitHub raw 已确认新版本包含 `Feedback Board`、资源版本 `1778145900`，且 Serper fallback 明文 key 不再存在。
- 云端状态：Vercel `https://aquarius-seven.vercel.app/` 已自动更新到新前端（页面含 `Feedback Board`，引用 `style.css/app.js?v=1778145900`）。Render 旧后端 `https://aquarius-5ss0.onrender.com` 仍在运行且 `/health` 正常，但实际端出的 `/ws-bridge.js` 仍是旧版本，说明 Render 还未自动 redeploy 最新 commit 或服务未绑定当前 GitHub main；需要在 Render 面板手动 `Manual Deploy -> Deploy latest commit` 或重新绑定仓库，并在 Render Environment Variables 中配置新的 `OPENROUTER_API_KEY`、可选 `SERPER_API_KEY`、如仍需 Clerk 后端则配 `CLERK_SECRET_KEY`。注意：旧 Git 历史曾经包含明文 secret，最新提交已清理，但真正安全仍建议轮换旧 key；若要从 public history 中尽量移除，需要用户明确授权后再做 force push/历史重写。
- Harrison 发 Render Environment Variables 截图确认是否配置正确。判断：位置正确，`OPENROUTER_API_KEY` 应配置在这里；但截图中 value 看起来可能有换行或被分成两行，需确保 key 是一整行连续字符串、无空格无换行无引号。`SERPER_API_KEY` 仅影响 Web Search，可选；`CLERK_SECRET_KEY` 若后端不用 Clerk secret 可暂不填。提醒 Harrison 不要再公开截图 key，旧 key 仍建议轮换。
- Harrison 问现在填上后两个 key 是否可以。答复：可以，Render Environment Variables 是正确位置；`SERPER_API_KEY` 用于网页搜索增强，`CLERK_SECRET_KEY` 仅在后端需要 Clerk secret 时有用。提醒不要截图/公开 key，保存后用 `Save, rebuild, and deploy` 触发重新部署。
- Harrison 发 Clerk API keys 页面截图，问 `CLERK_SECRET_KEY` 应该用哪个。答复：Render 的 `CLERK_SECRET_KEY` 应填 Clerk 页面底部 **Secret keys** 区域的 `default` secret key；不要填 `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` / Publishable key，因为 publishable key 是前端公开 key，项目前端已使用它。提醒不要公开 secret 截图，复制 secret 后贴入 Render 的 `CLERK_SECRET_KEY`。
- Harrison 完成 Render deploy。已远程验收：Render `https://aquarius-5ss0.onrender.com/health` 返回 `status:"ok"`，并显示 `indexedPages: { old: 104, new: 149 }`，说明新书数据链也已上线；公开 `/ws-bridge.js` 中 `SERPER_API_KEY` fallback 已变为 `process.env.SERPER_API_KEY || ''`，不再含旧明文 key；Render 首页与 Vercel `https://aquarius-seven.vercel.app/` 均已端出新前端资源版本 `style.css/app.js?v=1778145900`，页面含 `Feedback Board`；`/api/feedback` 返回 `{"items":[]}`，接口正常。结论：GitHub + Render 后端 + Vercel 前端的安全版部署已成功跑通。
- 仍需长期安全动作：旧 GitHub 历史里曾暴露过旧 secret，虽然最新提交树已清理 `.env` / logs / users / node_modules，且新代码已不含明文 key，但建议 Harrison 尽快轮换旧的 OpenRouter / Serper / Clerk secret；若要进一步从 public repo 历史中降低暴露面，需要 Harrison 明确授权后再做 force push / 历史重写。
- Harrison 说明此前暴露的 key 已经停用。当前结论：最紧急的泄露风险已解除；现在线上运行的是不含明文 API key 的新代码树，Vercel/Render 均已更新。后续如无强安全合规要求，可先保持当前部署；若未来想继续降低 GitHub 历史暴露面，再考虑做仓库历史重写。
- Harrison 让继续做“前端 API base 可配置”，并要直接可进的网站。已在桌面真源与部署仓库同步实现：新增 `tutor-openclaw-ui/config.js`（当前 `apiBase` 指向 `https://aquarius-5ss0.onrender.com`），`app.js` 改为优先读取 `window.AQUARIUS_CONFIG.apiBase`，无配置时本地默认 `http://127.0.0.1:9000`、线上默认同域；`index.html` 在 `app.js` 前加载 `config.js`，资源版本 bump 到 `1778185000`。已提交并推送 `ff6a50b Make frontend API base configurable`。Vercel 线上已更新，首页脚本标签为 `config.js?v=1778185000` 与 `app.js?v=1778185000`，`https://aquarius-seven.vercel.app/config.js?v=1778185000` 可读到当前后端地址配置。
- Harrison 反馈两个新的线上交互问题：1) 点击 `Open Workspace` 会直接绕过登录进入 workspace，但他希望必须先登录；2) 进入网站时会先闪一下进入后的页面，再回到介绍页，不希望出现这种 flash。当前判断是同一类首屏状态机问题：`Open Workspace` 按钮点击路径缺少 auth gate，初始化阶段主 app shell 在 intro 判定前先被渲染。已先给出修复计划但按长期规则尚未执行：未登录时让 `Open Workspace` 直接打开登录流程；首屏改成“先判定 intro / auth，再显示 app”，避免 workspace 先闪现。

## 2026-05-08 登录门禁与首屏闪屏修复
- 用户反馈线上点击 Open Workspace 会绕过登录，并且进入网站时会先闪 workspace 再回介绍页。
- 已修复：introHeroExploreBtn 不再直接 showWelcome，而是走 handleEnter；未登录时 hideIntroLanding 后 showLoginView。
- 已修复：index.html body 加 app-booting；style.css 在 app-booting 下隐藏 .app 和 .login-view；app.js 在 startup routing 完成后 finishStartupBoot 移除。
- 同步修改真源、workspace_tutor_related_full 镜像与 /tmp/aquarius-deploy-clean 部署克隆。
- 本地静态服务 http://127.0.0.1:4178 验证：初始 intro 可见，app 不可见；点击 Open Workspace 后 login 可见，welcome 不可见。
- 已推 GitHub main：84f6c72 Gate workspace behind login；Vercel 已更新到 app.js/config.js v=1778188600。
- 线上静态文件验证：app.js 包含 finishStartupBoot、bootWantsLogin、heroExploreButton.onclick = handleEnter；style.css 包含 body.app-booting 隐藏规则。

- 修复 B.8 附录特殊结构：左侧目录保留单个 B.8 入口，lesson 直接返回 10 页公式附录（B.8-1 到 B.8-10），每页只列 LaTeX 公式和编号，不生成讲解、不追加 quiz。
- 修复 lecture 页内 overview chooser 卡片重渲染后失效问题：在 renderCurrentKnowledgePoint 后重新 bindOverviewSubsectionCards，解决 B.2 第一页点击子节卡片不能跳转。
- 修复侧栏子节切换时闪 4 步 AI 生成页：openLearnMode 改为 silent startLesson，silent 模式直接 hideSplash，普通模式 splash 延迟到 1800ms，避免本地切换/缓存切换时闪屏。

- 修复 OAuth 登录跳转中间态闪回 Settings/workspace：新增 authRedirectInProgress 与 body.auth-redirecting，Google/GitHub redirect 启动与 callback 完成前强制保持 login view；showAuthOverlay 不再打开 settings，而是回到 login。

- 修复 OAuth 登录完成后回到 landing 的落点问题：新增 aquarius-auth-return-intent（sessionStorage），开始 Google/GitHub 登录前记录 workspace 意图；启动阶段若存在该意图则不显示 intro landing；onUserSignedIn 后强制 hideIntroLanding 并 showWelcome，未完成 quiz 时在 workspace 上弹 quiz。

- 修复登录成功后自动弹出学习画像问卷：onUserSignedIn、guest mode、fallbackLocalUid 不再自动 showQuiz，统一直接 renderUserBadge + showWelcome 进入 workspace；学习画像后续只保留为主动入口。

- 修正登录后落点判定：onUserSignedIn 仅在 authReturnIntent=workspace 或 loginView 当时可见时才 hideIntroLanding + showWelcome；普通已登录回访网站不再绕过介绍页。
- 恢复问卷正确时机：刚登录/guest/fallback 进入 workspace 且 quiz 未完成时继续 showQuiz；不再在普通已登录回访首页时自动跳入 workspace。

- 重修认证状态机：启动页不再因 Clerk 已登录或残留 auth intent 自动进 workspace；intro 所有入口统一 setAuthReturnIntent + showLoginView；Clerk session 恢复只同步 currentUser/userMemory，不导航；只有用户从登录页/OAuth 回来或点击入口且已有 session 时才 showWelcome，首次用户继续弹 quiz。

- 修复登录页只闪一下直接进问卷：intro 入口不再 setAuthReturnIntent，也不再手动 onUserSignedIn；新增 allowAuthNavigation，只在真实登录动作（OAuth start / Clerk mount / OAuth callback）后允许 onUserSignedIn 导航到 workspace，Clerk session 恢复不会因 loginView 可见而直接跳转。

- 修复 OAuth 回调成功后无人继续导航的问题：handleAuthRedirectIfNeeded 现在设置 return intent；initClerk 在 handledRedirect 后重新 load Clerk，并在 clerkInstance.user 存在时主动 onUserSignedIn，否则停在 login。fallbackLocalUid 的问卷触发条件也改为 allowAuthNavigation/authRedirectInProgress，不再由 loginView 可见触发。

- 修复 Clerk OAuth 400：如果浏览器里已存在 Clerk user，则点击 Google/GitHub 或 Sign In 不再创建新的 sign_in，而是直接 enterWorkspaceWithExistingSession -> onUserSignedIn；OAuth 400 时不再自动打开嵌入式登录框，只提示刷新后重试。

- 安装 better-auth-best-practices skill 到 ~/.agents/skills/better-auth-best-practices。参考其显式会话原则修复登录绕过：已有 Clerk session 不再由 Google/GitHub/Sign In 按钮直接进入 workspace；登录页新增 Continue as <name> 显式继续按钮，只有用户点这个按钮才复用已有 session 进入 workspace。

## 2026-05-08 Codex desktop takeover follow-up
- Harrison 再次要求确认是否能看到桌面上的 `tutor agent` 文件夹，并读取记忆后接管。已确认真实项目目录为 `/Users/chenghaoxiang/Desktop/tutor agent`，而当前 Codex 线程默认 cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-08/tutor-agent-2` 只是独立空工作目录，不是主项目本体。
- 本次补读并确认的核心上下文包括：根目录 `README.txt`、`CODEX_PROJECT_GUIDE.md`、`package.json`，项目记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、`2026-05-08.md`，以及主工程入口 `tutor-openclaw-ui/index.html`、`app.js`、`style.css`。当前已按这些资料完成接管，后续默认以桌面这个主项目为准继续工作。

## 2026-05-08 login continue-session removal
- Harrison 发来线上 `aquarius-seven.vercel.app` 截图：Google OAuth 触发 Clerk `sign_ins` 400，且登录页出现 `Continue as 程浩翔` 模块；要求删除图二模块。已在桌面真源与 `workspace_tutor_related_full` 镜像删除 `clerkContinueSessionBtnLogin` 对应 UI/绑定/显示逻辑，保留 Google/GitHub/Email/Guest 主登录路径。
- 同时修复 OAuth 点击路径：`startOAuthRedirect` 在 Clerk 已有 `user` 时直接调用 `enterWorkspaceWithExistingSession()` 进入 workspace，不再继续创建新的 Clerk `sign_in`，减少已有 session 下的 400；残留 400 提示文案改为让用户刷新后用 `Sign In` 或 Guest，不再指向已删除的 Open Workspace/Continue 模块。`node --check app.js` 通过。
- 已重建 `/tmp/aquarius-deploy-clean` 部署克隆，同步 `index.html/app.js` 并推送 GitHub main：`09b10ac Remove continue-session login button`、`26fbcd6 Update Clerk session error copy`。Vercel 线上已从旧 `app.js?v=1778188600` 更新到 `app.js?v=1778193600`，首页不再包含 `clerkContinueSessionBtnLogin` 或 `Continue to Workspace`，只剩 `Continue as Guest`。

## 2026-05-08 OAuth landing bounce fix
- Harrison 继续反馈：Google OAuth 第一张图继续完成后，会直接跳回 landing（第二张图），但按产品预期应该进讲解页面。根因判断：OAuth 回调返回干净首页 URL 后，前端启动阶段先显示 landing；随后 Clerk 恢复 session 时因为缺少 `allowAuthNavigation/authRedirectInProgress` 被当作普通回访，只同步用户，不导航。另一个问题是画像问卷最后 `Start Learning` 保存后没有继续打开目标讲解页。
- 已修复：新增 `aquarius-auth-return-target`，landing 的 `Sign In/Open Workspace` 入口调用 `prepareLearnReturnTarget()`，默认指向当前书本第一个可学小节；`shouldShowIntroLanding()` 在存在 pending auth intent 时不显示 landing；Clerk listener 和初始 session 检查把 `hasPendingAuthReturnIntent()` 也视为允许导航；`onUserSignedIn()` 支持 `authReturnIntent === 'learn'`，画像已完成时直接 `continueToPendingLearnTarget()` 打开讲解页，未完成则先显示问卷；问卷最后保存画像后也会优先继续到 pending lesson target，否则才回 welcome。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像与 `/tmp/aquarius-deploy-clean` 部署克隆；`node --check app.js` 通过；`index.html` 资源版本 bump 到 `app.js?v=1778197200`；已推 GitHub main `88c6ced Preserve lesson destination after OAuth`。Vercel 线上已更新并确认 `https://aquarius-seven.vercel.app/` 加载 `app.js?v=1778197200`，新脚本包含 `AUTH_RETURN_TARGET_KEY`、`prepareLearnReturnTarget`、`continueToPendingLearnTarget` 与 `authReturnIntent === 'learn'`。

## 2026-05-09 learn Q&A panel resizer fix
- Harrison 反馈本地 `localhost:9000` 讲解页右侧提问框比例调整不了。定位后发现当前可见布局由 `.learn-body-inner` 的 CSS grid 强规则控制（例如 `grid-template-columns: minmax(0, 1fr) 10px minmax(320px, 34%) !important`），而旧 JS resizer 只修改 `learnExplainCol/learnChatCol` 的 flex/width，因此会被 grid 覆盖，看起来拖拽无效。
- 已修复：`app.js` 的 resizer 改为直接更新 `.learn-body-inner` 的 `grid-template-columns`，支持 pointer events、拖拽时禁选文本、限制左栏最小 420px/右栏最小 320px，并把右侧比例保存到 `localStorage` 的 `aquarius-learn-split`；窗口 resize 时会重新应用比例，移动端单列会清掉自定义 grid。`style.css` 增加 `touch-action:none`、`body.learn-resizing`、以及移动端 `data-custom-split` 兜底。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像和 `/tmp/aquarius-deploy-clean`；`node --check app.js` 通过；使用系统 Chrome + Playwright 实测本地拖拽：右栏宽度从约 441px 变到 320px，左栏从约 845px 变到 966px，并写入 `localStorage`。资源版本 bump 到 `style.css/app.js?v=1778235600`，已推 GitHub main `00c5921 Fix learn panel resizer`；线上 `aquarius-seven.vercel.app` 已确认加载新版本且 JS/CSS 包含 `aquarius-learn-split`、`applyLearnSplit`、`learn-resizing` 等修复逻辑。
