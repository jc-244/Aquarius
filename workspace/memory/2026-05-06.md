- 2026-05-06: Harrison 继续在桌面主项目 `/Users/chenghaoxiang/Desktop/tutor agent` 上迭代 tutor-openclaw-ui，重点围绕 lesson generation、cache 命中、Agent A / Agent B 双链路、Background 考试偏置注入逻辑、前端分页 bug 和全局 Cute Studygram shell 风格。当前真正运行中的主工程仍是 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/`，同步副本是 `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-openclaw-ui/`。任何前端/后端运行态修改必须同步两边，任何对话结束前都必须自动写入项目记忆。

- 2026-05-06: Harrison 要求以后若生成链失败，不准再用手写 cache / shortcut draft 冒充正常双 Agent 结果。当前硬规则已收敛为：external blueprint 全局禁用；Agent A 失败则立即停止并直接报错给用户；不允许 fallback blueprint / external blueprint / direct markdown last resort 偷偷继续；lesson 重生成默认包含 quiz_plan 同步重生成；不能再拿低质量手工 cache 冒充“已经生成成功”。当前这套 fail-stop / no-shortcut 规则主要写在 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/ws-bridge.js`，并已同步运行副本。

- 2026-05-06: 已重新解释整个项目的讲解生成主流程，当前准确心智模型是：前端 `app.js` 的 `openLearnMode(...)` -> 请求 `/api/section` -> 后端 `ws-bridge.js` 查 lesson cache -> cache miss 时走 `preGenerateSectionLesson(...)` / `generateSectionLesson(...)` -> Agent A 规划 blueprint -> Agent B 执行 blocks -> `blueprintToMarkdown(...)` 组装最终 lesson markdown -> 写入 `lesson-cache/<section>/new__aquarius_std_v1.aquarius_std_v1.en.md` -> 前端 `parseLessonKnowledgePoints(...)` 分页 -> `renderCurrentKnowledgePoint(...)` 渲染当前页。要掌控项目，最重要的 5 个文件是：`app.js`、`ws-bridge.js`、`agent-a-planner.md`、`agent-b-tutor.md`、当前 section 的命中 lesson cache 文件。

- 2026-05-06: 生成链当前正式模型配置已回到：Agent A = `gpt-5.5`，Agent B = `anthropic/claude-sonnet-4.6`。中间曾尝试把 Agent A 切为 `google/gemini-3.1-pro-preview`，也曾试验让 Agent B 暂时换成 `gpt-5.5` 排查，但用户最终判断规划质量仍以 GPT-5.5 更稳、讲解写作质量仍以 Sonnet 4.6 更好，因此目前固定为 A=GPT-5.5、B=Claude Sonnet 4.6。OpenRouter key 已在 `.env` 中更新，过去的 `403 model not available in your region` / `401 User not found` / 旧 key 干扰问题都作为历史坑点记录，后续如果再出现同类错误，优先检查 `.env`、进程是否重启、以及请求是否真的打到当前 9000 端口上的新进程。

- 2026-05-06: 已对 `ws-bridge.js` 做大量 lesson generation 规则硬化。当前真正影响讲解行为的规则包括：overview 页只能是极简 `Section Objective + Concepts In This Section`；concept 列表只能列概念名、不写解释句；每个定义型知识点必须紧跟最小例子；易混概念（如 diagonal / identity）必须拆开；一页只讲一个主要知识点；重生成 lesson 时 quiz_plan 也必须同步重生；Wikipedia / Wikimedia / textbook / LaTeX / interactive demo / gptimage2 采用分层选择；`gptimage2` 只能最后兜底且一图一概念，禁止多概念教学拼图。这些规则一部分在 prompt 文件，一部分在 `ws-bridge.js` 的 `LESSON_GENERATION_RULES_TEXT`、Agent A/B appendix 和视觉决策逻辑里。

- 2026-05-06: Harrison 提供了背景作业 PDF `/Users/chenghaoxiang/Desktop/HW1_Yanglet.pdf`，要求蒸馏出考试重点并在 Background 章节讲解生成时参考。已产出两份蒸馏文件：`/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/exam-priority/background_hw1_distilled.md` 与 `.json`。但后续实战发现最初的注入逻辑太粗：只要 section 属于 `B.*` 就强加作业偏置，导致像 `B.1-1 A Historical Note` 这种历史页也被往“复数形式转换 / 极坐标 / 欧拉公式 / 象限陷阱”的考试技能课方向带偏。根因不是前端，而是作业偏置逻辑粗暴地把 exam relevance 当成 section identity。为此已在 `ws-bridge.js` 里重构：新增 `classifyBackgroundSection(sectionId, sectionTitle)`，将 Background 小节细分为 `historical_note` / `conceptual_intro` / `computational_core` / `background_general`，并让 `loadExamPriorityGuidance(...)` 和 `buildExamPriorityPromptSection(...)` 根据类型控制偏置强度。当前规则是：`historical_note` 不再注入 HW1 偏置；`conceptual_intro` 只轻度参考；`computational_core` 才允许强考试导向偏置。还新增了 `buildSectionSpecificPromptSection(...)`，对 `B.1-1` 这类历史页明确禁止滑进 Cartesian / polar / Euler / quadrant trap / later operational tools。

- 2026-05-06: `B.1-1` 是本轮 debug 的核心案例。用户正确指出：`B.1-1` 应该是 `A Historical Note`，不该出现欧拉公式、极坐标、工程式 `j` 技能课内容。之前之所以出现偏航，是因为生成时 `section_title` / exam-priority 注入共同把它带成了“Complex Numbers and Their Algebra”。当前 active B.1-1 lesson cache 路径是 `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md`。最近一次修正后，内容方向已经回到历史主线：number system expansion、imaginary unit、Cardano/Bombelli、Gauss 等，不再是 Euler / polar 主课；但用户后续又指出风格仍不够纯粹，依然残留偏考试/讲义化倾向。当前若要继续打磨 `B.1-1`，应优先审查：`/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/debug/agentA-B.1-1-preview.md`、`agentA-B.1-1-raw.txt`、`agentB-B.1-1-input.json`、以及最终 active cache 文件，明确是 Agent A 规划、Agent B 执行，还是 markdown 组装 / 前端分页出了问题。

- 2026-05-06: `B.1-1` 还暴露了一个重要的前端分页 / 解析 bug。症状包括：overview 页 `Concepts In This Section` 被错误喂进长解释句；`Key Takeaways` 页只显示标题没有内容；某些情况下 summary 与 quiz / trailing paragraph 错位。经核对 active cache 可知，Key Takeaways 内容实际上已经在 markdown 里，但前端 `app.js` 的 `parseLessonKnowledgePoints(...)` / `buildLessonOverviewHtml(...)` / `flushIntroNodes(...)` 解析逻辑过于宽松，把过长 P 节点也当成 concepts 或没把 summary 后续节点稳定挂进 summaryPage。已在主运行文件 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/app.js` 做了前端级修复：overview 的 concept 收集更严格，只接受短、无句号感叹号的概念型文本；summary 页在 `pushCurrent()` 后会显式归并 `summaryPage.parts`，防止 Key Takeaways 只有标题。修改已同步到运行副本，并通过 `node --check`。如果后续再看到 `Key Takeaways` 只有标题、overview 吃掉解释句、页数异常等问题，优先查 `parseLessonKnowledgePoints(...)` 和 `buildLessonOverviewHtml(...)`，而不是先怪 Agent A/Agent B。

- 2026-05-06: B.2-1（Addition of Sinusoids）也经历了完整修复过程，积累了一些关键经验。起初 `B.2-1` 的分页结构失真，甚至出现 `## 1` 重复、`\[4pt]` 等 LaTeX spacing 残留、以及只有标题没有内容的页。已确认部分问题来自 Agent A / Agent B 产物格式，部分问题来自 `ws-bridge.js` 组装与前端分页。修复项包括：为 `phasor` interactive demo 在 `app.js` 中补上真正的 hydrate/render 分支和样式；处理多行 `$$ ... \begin{bmatrix} ... \end{bmatrix}$$` 的公式解析；把 `\[4pt]` 这类 LaTeX 行距命令在前端预处理里清洗掉；提升 lesson 主标题到可分页的 `##` block。当前 `B.2-1` 的 active cache 是可用的多页版，并且已能展示 interactive demo，但如果未来再出现“某页只有标题空白”，一个高概率原因是前端只识别了 heading，但没有成功挂载该页对应的 interactive demo。本项目的经验是：页面空白不代表 markdown 没写，多半还要查前端 hydrate 路径。

- 2026-05-06: `B.6-1` 和 `B.6-2` 的 lesson 生成也暴露了多轮经验，尤其是：不要把手写 cache 冒充正常双 Agent 重生成结果；定义型矩阵课程不应默认被 gptimage2 总览图主导，而应逐定义先用 LaTeX / math_block 明确规则，再补 supporting visual；Wikipedia/Wikimedia 必须优先于 gptimage2；视觉选择必须逐知识点决定，而不是整章只用一种形式。当前 `B.6-1` 的 active cache 仍是 `/Users/chenghaoxiang/Desktop/tutor agent/workspace_tutor_related_full/tutor-materials/lesson-cache/b_6-1/new__aquarius_std_v1.aquarius_std_v1.en.md`，结构已经改成 formula-first definitions + supporting visuals 的方式。`B.6-2` 则经历了从只有两页、external blueprint 污染、到强制走 Agent A -> Agent B 真链的修复过程。整体记住：B.6 系列是“定义型、规则型、考试陷阱型”课程，不要让视觉策略压过定义本体。

- 2026-05-06: 用户要求使用自己的 OpenRouter key 调 `google/gemini-3.1-pro-preview` 来给前端 Cute Studygram / Flashcard shell 做审查，并根据建议全局改造 UI 外壳。已用 `.env` 中 key 通过 OpenRouter 调用 Gemini 成功，反馈记录在 `/Users/chenghaoxiang/Documents/Codex/2026-05-04/tutor-agent/gemini-ui-shell-review.md`。据此，已对 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/style.css` 与 `index.html` 进行全局风格改造：使用 dotted notebook paper 背景、Nunito/Quicksand 字体、flashcard 式 `.main` 容器、neo-brutalism 硬阴影按钮和输入框、侧栏和 modal 统一厚边框风格。修改已同步到 workspace 副本，并更新前端版本号。后续如果用户再提“整个 app shell 要更 cute / studygram / flashcard”，优先看 `style.css` 末尾的 cute shell override，而不是重新堆新的风格补丁。

- 2026-05-06: 运行时与调试文件的重要位置：1) 运行日志：`/private/tmp/openclaw-tutor-ui.out.log` 与 `/private/tmp/openclaw-tutor-ui.err.log`；2) lesson 生成调试文件：`/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/debug/agentA-<section>-raw.txt`、`agentA-<section>-preview.md`、`agentB-<section>-input.json`；3) 运行进程多次出现 `EADDRINUSE :::9000`，说明曾存在多个旧进程抢端口，后续排障时如果发现“明明改了代码但页面像旧版”，一定要先确认当前 9000 上实际跑的是哪个进程、日志里有没有 cache hit 直接跳过 pipeline、以及 `index.html` / `app.js` 版本号有没有刷新到新值。

- 2026-05-06: 用户多次强调自己是 pure vibecoding，对项目失控感很强，因此后续所有协助都应围绕“建立可控心智模型”展开，而不只是帮他 patch bug。需要始终记住并反复复述的项目结构是：前端 shell / lesson renderer (`app.js` + `index.html` + `style.css`)；后端 lesson generator / cache orchestrator (`ws-bridge.js`)；Agent A prompt / Agent B prompt；OCR / section-page-map；exam-priority distilled docs；lesson-cache active markdown。任何问题都要帮用户明确归因到其中一层，而不是模糊地说“模型写坏了”或“前端坏了”。

- 2026-05-06: 新对话接管后，先按用户要求重新读取并核对了当天 project memory、`MEMORY.md`、运行态 `ws-bridge.js`、`app.js`、以及 `agent-a-planner.md` / `agent-b-tutor.md`。当前确认：1) live 生成链是前端 `/api/section` -> `preGenerateSectionLesson` / `generateSectionLesson` -> Agent A (`gpt-5.5`) 规划 -> Agent B (`anthropic/claude-sonnet-4.6`) 执行 -> `blueprintToMarkdown` -> lesson cache -> 前端 `parseLessonKnowledgePoints` / `renderCurrentKnowledgePoint` 分页渲染；2) external blueprint 在 `loadExternalBlueprint()` 中全局禁用；3) Agent A 失败时 `generateSectionLesson()` 直接抛错停止，不再继续 fallback；4) B.1-1 过去被带偏的根因确实是旧版对全部 `B.*` 统一注入 `background_hw1_distilled` 考试偏置，把 historical note 错当成 computational core，导致规划滑向 Euler / polar / algebra drill；5) 当前 `classifyBackgroundSection()` + `buildSectionSpecificPromptSection()` 已把 `B.1-1` 特判成 `historical_note`，明确禁止滑进 Cartesian / polar / Euler / quadrant trap；6) 前端分页 bug 的关键位置仍是 `app.js` 里的 `buildLessonOverviewHtml()` 与 `parseLessonKnowledgePoints()`：前者过去会把较宽泛的段落误收进 overview concept list，后者过去会让 `summaryPage.parts` 收集不稳，表现为 `Key Takeaways` 只剩标题或 overview 吞解释句。本轮先完成理解确认，未改代码。 

- 2026-05-06: 用户追问“我怎么给你装载 skills”。已解释当前机制：这个桌面环境里的 skill 是否可用，取决于会话里系统已经暴露的 skills 列表；用户若想让我使用某个已存在 skill，只需要在需求里点名 skill 名称，或直接提出明显匹配该 skill 的任务；若想新增 skill，则需要走现有 `skill-installer` / `skill-creator` 能力安装或创建。说明了“我不能凭空加载一个当前会话未暴露的 skill，但可以安装新 skill、下轮继续用，或在当前会话用可见 skills 工作”。未改业务代码。 

- 2026-05-06: 用户要求列出本次会话当前可用的所有 skills。已准备按当前系统暴露的 skills 列表，分名称与用途给出完整清单，帮助用户理解“哪些能力可以直接点名调用”。未改业务代码。 

- 2026-05-06: 用户进一步询问几条 `npx skills add ... --skill ...` 命令对应的 skills 是否已经安装。已检查本机 `~/.agents/skills` 与 `~/.codex/skills`：确认 `find-skills` 与 `frontend-design` 已存在并有 `SKILL.md`；其中 `find-skills` 内容明确引用 `npx skills` 生态，`frontend-design` 也与当前会话暴露的 skill 一致。未发现 `vercel-react-best-practices` 与 `web-design-guidelines` 的安装目录或 skill 文件，因此当前判断是这两个还没装，至少本机 skill 目录里没有可用实例。未改业务代码。 

- 2026-05-06: 用户要求把缺失 skills 补装上。已执行 `npx skills add https://github.com/vercel-labs/agent-skills --skill web-design-guidelines -g -y` 与 `npx skills add https://github.com/vercel-labs/agent-skills --skill vercel-react-best-practices -g -y`。其中首次并行执行 `vercel-react-best-practices` 时遇到 `npm ERR! ENOTEMPTY`，属于 `npx` 临时目录重命名冲突，不是 skill 本体问题；随后顺序重跑成功。当前本机已确认存在 `~/.agents/skills/web-design-guidelines` 与 `~/.agents/skills/vercel-react-best-practices`。需要提醒用户：安装已完成，但当前会话的系统暴露 skill 列表未必立刻刷新，通常需要重启 Codex 或开启新对话，新的 skills 才会出现在“本轮可用 skills”列表中。未改业务代码。 

- 2026-05-06: 用户进一步询问“做前端时会不会自动调用这些 skills、什么情况下自动调用、还是必须用户主动指定”。已说明当前规则：如果任务明显匹配且该 skill 已在本轮会话暴露列表中，我会自动使用；若用户明确点名某个 skill，则优先按点名执行；若 skill 虽然已安装到本机但还没进入当前会话可见列表，我不能假装已加载，只能建议用户重启 Codex / 新开对话后再用。对 tutor agent 前端场景，应优先自动触发 `frontend-design`；涉及 React/Next 最佳实践时，如果未来该 skill 在新会话中可见，也可根据任务匹配或用户点名使用 `vercel-react-best-practices`；更偏通用网页视觉规范时可用 `web-design-guidelines`。未改业务代码。 

- 2026-05-06: 用户新增协作偏好：以后只要我准备实际调用某个 skill，必须先在对话里明确告诉用户“这一步会用哪个 skill，以及为什么”。后续前端、文档、PPT、浏览器测试等场景都应先显式报 skill，再执行。未改业务代码。 

- 2026-05-06: 用户要求重新生成 `B.1-1` 讲解，并强调必须走真实双 Agent 链、不能手写替代。执行过程确认了几件关键事实：1) 先删除了 active cache `lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md`；2) 初次尝试直接跑 `node ws-bridge.js --pregen-section ...` 时撞到 `EADDRINUSE :9000`，不能算成功；3) 改为调用 live `POST /api/pregen/section` 后，发现 9000 上运行的旧进程虽然路径是主项目 `tutor-openclaw-ui/ws-bridge.js`，但运行态仍在用旧 Agent A 配置（日志显示 `google/gemini-3.1-pro-preview`），所以先停掉旧进程并重启了当前磁盘上的最新 `ws-bridge.js`；4) 在最新进程上，第一次 `gpt-5.5` 规划仍然把 `B.1-1` 带偏到 Cartesian / polar / Euler / quadrant trap，随后确认不是 section-page-map 错，也不是 OCR 页错，因为新书 `B.1-1` 只映射 `page-001`，OCR 内容本身确实是历史 note；5) 因此只修改了生成约束层 `ws-bridge.js`：对 `historical_note` 增加更硬的禁止项，并新增 `focusOcrTextForSection()` 把 historical note 的 OCR 裁到小节标题之后，避免页首 `B.1 Complex Numbers` 总引言污染 Agent A；6) 重启后再次强制重生成，新的 `agentA-B.1-1-preview.md` 已明显修正为真正的历史主线：number-system expansion、Cardano、Bombelli、Gauss、complex numbers as intermediate tool，不再出现 rectangular/polar/Euler/quadrant-trap 规划；7) Agent B 也已成功返回 JSON，但在最终 `blueprintToMarkdown` / 图片后处理 / cache 落盘阶段仍未看到新的 active cache 文件写回，当前真实状态是“规划方向已修正，执行到后处理阶段，但 active cache 尚未成功落盘”；8) 观察到 `image-skill-last-error.log` 有 `gpt-5.4-image-2 returned no image URLs` 记录，这可能是后处理拖慢或阻塞点之一，但按代码设计这类图失败应只降级为无图继续，不应假装整课已成功。 

- 2026-05-06: 用户补充说明 OCR 页面有时会混入上一章节或下一章节内容，并询问应如何处理。已明确建议把问题拆成三层：1) page-map 层仍允许一页属于某 section，但生成前要做 subsection-aware OCR 裁剪；2) 对页内文本按“当前 subsection 标题开始、下一 subsection 标题前结束”截断，默认只把中间正文喂给 Agent；3) 对于页首父章节导语和页尾下一小节开头，仅作为弱上下文或直接丢弃，不能与当前 section body 同权输入。后续应优先在 `ws-bridge.js` 的 OCR 预处理层解决，而不是依赖 Agent A/B 自己忽略污染文本。未改业务代码。 

- 2026-05-06: 用户要求正式实现通用 OCR 页内裁剪。已在主工程 `tutor-openclaw-ui/ws-bridge.js` 中把 OCR 预处理从单一 `historical_note` 特判升级为通用 subsection-aware 裁剪机制：新增 `buildSectionCodeRegex()`、`buildHeadingTitleTokenRegex()`、`extractHeadingCodeFromLine()`、`looksLikeSectionHeadingLine()`、`cropOcrTextToSectionBoundaries()`，并让 `focusOcrTextForSection()` 先统一执行“从当前小节标题开始、到下一小节标题前结束”的页内截断，再在 historical note 场景额外保留原先更强的定向裁剪。这样即使某页 OCR 混入上一节尾巴或下一节开头，也会优先只把当前小节正文喂给 Agent。已将同一份 `ws-bridge.js` 同步复制到副本 `workspace_tutor_related_full/tutor-openclaw-ui/ws-bridge.js`，两边均通过 `node --check`，并重启了 9000 上的 live 服务以启用新逻辑。未改前端。 

- 2026-05-06: 在启用通用 OCR 页内裁剪并重启 live 服务后，继续收尾 `B.1-1`。最终确认：上一轮 `/api/pregen/section` 调用实际已经完成并返回 `ok: true`，新的 active lesson cache `lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md` 已在 22:42 落盘。返回 lesson 内容明显保持历史主线：number-system expansion、negative/imaginary triggers、Cardano/Bombelli/Gauss、complex numbers as shortcut，不再出现此前的 rectangular/polar/Euler/quadrant-trap 主链偏航。说明本轮通用 OCR 裁剪 + historical note 约束强化已经对 `B.1-1` 生效。 

- 2026-05-06: 用户查看新的 `B.1-1` live 页面后反馈：整体质量明显提升，但连续大段正文堆在一起会造成视觉疲劳。已分析这不是单纯字体问题，而是“生成页内信息密度”和“知识动作混装”问题：同一页里公式解释、历史叙述、notation 说明、common mistake 容易堆成一整大段。已建议后续优化重点应放在生成侧而不是只靠 CSS：缩短段落、把长解释拆成 micro-block（1 句主结论 + 2–4 个 bullets）、强制一页只做一个认知动作、把 formula 后解释改成短卡片/短 bullets、把标题与正文主线更严格对齐（例如标题写 Gauss 的页不该先花半页解释 `j` 和 `sqrt{-4}`）。如用户确认，可下一步把这些规则写入 Agent A/B prompt 与 markdown 组装逻辑。 

- 2026-05-06: 用户继续追问：为什么 `B.1-1` 里路线类比图用了 gptimage2 生成图，而不是直接用 textbook 原图。已根据 live 结果和日志解释清楚：当前不是“完全没用 textbook”，而是同时出现了两种视觉——一张 generated image 加一张 injected textbook figure。之所以生成图被选为主锚点，原因有三层：1) Agent A 当前把这张路线图判断为“特定教学类比图”，并在 `visual_plan.rationale` 中明确认为 textbook route figure 带有页面杂质、版面噪音、不是最干净的学生主视觉，所以把 `generate_image` 当 primary anchor；2) 后端在 Agent A 完成后又通过 canonical `book_image` 注入逻辑，把教材图作为补充锚点塞回 lesson，所以最终 lesson 实际上两张都在；3) 当前策略还没有把“historical note 中若 textbook figure 已足够表达核心类比，则禁止再生一张风格复写图”写成硬规则，因此模型仍会觉得“clean remake”更适合当主视觉。后续如果用户确认，应把这类规则收紧为：对历史页/类比页，教材原图若已清楚承载单一知识点，则默认 textbook-only 或 textbook-first，不允许再额外生成同构替代图。 

- 2026-05-06: 用户进一步把视觉策略升级为更硬规则：**不是 textbook-first，而是“只要 textbook / Wikipedia / Wikimedia / LaTeX / interactive demo 任一可用，就绝对禁止 gptimage2 生成图”。** 已把这条从偏好升级为 live 硬规则：1) 在 `LESSON_GENERATION_RULES_TEXT`、`VISUAL_SELECTION_DECISION_LADDER`、`AGENT_A_RULES_APPENDIX`、`AGENT_B_RULES_APPENDIX` 中明确写入“有任一非生成视觉路线可用时，generate_image forbidden”；2) 在 `ws-bridge.js` 中新增 `normalizeVisualPurposeKey()`、`getVisualPurposeKey()`、`isNonGeneratedVisualBlock()`、`pruneForbiddenGenerateImageBlocks()`，作为后端代码层兜底：即使 Agent A 仍规划了 `generate_image`，只要同 lesson 已存在 `book_image` / `web_search_image` / `interactive_demo` / `math_block` 等非生成路线，就会直接剪掉对应 `generate_image` block，并调整 `visual_plan.primary_anchor`；3) 已同步到副本 `workspace_tutor_related_full/tutor-openclaw-ui/ws-bridge.js`，两边通过 `node --check`，并重启了 9000 live 服务启用新规则。 

- 2026-05-06: 用户随后澄清了真实意图：**不是彻底把 gptimage2 压到所有非生成路线之后，而是“只要已经有现成图片（textbook / Wikipedia / Wikimedia 等 image asset）可用，就不要再生 gptimage2 图片”；如果没有现成图片，只有 LaTeX / interactive demo 这类非图片路线时，gptimage2 仍可作为图片兜底。** 这意味着我上一轮把规则收得过头了，后续需要把 live 规则修正为“image-asset-first anti-duplicate rule”，而不是“all non-generated routes forbid generate_image”。 

- 2026-05-06: 已按用户澄清后的真实意图修正规则实现：在 `ws-bridge.js` 中把“禁用 generate_image 的条件”从“任意非生成视觉路线存在”改回“仅当 ready-made image asset 存在时”，即只认 `book_image` 与 `web_search_image`。具体包括：1) `isNonGeneratedVisualBlock()` 改为 `isReadyMadeImageVisualBlock()`，仅识别 `book_image` / `web_search_image`；2) `pruneForbiddenGenerateImageBlocks()` 只在同 lesson 已有 ready-made image asset 时才剪掉 `generate_image`；3) prompt 文案同步修正，不再把 `math_block` / `interactive_demo` 自动视为禁用 gptimage2 的条件；4) 已同步副本并通过 `node --check`，重启了 9000 live 服务。当前准确策略是：**有现成图片就不用 gptimage2；没有现成图片时，LaTeX / interactive demo 并不自动禁止 gptimage2 作为图片兜底。** 

- 2026-05-06: 按修正后的 visual policy 重新强制生成 `B.1-1` 并完成验证。结果：1) Agent A preview 不再规划 route analogy 的 `generate_image` remake，而是改为 `web_search_image`（用于 number-system hierarchy）+ injected `book_image`（用于 textbook 的 country-route analogy）；2) live `/api/pregen/section` 最终成功返回 `ok: true`，新的 active cache `lesson-cache/b_1-1/new__aquarius_std_v1.aquarius_std_v1.en.md` 已在 23:37 落盘；3) 最终 lesson 文件中未出现任何 `/generated/gptimage2-...` 路径，说明“已有现成图片时不再生 gptimage2”规则已生效；4) 当前 `B.1-1` 的视觉策略变成：wiki 图承担 number-system hierarchy 主锚点，textbook 图承担 complex-number detour / country-route analogy，不再额外生成同构图片。 

- 2026-05-06: 用户在 live 页面反馈登录界面出现严重叠层 bug：登录卡片显示时，主应用 `.app` 外壳、左侧 sidebar 和 dotted notebook 背景仍然透出，像是把登录页半透明盖在主界面上。已定位为两层原因叠加：1) `app.js` 的 `showLoginView()` 只隐藏 `welcomeScreen/answerScreen/learnView/settingsView/libraryView`，没有真正隐藏最外层 `.app` 容器，因此主 shell 仍在 DOM 中可见；2) `style.css` 末尾的 cute shell 全局 override 误把 `.login-view` 也包含进 `background: transparent !important` 规则，直接覆写了登录页原本的深色全屏背景。修复方式：在主工程 `tutor-openclaw-ui/app.js` 中新增 `const appShell = document.querySelector('.app')`，并在 `showLoginView()` 里对 `.app` 添加 `hidden`，在 `showWelcome()/showAnswer()/showLearnView()/showSettingsView()/showLibraryView()` 中恢复移除 `hidden`；同时在 `tutor-openclaw-ui/style.css` 的全局透明化规则中移除 `.login-view`，保留其独立全屏深色背景。修改已同步到副本 `workspace_tutor_related_full/tutor-openclaw-ui/app.js` 与 `style.css`，并通过 `node --check`。后续如果再看到“登录页像磨砂玻璃一样叠在 workspace shell 上”，优先检查这两处，而不是怀疑 Clerk 或单纯 CSS 卡片样式。 
