
## 2026-05-10 Homepage proof section redesign
- User disliked previous proof/verification section and asked for Gemini 3.1 Pro Preview plus continuity with intro page style. Used OpenRouter model `google/gemini-3.1-pro-preview` for critique (`/tmp/gemini-31-ui-review.md`), then reworked `index.html` proof section from dashboard-like cards to scrapbook/study-note style: looseleaf guess vs Aquarius textbook page pull, paper grid texture, washi tape, red-pen note, handwritten margin note, source-locked stamp, page tabs, and yellow note response. Removed status chips, checklist rows, progress meter, and the dark response block. Synced to workspace/deploy; HTML structure still balanced.

## 2026-05-10 Lesson cache visibility fix
- User reported newly generated Chapter 1 lessons still showing “This section has not been prepared yet” in UI (screenshot on `1.1-1 Signal Energy`). Backend API verified `1.1-1`, `1.1-2`, `1.12` all hit cache correctly; parent `1.1 Size of a Signal` is intentionally cache-miss because only leaf lesson caches exist. Added front-end request sequence guard in `app.js` (`learnRequestSeq`) so stale parent/previous section responses cannot overwrite the current subsection lesson. Bumped `index.html` app.js version to `1778370900` to force browser cache refresh. Synced app.js/index.html to workspace/deploy.

## 2026-05-10 Chapter 1 parent lesson fallback
- Added `findSyllabusSubsections()` in `app.js`. If a parent syllabus node returns a lesson `cacheMiss` but has generated child lessons, the UI automatically opens the first child lesson (e.g. `1.1 Size of a Signal` -> `1.1-1 Signal Energy`) instead of leaving the user on “This section has not been prepared yet.” Bumped app script version to `1778379000` and synced `app.js`/`index.html` to workspace and deploy mirrors.

## 2026-05-10 Homepage pipeline/footer Gemini redesign
- User asked to also redesign the multi-agent pipeline and footer pages using Gemini. Called OpenRouter `google/gemini-3.1-pro-preview`; critique saved at `/tmp/gemini-31-pipeline-footer-review.md`. Applied direction in `index.html`: removed dark navy slab and big X from pipeline; replaced with paper-grid study desk timeline (`multi-agent desk`) showing textbook read -> lesson card -> test/check artifacts connected by hand-drawn arrows. Reworked footer from empty white CTA into taped final-notebook page (`last page before the exam`) with exam checklist and A+ grade card. Browser screenshots verified both sections; synced `index.html` to workspace and deploy mirrors.

## 2026-05-10 Codex desktop takeover - tutor-agent thread
- Harrison asked whether I can see the desktop `tutor agent` folder and told me to read memory and take over. Current Codex cwd is `/Users/chenghaoxiang/Documents/Codex/2026-05-10/tutor-agent`, but the real project is `/Users/chenghaoxiang/Desktop/tutor agent`.
- Read takeover context: `CODEX_PROJECT_GUIDE.md`, `README.txt`, `package.json`, project core memory `workspace_tutor_related_full/project-memory/MEMORY.md`, latest project memories `2026-05-09.md` and `2026-05-10.md`, and confirmed global memory folder exists at `/Users/chenghaoxiang/Desktop/Memory/memory`.
- Current takeover summary: main source is `tutor-openclaw-ui/` with `index.html`, `app.js`, `style.css`, `ws-bridge.js`; textbook/OCR/cache assets are in `tutor-materials/` and `workspace_tutor_related_full/tutor-materials/`; deploy mirror is `/tmp/aquarius-deploy-clean`.
- Verification: `npm run check` passes in `/Users/chenghaoxiang/Desktop/tutor agent`. The deploy mirror is behind `origin/main` by 3 commits and has many uncommitted local changes/new OCR assets from recent work, so do not reset or clean it casually.

## 2026-05-10 Learn Q&A edge tabs and layout refresh
- Harrison pointed out that in focused Q&A mode the left `Show` tab and right `Hide` tab were not horizontally aligned, the arrows were reversed, and the icons/layout looked ugly. Root cause: `#learnFocusBtn` lived inside `.learn-chat-topbar`/`.learn-chat-col`, while restore tabs lived directly under `learn-body`; later CSS overrides gave them similar `top` values but different coordinate systems and older arrow semantics.
- Called OpenRouter `google/gemini-3.1-pro-preview` for critique. The response was truncated twice, but it confirmed the main implementation direction: anchor both tabs to one shared parent/top strategy and normalize the edge-tab component.
- Updated `tutor-openclaw-ui/index.html`: swapped the four lecture/Q&A show/hide icons so `Hide` arrows point toward the edge being collapsed and `Show` arrows point back into the content. Updated `style.css` with a final `Q&A layout v2` block: shared `--learn-edge-tab-top`, unified pill styling for `learnFocusBtn` and both restore buttons, cleaner circular icon capsules, and calmer focused-Q&A empty state/composer layout.
- Synced `index.html`/`style.css` to `workspace_tutor_related_full/tutor-openclaw-ui` and `/tmp/aquarius-deploy-clean`; bumped assets to `1778385200`. Verification: `npm run check` passed, `git diff --check` passed for main/deploy touched files, and browser verification on `localhost:9000` B.1 focused Q&A mode showed left `Show` and right `Hide` aligned on the same horizontal line.

## 2026-05-10 Ask-this-page speech badge and framed composer
- Harrison asked to add a chat-bubble style icon above `Ask this page` and make the Q&A input area resemble the supplied reference: large attachment button, focused blue input, blue send button, `Balanced` and `Web` controls below, while keeping all controls inside one large outer frame.
- Implemented as CSS-only refinement in `tutor-openclaw-ui/style.css` with `Q&A layout v3`: restores `.learn-chat-empty-bubble` only in focused Q&A mode as a thick outlined speech badge; styles the composer as a single dark-outlined large frame; uses a two-row grid with attachment/input/send on row one and mode/web on row two; then adjusted to a four-column grid so `Balanced` and `Web` stay fully inside the frame.
- Synced `style.css` and bumped `index.html` assets to `1778386600` across desktop source, `workspace_tutor_related_full`, and `/tmp/aquarius-deploy-clean`. `npm run check` and `git diff --check` passed. Browser screenshot before final four-column tweak confirmed the speech badge and large framed composer direction; final CSS is present in all three copies.

## 2026-05-10 Edge tab direction and journal styling
- Harrison corrected that the left lecture `Hide`/`Show` arrows were reversed and then clarified that the desired style is the hand-journal/sticker version, not a minimal drawer handle.
- Called OpenRouter `google/gemini-3.1-pro-preview` for edge-tab critique; output truncated, but the usable direction was that the previous pill felt like a floating widget. Initially tried a slim drawer-handle treatment, then reverted per Harrison's preference to a polished journal-paper tab.
- Final implementation: `index.html` now uses explicit direction SVGs: left lecture `Hide` arrow points left, left lecture `Show` arrow points right; right Q&A `Hide` points right and right Q&A `Show` points left. `style.css` final edge-tab block is now `Edge tabs final: study-journal paper tabs`: cream paper background, dashed stitch inset, small blue tape strip, round blue icon badge, slight left/right rotation. Removed/overrode the slim drawer treatment by replacing the tail block so the journal style is the final effective CSS.
- Synced desktop source, workspace mirror, and `/tmp/aquarius-deploy-clean`; bumped assets to `1778389600`. `npm run check` passed. Verified all three CSS files have the final journal block after any previous drawer text and no remaining `slim drawer` marker.

## 2026-05-10 Q&A surface 90 percent journal layout
- Harrison pointed out the focused `Ask this page` surface had too much top/bottom empty space and should occupy about 90% of the page, and asked that the narrow right-side Q&A page use the same hand-journal visual language.
- Added final `Q&A surface final` CSS override in `tutor-openclaw-ui/style.css`: focused Q&A empty state and composer are `min(90%, 1360px)`, with less bottom/top waste and a larger journal-card surface; the regular side panel now shares the same paper-grid empty card, taped speech badge, thick framed composer, large attachment/input/send controls, and second-row `Balanced`/`Web` controls.
- Synced `style.css`/`index.html` to `workspace_tutor_related_full/tutor-openclaw-ui` and `/tmp/aquarius-deploy-clean`; bumped assets to `1778390500`. `npm run check` passed and all three copies contain the final CSS block/version.

## 2026-05-10 Q&A proportion and smooth drawer pass
- Harrison clarified the Q&A page elements themselves were too large and should be scaled down proportionally, requested smooth Hide/Show collapse/expand, removal of the blue tape sticker at the upper-left of the Q&A card, and placeholder text changed to `start a question`.
- Updated `index.html` placeholders for the main and popover Q&A inputs to `start a question`; bumped CSS/JS assets to `1778392600`.
- Updated `app.js` so lecture/Q&A panels are no longer immediately `display:none` on Hide/Show, allowing CSS width/flex/opacity transitions to animate the drawer behavior.
- Added final CSS polish block: smaller empty-state bubble/title/prompts/composer controls, removed Q&A card tape pseudo-element, default input border/shadow is black, focus state turns blue with a small lift/blue shadow microinteraction, and drawer columns animate via flex-basis/width/max-width/opacity/transform.
- Synced source, workspace mirror, and `/tmp/aquarius-deploy-clean`; `npm run check` passed.

## 2026-05-10 Q&A composer compact follow-up
- Harrison said the full-screen Q&A composer was still too large, the whole composer frame should also change from black to blue on click/focus, and the narrow side-panel placeholder `start a question` was wrapping onto two lines.
- Added final `Q&A composer compact pass` CSS: full-screen composer is narrower/shorter (`min(74%, 1060px)`), empty-card icon/title/copy/prompts are smaller, composer `:focus-within` turns the outer frame blue with blue shadow, and narrow panel controls/gaps are compressed with a minimum input column width so the placeholder fits.
- Added `wrap="off"` to main and popover Q&A textareas to keep `start a question` on one line in tight side panels. Bumped assets to `1778393900`, synced source/workspace/deploy, and `npm run check` passed.

## 2026-05-10 Left Hide/Show arrow correction
- Harrison reported the left-side lecture `Hide`/`Show` arrows were still reversed. Updated only the left lecture controls in `index.html`: `Hide` now uses the right-arrow-with-left-bar icon, and `Show` now uses the left-arrow-with-right-bar icon. Right-side Q&A arrows were left unchanged.
- Bumped assets to `1778394300`, synced `index.html` to workspace and `/tmp/aquarius-deploy-clean`, and `npm run check` passed.

## 2026-05-10 Q&A blank background and smaller heading
- Harrison said the focused Q&A page text was still too large and the blue grid background should be blank/white instead. Added final `Q&A blank-card typography pass` CSS: focused Q&A column/scroll/card backgrounds are pure white with `background-image: none`, title/copy/prompts/bubble are smaller, and the card is slightly smaller.
- Bumped assets to `1778395000`, synced `index.html`/`style.css` to workspace and `/tmp/aquarius-deploy-clean`, and `npm run check` passed.

## 2026-05-10 Replace Hide/Show with progressive panel focus
- Harrison asked to remove the Hide/Show logic and instead keep the original left/right buttons in place as progressive expand controls: clicking a side gives that side more room; clicking again makes it full screen and pushes the other side away; clicking again resets. Right side should work the same.
- Implemented new `learnPanelFocus` state machine in `app.js` with states `normal`, `lecture-wide`, `lecture-full`, `qa-wide`, `qa-full`. Left button cycles `normal -> lecture-wide -> lecture-full -> normal`; right button cycles `normal -> qa-wide -> qa-full -> normal`. Old restore buttons are hidden and no longer part of the user flow.
- Renamed button labels to `More Lecture`, `Full Lecture`, `Reset`, `More Q&A`, and `Full Q&A` depending on state. Added CSS `Progressive panel focus` block to animate 55/45 normal, 70/30 wide, and 100/0 full layouts with smooth flex/width/opacity transitions. Guarded manual split resizing so it does not override progressive focus states.
- Bumped assets to `1778397600`, synced source/workspace/deploy, and `npm run check` passed.

## 2026-05-10 Composer containment and input-only focus
- Harrison reported the narrow Q&A composer send button protruded outside the frame, the composer was not filling the right panel cleanly, and clicking the input should make only the text input border turn blue rather than the whole outer composer frame.
- Added final `Composer containment + input-only focus polish` CSS: resets `.learn-followup-bar:focus-within` to keep the outer frame black, leaves the blue microinteraction only on `.learn-followup-input:focus`, switches the side-panel composer to `box-sizing: border-box`, `width: calc(100% - 32px)`, and a safe `44px minmax(0, 1fr) 48px` grid so attach/input/send stay inside the frame. Wider Q&A focused states keep the larger five-column layout.
- Bumped assets to `1778402200`, synced `index.html`/`style.css` to workspace and `/tmp/aquarius-deploy-clean`, and `npm run check` passed.

## 2026-05-10 Edge arrow direction and icon refresh
- Harrison clarified directional semantics for the progressive panel buttons: left-side controls should use right-pointing arrows, and right-side controls should use left-pointing arrows. Replaced the old bar-arrow SVGs with cleaner double-chevron icons in `index.html`: left lecture controls use `>>`, right Q&A controls use `<<`; hidden restore controls were updated too to avoid stale wrong arrows later.
- Added `Cleaner double-chevron edge arrows` CSS to make the circular icon badge cleaner/centered with a soft blue radial fill and subtle shadow. Bumped assets to `1778402800`, synced source/workspace/deploy, and `npm run check` passed.

## 2026-05-10 Overview lesson split Q&A button/function fix
- Harrison reported the new progressive arrows had bugs: the left `More Lecture` button did not show in the overview lesson split page, clicking controls did nothing, and the Q&A composer did not fill the right panel. Root cause was mixed layout state: `overview_lesson` was still treated as pure `overview` in `_setLearnMode`, which set chat/resizer to `display:none`, while the final chapter-overview CSS hid split controls.
- Patched `app.js` so `_setLearnMode` treats only `_learnLayoutMode === 'overview'` as the single-column overview; `overview_lesson` stays in the split workbench path and can use `learnPanelFocus`.
- Added a final EOF CSS override in `style.css` for `.chapter-overview-split-active`: restores both side buttons with absolute left/right placement, keeps chat/resizer/explain columns visible, preserves the smooth progressive 55/45 -> 70/30 -> 100/0 behavior, and makes the Q&A composer/input fill the available right panel without protruding.
- Current asset version is `1778415600`; synced `index.html`, `app.js`, and `style.css` to `workspace_tutor_related_full/tutor-openclaw-ui` and `/tmp/aquarius-deploy-clean/tutor-openclaw-ui`. `npm run check` passed.

## 2026-05-10 Aquarius logo replacement
- Harrison provided a new logo SVG: no-fork chat bubble, Aquarius signal waves, yellow star, and angled blue textbook. Replaced `tutor-openclaw-ui/aquarius-logo.svg` with that SVG.
- Replaced old inline square-wave logos in `index.html` with shared `<img src="aquarius-logo.svg?v=1778416200">` references for the intro nav sticker, sidebar logo, login brand mark, and favicon. This makes all visible product logo instances load the same source file.
- Synced `index.html` and `aquarius-logo.svg` to `workspace_tutor_related_full/tutor-openclaw-ui` and `/tmp/aquarius-deploy-clean/tutor-openclaw-ui`. `npm run check` passed.

## 2026-05-10 Chapter overview expandable note and book pages button
- Harrison wanted the chapter overview map page to include an expandable chapter summary and a button to view the textbook pages involved in the chapter.
- Updated `getOverviewSummaryHtml()` in `app.js` to render `Chapter Overview` as a collapsed `<details>` paper note plus a `Book Pages` button. For overview pages with generated prelude/lesson content, the same controls render above the subsection list.
- Added `openChapterOverviewBookPages()` and binding in `bindOverviewSubsectionCards()`: clicking `Book Pages` switches the overview into `overview_lesson` textbook mode, loads pages using the existing section page maps, and opens the existing textbook focus modal at page 1.
- Added EOF CSS for the controls so they appear as hand-journal paper UI and override older duplicated overview styles. Synced `index.html`, `app.js`, and `style.css`; asset version remains `1778422400`; `npm run check` passed.

## 2026-05-10 Codex desktop takeover - tutor-agent-3 current thread
- Harrison asked in Chinese whether I can see the desktop `tutor agent` folder and told me to read memory and take over. Current generated cwd is `/Users/chenghaoxiang/Documents/Codex/2026-05-10/tutor-agent-3`; the real project is `/Users/chenghaoxiang/Desktop/tutor agent`.
- Read takeover context: `CODEX_PROJECT_GUIDE.md`, `README.txt`, `package.json`, project memory `workspace_tutor_related_full/project-memory/MEMORY.md`, latest project memories `2026-05-09.md` and `2026-05-10.md`, and global memory `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md` plus `/Users/chenghaoxiang/Desktop/Memory/memory/2026-05-10.md`.
- Current project understanding: main UI/API code lives in `tutor-openclaw-ui/` (`index.html`, `app.js`, `style.css`, `ws-bridge.js`); materials/OCR/cache live in `tutor-materials/` and `workspace_tutor_related_full/tutor-materials/`; deploy mirror is `/tmp/aquarius-deploy-clean`.
- Verification: `npm run check` passes in `/Users/chenghaoxiang/Desktop/tutor agent`. Deploy mirror still has many uncommitted changes/new OCR and cache assets, so future work must sync carefully and must not reset or clean it casually.

## 2026-05-10 More Lecture tab parity with More Q&A
- Harrison asked to imitate the `More Q&A` button style for `More Lecture`, based on screenshot where `More Lecture` was a larger stretched pill while `More Q&A` was a compact journal tab.
- Used `frontend-design` guidance and changed only CSS/resource sync: appended final `FINAL MORE LECTURE TAB PARITY TRUE EOF LOCK` in `tutor-openclaw-ui/style.css` so `#learnExplainToggleBtn` is positioned like a compact panel tab with matching paper gradient, dashed inner border, blue circular icon, tape strip, and hover behavior. It no longer participates as a stretched toolbar grid button.
- During cleanup, an over-broad removal temporarily truncated `style.css`; caught via line-count/diff sanity check before final. Restored CSS from the fuller `/private/tmp/aquarius-deploy-clean/workspace_tutor_related_full/tutor-openclaw-ui/style.css` backup (18k+ lines, containing current Q&A/progressive/overview fixes), then re-applied only the final `More Lecture` parity block. Final synced CSS line count is 18,846 in desktop source, workspace mirror, deploy source, and deploy workspace mirror.
- Synced `style.css`/`index.html` to `workspace_tutor_related_full/tutor-openclaw-ui`, `/tmp/aquarius-deploy-clean/tutor-openclaw-ui`, and `/tmp/aquarius-deploy-clean/workspace_tutor_related_full/tutor-openclaw-ui`. Verification: `npm run check` passed; `git diff --check` passed for touched main/workspace files; in-app browser on `localhost:9000` B.1-1 showed both `More Lecture` and `More Q&A` visible with matching compact journal-tab styling.

## 2026-05-10 Left lecture toolbar journal row
- Harrison said the left/right page navigation buttons were ugly and asked to use skills to make them hand-journal style, with the left-side controls all on one row.
- Used `frontend-design` guidance and added final CSS blocks in `tutor-openclaw-ui/style.css`: `FINAL LEFT LECTURE TOOLBAR JOURNAL ROW` plus `FINAL LEFT TOOLBAR CONTAINER + TIGHT FIT PATCH`. The lecture toolbar now lays out `More Lecture`, previous-page arrow, `Lecture/Textbook`, and next-page arrow in one row.
- Updated previous/next controls from hard circular buttons into compact journal-paper controls with cream paper background, dashed inset, tape accent, and a blue circular arrow badge. In narrow lecture panels, `More Lecture` automatically collapses to icon-only so the whole row fits without cropping.
- Bumped `index.html` assets to `1778430400` and synced desktop source, workspace mirror, deploy source, and deploy workspace mirror. Verification: `npm run check` passed, `git diff --check` passed, and in-app browser on `localhost:9000` B.1-1 confirmed all left toolbar controls are in one row and no longer clipped.

## 2026-05-10 Lesson split fill fix
- Harrison reported that lecture and Q&A still were not filling the lesson page; screenshot of B.5-1 showed a huge blank center column between lecture and Q&A.
- Root cause: the legacy `learnBookCol`/textbook column was still being re-shown by lesson-mode JS in several paths (`applyLearnPanelFocusState`, `applyLearnExplainCollapsedState`, `_setLearnMode`) and occupying layout space while the real textbook now renders inline through `learnBookOverlay`.
- Fixed `tutor-openclaw-ui/app.js` so `learnBookColEl` is always `display: none` in the lesson workbench paths. Added final CSS block `FINAL LESSON SPLIT FILL LOCK` to hide/collapse `#learnBookCol`, force the lesson body to flex, and make normal / lecture-wide / qa-wide / full panel focus states fill the available width. Normal mode uses existing `--learn-explain-basis` / `--learn-chat-basis` vars so manual resizer behavior is preserved.
- Bumped assets to `1778431800` and synced desktop source, the actual localhost service dir `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui`, workspace mirror, and `/tmp/aquarius-deploy-clean` mirrors. Verification: `npm run check` and `git diff --check` passed; browser on `localhost:9000` B.5-1 confirmed `learnBookCol` is `display:none` and Lecture/Q&A fill the split without the old blank middle column.

## 2026-05-10 Left progressive button text restored
- Harrison angrily pointed out the left lecture control was only showing an icon, unlike the right `More Q&A` control, and should show the same progressive text states (`More Lecture`, `Full Lecture`, `Reset`).
- Root cause: the JS progressive state machine already updated the left label correctly, but final narrow-toolbar CSS hid `#learnExplainToggleBtn .learn-edge-toggle-label` under container breakpoints to keep the previous toolbar in one row.
- Added final CSS block `FINAL LEFT PROGRESSIVE BUTTON TEXT LOCK` in `tutor-openclaw-ui/style.css`: left lecture progressive button always keeps its label visible, reserves enough first-column width, and still keeps the whole toolbar in one row with tighter sizing on narrow panels.
- Bumped assets to `1778432200`, synced actual localhost service dir, workspace mirror, and `/tmp/aquarius-deploy-clean` mirrors. Verification: `npm run check` and `git diff --check` passed; browser on B.5-1 confirmed left label sequence `More Lecture -> Full Lecture -> Reset` with visible text.

## 2026-05-10 Chapter overview code becomes title line
- User asked to replace the blue chapter code badge (example `1.3`) with black bold text the same size as the title below, and keep it on its own line.
- Added final CSS block `FINAL CHAPTER CODE AS TITLE LINE` in `tutor-openclaw-ui/style.css`: `.chapter-overview-kicker` becomes a normal block line and `.chapter-overview-code` loses the blue badge/dashed border/rotation, becoming black Quicksand/Nunito 900-weight text with the same clamp sizing and line-height as `.chapter-overview-title`.
- Bumped assets to `1778432600`, synced the actual localhost service directory plus workspace/deploy mirrors. Verification: `npm run check` and `git diff --check` passed; browser on chapter `1.3 Classification of Signals` confirmed code text `1.3` renders as a separate black bold title line.

## 2026-05-10 Gemini-planned login scrapbook collage
- User asked to call OpenRouter Gemini 3.1 Pro Preview and use skills to make the login frontend more complex and aligned with the intro/landing page style.
- Used `frontend-design` skill and called OpenRouter model `google/gemini-3.1-pro-preview` with current login screenshot/style constraints. Gemini recommended shifting from a single floating SaaS card to a tactile asymmetric desk collage: taped/pinned paper scraps, sticky notes, polaroid/grid-paper artifacts, chunky black ink borders, hard shadows, highlighter accents, physical button press states, and responsive simplification.
- Implemented in `tutor-openclaw-ui/index.html`: added decorative `login-desk-collage-left/right` markup around the login card, including a study route map, source-locked scrap note, yellow sticky note, and mini ticket/checklist.
- Implemented in `tutor-openclaw-ui/style.css`: appended `FINAL GEMINI LOGIN SCRAPBOOK COLLAGE` and `FINAL GEMINI LOGIN VIEWPORT SAFETY`. Login now uses warm intro-aligned dotted/grid paper, larger collage artifacts, taped/paper-textured main card with dashed inner border/member-pass label, stronger chunky borders/shadows, highlighter subtitle, more physical buttons/inputs, and short-height/mobile safety so the form remains usable.
- Bumped assets to `1778433800`, synced actual localhost service dir, workspace mirror, and `/tmp/aquarius-deploy-clean` mirrors. Verification: `npm run check` and `git diff --check` passed; browser on `localhost:9000/?view=login` confirmed version `1778433800` and visually verified the new scrapbook collage login page.

## 2026-05-10 Login collage no-overlap fix
- User reported the redesigned login card was covering some scrapbook decorative elements.
- Fixed with final CSS block `FINAL LOGIN COLLAGE NO-OVERLAP LOCK`: moved left/right collage artifacts outward, reduced their width at desktop breakpoints, hid the login card wrapper pseudo-paper pieces that were intentionally tucked behind the card but looked like blocked/covered content, and added medium-width rules so decorations either stay outside the card or disappear instead of sliding underneath it.
- Bumped assets to `1778434100`, synced actual localhost service dir, workspace mirror, and `/tmp/aquarius-deploy-clean` mirrors. Verification: `npm run check` and `git diff --check` passed; browser on `localhost:9000/?view=login` confirmed login card no longer covers the side decorative elements.

## 2026-05-10 Login left-bottom balance and warm paper background
- User said the login page lower-left corner felt a bit empty, then clarified they did not want the background to be a blue/green gradient.
- Added a small `login-practice-log` scrapbook paper artifact inside the left collage to fill the lower-left area without touching/covering the login card.
- Changed the login page background in the final Gemini scrapbook CSS from blue/green radial gradients to warm paper: dotted/grid paper with very light yellow/pink paper-light washes, removing the blue/green fog effect while keeping intro-page scrapbook alignment.
- Bumped assets to `1778434500`, synced actual localhost service dir, workspace mirror, and `/tmp/aquarius-deploy-clean` mirrors. Verification: `npm run check` and `git diff --check` passed; browser on `localhost:9000/?view=login` confirmed the lower-left is filled and the background is warm paper rather than blue/green gradient.
