# 2026-06-12

## Sev-1 blocker：B.8 子主题永久卡在 "Preparing lesson..."（已修复并上线 v1.2.1）

- Harrison 报告：Syllabus → B Background → B.8 → 点右侧子主题后，页面永远停在 "Preparing lesson..."，控制台显示 `[preview result] NOT FOUND`。这是第一个直接阻断学习的 Sev-1。
- **根因（纯前端）**：`openLearnMode` 先自动调用 `startLesson({silent:true})`（其 lesson fetch 持有共享的 `learnAbort` 控制器），随后 preview 未命中分支执行 `learnAbort.abort()` + 重建控制器来发 intro 请求——把自己刚发出的 lesson 请求杀掉了；`startLesson` 的 catch 静默吞掉 `AbortError`，界面永不更新。服务端无辜（B.8 走 `getB8FormulaAppendix` 短路，~15ms 即响应）。
- **为什么今天才爆**：该组合自 5/6（`9f30d11` 自动开课）起潜伏；只有没有 pregen preview 的小节才会触发。`SECTION_PREVIEWS_NEW` 缺 B.8-1/2/4/5/9（preview 生成脚本跳过了 OCR 文本过短的小节），过去一个月学的章节全部有 preview，今天第一次点到没有的。与今天早些时候的 lesson-cache 改名提交 `e269436` 无关。
- **修复** `c3f05c5`：intro fetch 改用独立 `AbortController`，通过 `learnAbort.signal` 的 once 监听器链式联动（导航离开仍能取消，但不再误杀 lesson 请求）。
- **验证**：新增 Playwright 回归测试 `tools/test-lesson-open-no-hang.js`（真实点击路径：Guest → Syllabus → B Background → B.8 → 子主题）。修复前 FAIL（复现卡死），修复后 B.8-2 / B.8-3（preview 命中路径）/ B.8-5 / 非 B.8 cacheMiss ghost 路径全部 PASS。另有 3 个独立视角的对抗性审查（生命周期/竞态/完整性），未找到反驳。
- **发布**：`53e3521` release v1.2.1（index.html 可见版本 + `app.js?v=1201` + 三处 package.json）。push 到 main 后 Vercel 自动部署（约 1 分钟），生产已用 Playwright 走完整复现路径确认课程正常渲染。**结论：push main = 前端自动上线**。
- **缺陷登记（GitHub issues）**：#5 Sev-1 本 bug（已关闭）；#6 Sev-2 所有 B.8 子主题返回同一段通用 appendix 文本、Textbook tab 几乎为空；#7 Sev-3 preview 未命中时 intro fallback 是死代码且每次开课烧一次 haiku 调用；#8 Sev-3 startLesson cacheMiss 递归无深度保护。按规则只记录未修复。
- 本次会话同时创建了 `CLAUDE.md`（给 Claude Code 的仓库指南：命令、双 agent 管线、materials 目录回退链、硬约束、部署方式）。

## 第二个 Sev-1（同日）：所有 B.8 子主题显示同一份通用 appendix 页（已修复并上线 v1.2.2）

- Harrison 回来确认 hang 修复生效后报告：每个 B.8 子主题点开都是同一份"B.8 Appendix"通用页，看不到真正内容（如 B.8-2 复数）。原登记为 #6 Sev-2，但按 Gated Timebox 协议（正在学习 B.8、阻断学习单元）提升为 Sev-1 立即修。
- **根因：宽正则遮蔽真实内容。** `/\bB\.8\b/i` 本意只匹配父节"B.8 Appendix"，但 `\b` 在 `8` 和 `-` 之间成立，所以把 B.8-1..B.8-10 全部吞掉。同一宽模式存在于**三处**：服务端 `getB8FormulaAppendix` 短路、客户端 `isB8TextbookOnlySection` 二次覆盖、`pregen-background-chapter1.js` 跳过名单（这也解释了为何子主题 cache 停留在 legacy v20/v21、b_8-5 完全没有 cache）。
- **修复 `838658e`（v1.2.2）**：三处正则全部加 `(?!-\d)` 收窄为仅父节；删掉 `readLegacyLessonCacheFallback` 的 null-memory 提前返回（`/api/section` 恒传 `baseLessonMemory=null`，该 fallback 此前是死代码；评分本就默认 standard 轨）→ 9 个子主题直接复活 legacy v21 真课程（数学已抽查正确，gptimage2 死图自动剥离，`\qquad` 由 normalize 重写）；B.8 子主题 cache miss（目前只有 B.8-5）回落到 appendix 页而不是"尚未准备"死胡同。
- **非 B.8 的 legacy-only 节（0_5, 1, 1_2, 1_4, b, b_1）实测仍 fail-closed**：fallback 读到内容后被格式/图源闸门拒绝，行为不变。
- **对抗性审查（3 视角 Workflow）在发布前抓到 B.8-5 退化 + pregen 跳过名单两个问题** —— 第一版修复会让 B.8-5 从 appendix 页降级为死胡同。教训：收窄特例正则前，先枚举被特例遮蔽的每个实例的"修复后去向"。
- **遗留**：`b_8-5` 无 cache（pregen 修好后需带 OPENROUTER_API_KEY 跑 `npm run pregen:bg-ch1` 回填，同时补 #7 的 5 个缺失 preview）；`workspace/app-mirror/` 仍是旧宽正则（非运行时，勿从 mirror 同步）；`b_8-*` cache 只在 workspace 树（prod 因 Docker `COPY . .` 两树都带所以没事）。
- 其他确认：Felix 确认 OpenRouter 泄漏 key 已轮换（Issue #1 尾巴关闭）；blocker 等级确认维持 3 级 Sev 闸门（"第四级"记忆作废）。
- 测试基建教训：每次 Bash 工具调用是新 shell，`kill %1` 杀不掉上一次调用启动的后台 server —— 9125 端口的陈旧 bridge 进程曾让新代码测试呈现假阴性（EADDRINUSE 在日志里）。用 `pkill -f "node app/ws-bridge.js"` 清理。
