# 2026-03-24

- 用户希望通过固定外网 URL 打开 **Tutor Agent 页面**，而不是 OpenClaw Control UI。
- 已将保留 ngrok 域名 `https://unsinkable-dyan-staidly.ngrok-free.dev` 改为转发到本地 Tutor UI `9000` 端口；该域名不再指向 Control UI。
- 已为本机配置两个 macOS LaunchAgent 常驻服务：
  - `ai.openclaw.ngrok`：开机后自动启动 ngrok 隧道
  - `ai.openclaw.tutor-ui`：开机后自动启动 `tutor-openclaw-ui/ws-bridge.js`
- 已确认外网访问该 ngrok 域名时返回 Tutor 页面标题 `Tutor Agent · Visual Learning`。
- 为解决 Control UI 远程来源校验报错，曾给 OpenClaw 配置添加 `gateway.controlUi.allowedOrigins`，包含 ngrok 域名和本地 `127.0.0.1/localhost`。
- 用户要求切换模型到 **Gemini 3.1 Pro**，本会话已切换到 `openrouter/google/gemini-3.1-pro-preview`。
- 修复 Tutor UI 后端调用 Python 技能脚本时的环境问题：后台 LaunchAgent 运行 Node 时，`spawn('python3')` 实际命中了系统 Python 3.9，导致 `requests` 模块缺失；已将 `tutor-openclaw-ui/ws-bridge.js` 改为显式调用 `/usr/local/bin/python3`，问题解决。
- 本次相关服务/页面目标：在本机开机常驻前提下，通过固定 ngrok 域名直接访问 Tutor Agent demo 前端。
