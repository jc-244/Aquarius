## 2026-05-21 Desktop tutor agent takeover in tutor-agent-3 thread
- Harrison asked in Chinese: "你看到我桌面上的tutor agent文件夹了吗，你读取记忆并接管".
- Located the active project at `/Users/chenghaoxiang/Desktop/tutor agent`. Related nearby items include `/Users/chenghaoxiang/Desktop/tutor agent backup 2026-05-15-222009` and `Tutor agent Testing Mannual.docx`, but the canonical working folder remains the Desktop `tutor agent` directory.
- Read key project context: root `README.md`, `workspace/memory/MEMORY.md`, latest handoff files `workspace/memory/2026-05-19.md` and `workspace/memory/2026-05-20.md`, current git status, recent commits, and diff summary.
- Current understanding: the product is now Fourier Tutor Agent; runtime UI lives in root `app/`, server/bridge is `app/ws-bridge.js`, runtime materials/OCR/page assets live in root `materials/`, and `workspace/` holds broader workbench context plus project memory.
- Verified current health: `npm run check` passes, and `http://127.0.0.1:9000/health` returns ok with indexedPages old=104 and new=329.
- Current git state at takeover: branch `main` is ahead of `origin/main` by 3 commits; active uncommitted changes include `README.md`, `app/app.js`, `app/index.html`, `app/style.css`, `package.json`, `package-lock.json`, `workspace/memory/2026-05-19.md`, plus untracked `app/fourier-logo-original.png`, `app/fourier-logo.png`, `workspace/memory/2026-05-20.md`, and this new `workspace/memory/2026-05-21.md`. Preserve existing work and do not revert casually.
- Latest product state carried forward: Fourier rename/icon work, standalone Homework nav/page removed, Feedback Board glass/ratio/reply styling refinements, sidebar collapsed width lock, homepage Ask lowered, Chapter Overview notebook/index-card CSS work, intro page Fourier blue theme, and ongoing RAG-lite to semantic RAG/RAGFlow sidecar direction.

## 2026-05-21 Windows RAG reminder
- Harrison said he now has a Windows computer and asked whether I remember the RAG plan.
- Recalled the intended architecture: Windows runs RAGFlow or another semantic RAG service as a retrieval-only sidecar; Mac Fourier Tutor Agent calls it over LAN from `app/ws-bridge.js`, receives chunks/citations, and keeps the existing Fourier UI, tutor prompt, history, attachments, exam style, and answer generation logic.
- Recalled current state: Fourier currently has OCR-aware keyword/page-level RAG-lite, not true semantic chunk retrieval. The intended upgrade is chunking textbook/OCR material, embedding chunks, retrieving semantically relevant results, optionally reranking, then injecting those chunks into the existing tutor answer pipeline.

## 2026-05-21 Windows cannot share Codex conversation
- Harrison said the Windows computer cannot share his Codex conversation because he uses an API connection, and asked how to solve it.
- Key guidance: Windows does not need access to the Codex chat thread for the RAG plan. The better architecture is to keep Mac/Codex/Fourier as the brain/orchestrator and make Windows a retrieval-only service. Mac sends query + optional section/user context to Windows over HTTP, Windows returns chunks/citations/scores, and Mac keeps all tutor behavior and conversation state.
- If shared context is needed, sync project artifacts rather than Codex conversations: `workspace/memory/`, ingestion datasets, OCR/material files, and a small handoff JSON/MD can be shared via Git, SMB/Windows share, Syncthing, OneDrive, or Tailscale. Avoid exposing API keys or full private chat logs to the Windows service.

## 2026-05-21 Zip project for Windows Codex handoff
- Harrison asked whether he can zip this project, send it to Windows Codex, let Windows read the project memory, and continue working.
- Guidance: yes, this is a workable substitute for shared Codex conversation. The Windows Codex should first read `workspace/memory/MEMORY.md`, the latest daily memory files, root `README.md`, and `PROJECT_STRUCTURE.md`, then inspect `git status` and run the project checks.
- Important packaging advice: exclude `node_modules`, `.local`, logs, caches, backup folders, and any `.env`/API-key files. Include `app/`, `materials/` if RAG ingestion or local app verification is needed, `workspace/memory/`, `docs/`, package files, and current uncommitted assets such as the Fourier logo.
