## 2026-05-09 learn Q&A panel resizer fix
- Harrison 反馈本地 `localhost:9000` 讲解页右侧提问框比例调整不了。定位后发现当前可见布局由 `.learn-body-inner` 的 CSS grid 强规则控制，而旧 JS resizer 只修改 `learnExplainCol/learnChatCol` 的 flex/width，因此会被 grid 覆盖，看起来拖拽无效。
- 已修复：`app.js` 的 resizer 改为直接更新 `.learn-body-inner` 的 `grid-template-columns`，支持 pointer events、拖拽时禁选文本、限制左栏最小 420px/右栏最小 320px，并把右侧比例保存到 `localStorage` 的 `aquarius-learn-split`；窗口 resize 时会重新应用比例，移动端单列会清掉自定义 grid。`style.css` 增加 `touch-action:none`、`body.learn-resizing`、以及移动端 `data-custom-split` 兜底。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像和 `/tmp/aquarius-deploy-clean`；`node --check app.js` 通过；使用系统 Chrome + Playwright 实测本地拖拽：右栏宽度从约 441px 变到 320px，左栏从约 845px 变到 966px，并写入 `localStorage`。资源版本 bump 到 `style.css/app.js?v=1778235600`，已推 GitHub main `00c5921 Fix learn panel resizer`；线上 `aquarius-seven.vercel.app` 已确认加载新版本且 JS/CSS 包含 `aquarius-learn-split`、`applyLearnSplit`、`learn-resizing` 等修复逻辑。

## 2026-05-09 collapsed Q&A button position fix
- Harrison 反馈点击讲解页 Q&A 后按钮跑到左上角。定位为 Q&A 折叠后的 `learnChatFab` 同时被旧的 `enableFloatingDrag(learnChatFab, ...)` 和多层 CSS 控制；旧拖拽逻辑会写入 inline `left/top/right/bottom/width/height`，与 chat-collapsed 状态冲突，导致圆形 Q&A 浮标出现在页面左上区域。
- 已修复：移除 `enableFloatingDrag(learnChatFab, learnChatFab, ...)`，保留 popover 头部拖拽；`applyLearnChatCollapsedState()` 在折叠时调用 `resetLearnChatFabPosition()`，强制清掉旧 drag dataset 并重置 inline 坐标到 `right:28px; bottom:28px`；CSS 增加 `.learn-body.chat-collapsed .learn-chat-fab` 固定右下角，以及 `.learn-body.chat-collapsed .learn-side-restore-chat` 固定右上角。
- `node --check app.js` 通过；用系统 Chrome + Playwright 实测本地点击 `#learnFocusBtn` 后：`learnBody.chat-collapsed=true`，`learnChatFab` 位于右下角约 left=1504/top=804/right=1572/bottom=872，inline 坐标为 left/top auto、right/bottom 28px；不再在左上角。资源版本 bump 到 `style.css/app.js?v=1778236800`，已推 GitHub main `addf50a Fix collapsed Q&A button position`，线上 `aquarius-seven.vercel.app` 已确认加载新版本并包含对应修复逻辑。


## 2026-05-09 B.1-2 rectangular form figure insertion
- Harrison 要求在 B.1-2 小节第二页、解释 Eq. (B.1) rectangular form 的段落下方插入 `page-005-fig__b_2-1.png`。目标段落为 “This is Eq. (B.1), the standard rectangular form of a complex number...” 这一段。
- 已修改本地活跃缓存：`workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，在该段落后加入 `![Rectangular form of a complex number](/figures/page-005-fig__b_2-1.png)` 和一行图注。确认图片文件存在于 `new-book-figures/page-005-fig__b_2-1.png`，尺寸 639x558，且本地 `http://127.0.0.1:9000/figures/page-005-fig__b_2-1.png` 返回 200。
- 已同步到 `/tmp/aquarius-deploy-clean` 并推送 GitHub main：`4c24b97 Insert B.1-2 rectangular form figure`。本次只改 lesson cache，无需 bump `app.js/style.css` 版本。

## 2026-05-09 B.1-2 polar/exponential explanation expansion
- Harrison 指出 B.1-2 第二页生成讲解相比书中 Fig. B.2 和原文过于粗略：书页实际在讲复平面中 `a,b,r,theta,z,z*` 的几何关系、`a=r cos theta`、`b=r sin theta`，再推到 `z=r(cos theta + j sin theta)` 和 Euler 公式。
- 已扩写活跃缓存 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_1-2/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 的 “2. Polar/exponential form: distance plus direction” 小节：加入矩形读法 vs 极坐标读法、Fig. B.2 虚线投影、公式推导、`z*` 共轭解释、图中符号速读、示例与度/弧度 common mistake。
- 已同步到 `/tmp/aquarius-deploy-clean` 并推送 GitHub main：`ba46d3f Expand B.1-2 polar form explanation`。本次只改 lesson cache，无需 bump 前端资源版本。

## 2026-05-09 B.1-2 Fig B.2 symbol guide image placement
- Harrison 建议在 “Reading Fig. B.2 quickly” 上方再插入刚才的 `page-005-fig__b_2-1.png`，因为该文字就是逐项读图。已在该 heading 前加入同一张图和一句图注，让学生阅读符号 guide 时不用往上找图。
- 初次修改时我把符号列表从 LaTeX inline 改成普通加粗文本，Harrison 纠正该排版本来是正常的。已恢复列表为原始数学写法 `\(a\)`、`\(b\)`、`\(r\)`、`\(\theta\)`、`\(z^*\)`，只保留图片插入。
- 已同步并推送：`aa98f65 Add Fig B.2 near symbol guide` 插入图片，随后 `ae8c068 Restore Fig B.2 symbol math formatting` 恢复符号数学排版。

## 2026-05-09 phasor interactive demo overflow fix
- Harrison 反馈 B.1-2 里的 “Drag the complex number” 可交互 demo 超出左侧讲解框，右侧 canvas 压进 Q&A 区域。定位到 phasor demo 的两列 grid 和 canvas sizing：`.phasor-demo-grid` 使用固定左栏 `minmax(260px,320px) 1fr`，JS `sizeCanvas()` 又强制 canvas 最小宽度 360px；拖动讲解/Q&A 分栏时窗口尺寸未变，demo 不会重新判定布局。
- 已修复前端真源 `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui` 并同步 workspace 镜像：`.kc-interactive-demo`、`.phasor-demo-shell`、grid/canvas wrapper 增加 `max-width:100%`、`min-width:0`、`overflow:hidden`；grid 改为 `minmax(180px,300px) minmax(0,1fr)`；窄于 760px 时 `.phasor-demo-shell.is-narrow` 自动切单列；canvas 宽度按父容器扣 padding 后计算，最小 160px，CSS 宽度固定 `100%`。
- JS 新增 `ResizeObserver` 监听 demo shell 自身宽度，配合 `requestAnimationFrame` 节流；因此拖 Q&A/讲解比例时 demo 会即时重绘和切换单列，不再等 browser resize。`node --check app.js` 通过；资源版本 bump 到 `style.css/app.js?v=1778237600`。已推送 GitHub main：`147cc62 Constrain phasor demo within lesson panel`。

## 2026-05-09 B.2 nonexistent demo reference cleanup
- Harrison 在 B.2 Sinusoids 页指出正文写着 “Look at the phasor inset in the demo: as the vector rotates, the red dot...” 但当前页面没有对应 demo/phasor inset，因此该句是内容幻觉：生成内容引用了 planner/hidden block 里的可选 demo 设想，实际前端未渲染出该 React-code demo。
- 已修改活跃 cache `workspace_tutor_related_full/tutor-materials/lesson-cache/b_2/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`：把该句改为不依赖 demo 的手画 phasor 说明：“If you sketch the phasor by hand...” 等；同时把该页单行 display math `$$...$$` 改成多行 `$$` 块，避免页面显示原始 `$$x(t)=...$$`。
- 已同步 `/tmp/aquarius-deploy-clean` 并推送 GitHub main：`1a897ce Remove nonexistent B.2 demo reference`。后续生成/审校规则：正文不得引用 student-visible demo/image/inset，除非该视觉在页面上确实渲染可见。

## 2026-05-09 Codex desktop takeover
- Harrison 要求确认是否看到桌面 `tutor agent` 文件夹，并“读取记忆并接管”。当前 Codex 默认 cwd `/Users/chenghaoxiang/Documents/Codex/2026-05-09/tutor-agent` 是空目录，真实项目位于 `/Users/chenghaoxiang/Desktop/tutor agent`。
- 已读取并接上核心上下文：`CODEX_PROJECT_GUIDE.md`、`README.txt`、`package.json`、`workspace_tutor_related_full/README_FULL_SCAN.txt`、`workspace_tutor_related_full/project-memory/MEMORY.md`、`2026-05-08.md`、`2026-05-09.md`，并查看 `/tmp/aquarius-deploy-clean` 最近提交。
- 当前接管结论：主工程是 `tutor-openclaw-ui/`，核心文件为 `index.html/app.js/style.css/ws-bridge.js`，内容/缓存资产在 `tutor-materials` 与 `workspace_tutor_related_full/tutor-materials`，部署同步仓库为 `/tmp/aquarius-deploy-clean`。后续默认用中文，不出现韩语；每次对话写记忆；“重新生成”先定位实际命中缓存并删除旧缓存再重生；生成讲解时多例子，能配图/交互就尽量配。
- 最新项目状态已接上：5月8日完成安全版 GitHub + Render + Vercel 部署、登录/OAuth 状态机多轮修复、父章节前言与整章生成定义、Background 全章节生成与 page-map 审计；5月9日完成讲解页 Q&A 分栏拖拽、折叠按钮位置、B.1-2 复数图文内容修补、phasor demo 溢出修复、B.2 不存在 demo 引用清理。最新部署提交停在 `1a897ce Remove nonexistent B.2 demo reference`。

## 2026-05-09 compact learn Q&A skill-call UI
- Harrison 发截图反馈讲解页右侧“调用 skills / Ask about this page”空态过于臃肿，希望更简约。已使用 `frontend-design` skill 的方向做小范围 UI 精简，不改问答/上传/联网/发送逻辑。
- 修改桌面真源 `tutor-openclaw-ui/index.html/style.css` 并同步到 `workspace_tutor_related_full/tutor-openclaw-ui` 与 `/tmp/aquarius-deploy-clean`：空态删除纸夹/kicker/大海报式装饰，文案改为 `Ask this page` 与更短说明；隐藏 `Formula/Screenshot/Exam trap` 静态 pill；空态卡片宽度和 icon 大小明显缩小。
- 输入区从大卡片压成紧凑 composer：外框、padding、按钮尺寸、阴影都收敛；右栏窄宽时采用两行 grid，第一行 attachment + input + send，第二行 style select + web search，避免 `Balanced` 被压成 `Bal`。`style.css` 版本 bump 到 `1778239400`；`npm run check` 通过。
- 本地 Browser 预览了 lesson 右侧面板：第一版发现模式选择被截断，随后修为两行 grid。注意 `/tmp/aquarius-deploy-clean` 中存在此前遗留的未提交 `app.js` / lecture-card 差异，本次没有把这些旧差异纳入本轮改动或提交。

## 2026-05-09 B.7-3 blank Key Takeaways fix
- Harrison 发截图反馈 `B.7-3 Vector Operations` 讲解第 3/6 页只有 `Key Takeaways` 标题，大面积空白。定位后确认 B.7-3 活跃缓存存在，接口命中 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_7-3/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，不是缓存丢失。
- 根因是前端 `parseLessonKnowledgePoints()` 的 summary 分页逻辑：遇到 `Key Takeaways` 后创建 summary page，但因为该课件没有普通 numbered knowledge heading，后续 bullet list 被 `!sawPrimaryHeading` 分支当作 intro nodes 吞掉，没有加入 summary，所以页面只剩标题。已在 `app.js` 增加分支：如果当前页是 summary，后续非 quiz 节点直接追加到 summary parts。
- 同时修复 B.7-3 活跃缓存中的明显 Markdown 坏格式：去掉跨几十行的斜体星号、修正 `***This...**`、清理尾部多余 `*`，并把坏掉的 MATLAB 公式块 `$$\texttt{f =$$ ...` 改成 `$$\texttt{f = sin(2*pi*10*t + pi/6)}$$`。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像和 `/tmp/aquarius-deploy-clean`；`app.js` 版本 bump 到 `1778239600`；`npm run check` 通过；接口验证 B.7-3 `cached:true`、坏公式不存在、`Key Takeaways` 后 bullet 内容仍在。

## 2026-05-09 B.8 textbook-only lecture
- Harrison 要求删除 B.8 的讲解，只在第一页贴一段文字提醒学生自己去看 Textbook，因为全部内容都在 textbook 里。定位后发现 B.8 当前不是普通 cache 在生效，而是 `ws-bridge.js` 的特殊分支 `getB8FormulaAppendix()` 硬编码返回 10 页公式列表，并带 `formulaAppendix:true`。
- 已修改 `getB8FormulaAppendix()`：B.8 lesson 只返回一页提示，说明 B.8 是 formula appendix，没有单独 lecture，学生应切到 `Textbook` tab 直接看公式表；bookPages 仍保留，便于 Textbook tab 展示 page-054 到 page-058/page-064。
- 同时备份并替换 B.8 活跃缓存：旧 `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 备份到 `backup-before-b8-textbook-only-20260509/`，新活跃缓存写成同一段 textbook-only 提示，避免未来绕过特殊分支时露出旧 Greek Alphabet/公式讲解。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像和 `/tmp/aquarius-deploy-clean`；`npm run check` 与部署副本 `node --check` 通过；重启本地 `localhost:9000` 后接口验证 B.8 返回 `lessonLen:604`、`formulaAppendix:true`，正文只是一页 Textbook 提示。

## 2026-05-09 Codex desktop takeover - tutor-agent-2 thread
- Harrison 在新 Codex Desktop 线程要求确认是否看到桌面 `tutor agent` 文件夹，并“读取记忆并接管”。当前线程 cwd 是 `/Users/chenghaoxiang/Documents/Codex/2026-05-09/tutor-agent-2`，真实主项目仍是 `/Users/chenghaoxiang/Desktop/tutor agent`。
- 已读取并确认：`README.txt`、`CODEX_PROJECT_GUIDE.md`、`package.json`、`workspace_tutor_related_full/README_FULL_SCAN.txt`、项目主记忆 `MEMORY.md`、`2026-05-08.md`、`2026-05-09.md`，以及全局记忆 `/Users/chenghaoxiang/Desktop/Memory/memory/2026-05-09.md`。
- 接管结论保持不变：主工程为 `tutor-openclaw-ui/`，核心文件是 `index.html/app.js/style.css/ws-bridge.js`；内容与缓存资产在 `tutor-materials` 和 `workspace_tutor_related_full/tutor-materials`；部署同步目录是 `/tmp/aquarius-deploy-clean`，其最新提交为 `1a897ce Remove nonexistent B.2 demo reference`，但该部署目录当前还有未提交的 `app.js/index.html/style.css` 差异，来自后续 compact Q&A UI 等本地改动。
- 后续继续遵守：默认中文，不出现韩语；每次对话写入当日记忆；“重新生成”先定位前端/后端实际命中缓存并删除旧产物再重生；生成讲解要多举例，能配图或交互就优先配；整章生成按“父章节 prelude 判断 + 所有子节”定义执行，并行生成不同 section 可行但同一 section 不重复删/写。

## 2026-05-09 lesson takeaway and callout card styling
- Harrison 要求所有 `Key Takeaways` 都套一层壳，并把每条 takeaway 标成 1、2、3 这种序号；同时希望 B.2 页面中 `When to use it` / `EXAM TRIGGER` / `COMMON MISUSE` 这类普通段落也更多采用类似 `WORKED EXAMPLE` 的卡片壳。
- 已改前端渲染层 `tutor-openclaw-ui/app.js`：`decorateLectureContent()` 现在识别段落型和标题型 `Key Takeaways`，统一包成 `.lecture-note-card-summary`，并把后续段落或列表项转成 `.learn-key-takeaways-list` 有序列表；遇到 quiz/lesson-test 或 `Next, ...` 过渡句会停止收集，避免乱编号。`COMMON MISUSE`、`EXAM TIP`、`When to use it` 也加入卡片类型识别；H3/H4 callout 会独立成卡，内联 `**When to use it:** ...` 这种段落也会被拆成小卡。
- 已改 `style.css`：支持 H3/H4 卡片标题，新增 summary 卡壳、takeaway 编号圆章、内层 item 壳；保持 Worked Example / Warning / Exam / Rule / Formula 等不同色彩外壳。`index.html` 资源版本统一 bump 到 `style.css/app.js?v=1778239400`。同步了主源、workspace 镜像和 `/tmp/aquarius-deploy-clean`，`node --check app.js` 与 `git diff --check` 通过。已推送 GitHub main：`fbd71df Style lesson takeaway and callout cards`。

## 2026-05-09 B.2 sinusoid phasor demo rendered
- Harrison 追问：既然 B.2 正文原本提到 phasor inset/demo，为什么不真正做 demo。结论：B.2 cache 里确实有一个隐藏 `kc-interactive-demo`/React-code demo 计划，但前端 `hydrateInteractiveDemos()` 只支持 matrix demo 和 B.1-2 的 phasor demo，导致 B.2 demo 没被渲染出来，正文引用才显得错误。
- 已在 `tutor-openclaw-ui/app.js` 新增原生 Canvas `sinusoid_phasor_projection` demo 渲染器：显示 cosine wave + phasor inset，两者同步动画；支持 `C amplitude`、`f0 frequency`、`theta phase` sliders、Pause/Play、Reset；红点显示实轴投影，并在 waveform 上同步移动。新增 `style.css` 的 `.sinusoid-demo-*` 响应式卡片样式，资源版本 bump 到 `1778240800`。
- 已更新 B.2 active cache `workspace_tutor_related_full/tutor-materials/lesson-cache/b_2/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`：移除原先未渲染的超长旧隐藏 React demo 块，只在 `Phasor view / Convert a cosine-sine sum into one sinusoid` 前插入新的轻量 `kc-interactive-demo`，正文恢复为指向真实 demo 的 “Look at the phasor inset in the demo...” 版本。`node --check` 与 `git diff --check` 通过。已推送 GitHub main：`da419b4 Render B.2 sinusoid phasor demo`。

## 2026-05-09 B.4 wrong B.3 figure removal
- Harrison 发现 B.4 Cramer's Rule 页面出现了 Figure B.11 / `page-023-fig__b_11-1.png`，这张图实际属于 B.3/B.3-2 的 exponentially varying sinusoid，不应该出现在 Cramer's Rule。根因是 B.4 缓存跨页/图像归属错误，把前一节的图带入，并且生成了误导性的矩阵/线性系统图注。
- 已从 B.4 活跃缓存和三个旧 track 变体中删除该错误图片、图注和活跃缓存里的隐藏 visual meta：`new__aquarius_visual_latex_v2...`、`new__track:standard|math:calculus_ok.v9.en.md`、`new__track:foundation|math:calculus_ok.v9.en.md`、`new__track:cram|math:calculus_ok.v9.en.md`。同步修改本地 workspace 和 `/tmp/aquarius-deploy-clean`。
- 验证：B.4 缓存目录已搜不到 `page-023-fig__b_11-1.png`、`Fig. B.11` 或 `exponentially varying sinusoid`；`git diff --check` 通过。只提交了 B.4 四个缓存文件，保留部署目录里原本未提交的 `ws-bridge.js`、B.7-3、B.8 改动未触碰。已推送 GitHub main：`6eba90e Remove wrong B.3 figure from B.4 lesson`。

## 2026-05-09 Q&A assistant restore jump fix
- Harrison 反馈浮动 `Q&A Assistant` 第一次点击 `Restore` 时会移动到左上角。定位为 `Restore` 按钮位于可拖拽的 popover header 内，按钮 mousedown 同时触发 `enableFloatingDrag()` 初始化；旧逻辑在 mousedown 立刻把目标的 `right/bottom` 清成 `auto`，但没有实际拖动时 fixed 元素会退回 DOM 静态位置，看起来跳到左上/左侧。
- 已修复 `tutor-openclaw-ui/app.js`：`enableFloatingDrag()` 遇到 `button/a/input/textarea/select/label/[role=button]/[data-no-drag]` 时不启动拖拽；并且拖拽开始时只记录 rect，不立即清 `right/bottom`，只有鼠标移动超过阈值后才固化 `left/top` 并切换到拖拽坐标。资源版本 bump 到 `style.css/app.js?v=1778242100`，同步桌面真源、workspace 镜像与 `/tmp/aquarius-deploy-clean`。
- 本地浏览器验证路径：进入 B.4 lesson，点击右侧 Q&A `Hide`，打开底部 Q&A 浮窗，第一次点击浮窗 header 的 `Restore`；结果 popover 变为 hidden，`#learnChatCol` 恢复为右侧栏，没有移动到左上角。`node --check app.js` 通过。部署只提交本次 `app.js/index.html` 小修，未纳入工作树中其他 OCR/figure-map/B.7/B.8 未提交改动。已推送 GitHub main：`f7af3af Fix Q&A assistant restore positioning`。

## 2026-05-09 Codex desktop takeover - tutor-agent-3 thread
- Harrison 在第三个 Codex Desktop 线程要求确认是否看到桌面 `tutor agent` 文件夹，并“读取记忆并接管”。当前线程 cwd 是 `/Users/chenghaoxiang/Documents/Codex/2026-05-09/tutor-agent-3`，真实项目仍位于 `/Users/chenghaoxiang/Desktop/tutor agent`。
- 已读取并接上：`CODEX_PROJECT_GUIDE.md`、`README.txt`、`package.json`、项目主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、最新 `2026-05-08.md` 与 `2026-05-09.md`、以及全局记忆 `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md` 和 `/Users/chenghaoxiang/Desktop/Memory/memory/2026-05-09.md`。
- 当前接管结论：主工程是 `tutor-openclaw-ui/`，核心文件为 `index.html/app.js/style.css/ws-bridge.js`；教材/缓存资产在 `tutor-materials` 与 `workspace_tutor_related_full/tutor-materials`；部署同步目录为 `/tmp/aquarius-deploy-clean`。桌面根目录本身是未提交整理包，不应当被误认为线上提交仓库；部署目录最新提交为 `6eba90e Remove wrong B.3 figure from B.4 lesson`，当前仍有未提交差异 `tutor-openclaw-ui/ws-bridge.js`、`B.7-3` 活跃缓存、`B.8` 活跃缓存，来自之前本地修复，不能随手覆盖或清理。
- 后续继续遵守：默认中文，不出现韩语；每次对话写记忆；“重新生成”先定位实际命中缓存并删除旧产物再重生；讲解多例子，能配图/交互就优先配；整章生成按“父章节 prelude 判断 + 所有子节”定义执行；不同 section 可并行，同一 section 不能重复并行删/写。

## 2026-05-09 Chapter 1 OCR status check
- Harrison 问现在 Chapter 1 是否也有 OCR。已检查 `workspace_tutor_related_full/tutor-materials/new-book-ocr` 与 `tutor-materials/new-book-ocr`：新书 OCR 共 149 个 `page-*.txt`，Chapter 1 从 `page-064.txt` 开始，页首为 `CHAPTER 1 SIGNALS AND SYSTEMS`，内容文本已存在。
- 已确认 `tutor-openclaw-ui/section-page-map-new.json` 中 Chapter 1 已有完整映射：父章节 `1` 覆盖 `page-064` 到 `page-135`；小节包括 `1.1` 到 `1.12`，以及 `1.1-1/1.1-2`、`1.2-1` 到 `1.2-4`、`1.3-1` 到 `1.3-5`、`1.4-1` 到 `1.4-3`、`1.5-1/1.5-2`、`1.7-1` 到 `1.7-8`、`1.8-1` 到 `1.8-3`、`1.11-1` 到 `1.11-4` 等。
- 结论：Chapter 1 有 OCR，也已经接入新书 section page map；但这不等于所有 Chapter 1 lesson 都已经按最新 active variant 生成，需要另查 lesson-cache。

## 2026-05-09 Chapter 1 generation readiness check
- Harrison 追问生成 Chapter 1 的东西是否也齐了，书上图片映射是否也有。已检查：`tutor-openclaw-ui/section-figure-map-new.json` 存在 Chapter 1 图片映射，共 44 个 Chapter 1 section entries，121 个 figure references；所有引用在 `workspace_tutor_related_full/tutor-materials/new-book-figures` 中都有实体文件，missingCount=0。
- Chapter 1 图像文件覆盖 `page-066` 到 `page-131` 等正文页，并包括 problems 页 `page-136` 到 `page-139` 的若干图片；但 121 个引用中有 32 个文件名仍为 `unknown-*`，说明图片可用但部分命名/语义归属未必已经人工精修。
- `ws-bridge.js` 已加载 `section-figure-map-new.json` 和 `new-book-figures`，并通过 `/figures/:filename` serve 图片；生成链可使用这些 figure references。
- 现有 Chapter 1 lesson cache 不齐且多为旧 variant/track：目前只看到 `lesson-cache/1` 的旧 `v8` 变体，以及 `1_1-1`、`1_2`、`1_2-4`、`1_4` 的旧 track cache；尚未看到全 Chapter 1 按当前 active variant `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 全量生成。结论：生成素材基本齐了（OCR/page map/figure map/实体图），但讲解缓存还没全生成齐。

## 2026-05-09 B.8 stale lecture frontend guard
- Harrison 截图反馈 B.8 页面仍显示旧公式讲解 `B.8-1 Mathematical Constants`。复查后端 `/api/section` 已返回正确 one-page textbook-only prompt，`hasMathConstants:false`，说明浏览器里看到的是旧前端 session/localStorage 或已渲染页面状态。
- 已在 `tutor-openclaw-ui/app.js` 增加 B.8 前端硬保护：`B8_TEXTBOOK_ONLY_MARKDOWN`、`isB8TextbookOnlySection()`、`forceB8TextbookOnlyLesson()`；`startLesson()` 收到 B.8 响应后强制覆盖 `data.lesson` 并设 `formulaAppendix:true`；`loadHistoricalSession()` 恢复旧会话时也强制覆盖；`loadRecentConversations()` 与 `saveRecentConversations()` 读写时归一化 B.8 的 `lessonMarkdown`，把旧 `Mathematical Constants` 记录冲掉。
- `index.html` 资源版本 bump 到 `style.css/app.js?v=1778241200`，已同步到 `workspace_tutor_related_full/tutor-openclaw-ui` 与 `/tmp/aquarius-deploy-clean`；`npm run check` 通过。接口验证 B.8 `lessonLen:604`、`formulaAppendix:true`、`hasMathConstants:false`；本地页面加载到 `app.js?v=1778241200` 且页面文本无旧 `B.8-1 Mathematical Constants`。

## 2026-05-09 B.7-3 pagination split fix
- Harrison 截图反馈 B.7-3 第 2 页堆了很长一串内容，分页没做好。根因：该课件正文原本只有公式/段落，没有明确 `## 1...` 这种知识点标题，前端分页器只能切成 `Overview + Core Lesson + Summary + Quiz`，导致大段 MATLAB 示例挤在一个 page frame。
- 已在 `tutor-openclaw-ui/app.js` 增加无标题长课件自动拆分逻辑：对 `autoSplit` knowledge point 按 DOM node 权重、公式块、图片、列表等估算页面长度，遇到主要公式/标题锚点时拆页，并为 chunk 自动提取标题。这样以后没有小标题但内容很长的 lesson 也不会全部堆在一页。
- 同时修 B.7-3 活跃缓存 `workspace_tutor_related_full/tutor-materials/lesson-cache/b_7-3/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`，补上 4 个明确分页标题：`1. Colon notation creates stored vectors`、`2. Vectorize the cube roots`、`3. Index position is not function input`、`4. Build a sampled sinusoid vector`。已同步部署副本对应 cache。
- `index.html` 资源版本 bump 到 `style.css/app.js?v=1778241500`，同步主源、镜像与 `/tmp/aquarius-deploy-clean/tutor-openclaw-ui/`；`npm run check`、部署副本 `node --check`、`git diff --check` 均通过。完整 `/api/section` 请求验证 B.7-3 `cached:true`，lesson 返回上述 4 个 H2，旧坏公式不存在；Browser reload 后确认页面加载 `app.js?v=1778241500`。

## 2026-05-09 login default route to workspace home
- Harrison 反馈当前成功登录后会直接进入第一个讲解 B.1-1，但希望登录后一开始进入主工作区首页（带书封面、`Linear Systems & Signals`、主输入框的页面）。根因：intro landing 的 `Open Workspace` 点击处理调用 `prepareLearnReturnTarget()`，把 auth return intent 设为 `learn`，默认 target 是 syllabus 第一节，所以登录后自动 `openLearnMode(B.1-1...)`。
- 已在 `tutor-openclaw-ui/app.js` 增加 `clearAuthReturnTarget()` 与 `prepareWorkspaceReturnTarget()`；intro landing 的 `handleEnter` 改为 `prepareWorkspaceReturnTarget()`，明确设置 return intent 为 `workspace` 并清掉 lesson target。这样从首页点 `Open Workspace` 登录后进入 `showWelcome()` 主学习首页，不再自动打开第一节。
- 保留原有从具体小节触发登录的能力：`prepareLearnReturnTarget()` 仍存在，只有 lesson intent + target 时才会回到具体讲解；新用户完成 quiz 后仍会 `continueToPendingLearnTarget()`，但首页入口因为清空了 target，不会误跳 B.1-1。资源版本为 `app.js/style.css?v=1778241800`，已同步镜像与 `/tmp/aquarius-deploy-clean/tutor-openclaw-ui/`；`npm run check`、部署副本 `node --check`、`git diff --check` 通过；Browser reload 确认加载 `app.js?v=1778241800`。

## 2026-05-09 lesson page point-title header
- Harrison 澄清分页要求：不是仅仅把内容拆短，而是每个知识点分页都要像参考图一样，在页首显示明确小点标题（例如 `3. Square Matrices`）和虚线分隔。B.7-3 当前虽然拆页，但可视页首仍从公式/正文开始，看不到“小点分页”标题。
- 已在 `tutor-openclaw-ui/app.js` 增加结构级页眉：`getLessonPageDisplayTitle()`、`stripDuplicatePageHeading()`，`buildLessonPageFrameHtml()` 对 `knowledge` 类型 page 固定渲染 `<header class="lesson-page-heading"><h2>...</h2></header>`；若正文第一处 H1/H2/H3 与页眉同名则移除，避免重复标题。Overview/Summary/Quiz 不显示该 knowledge header。
- 已在 `style.css` 增加 `.lesson-page-heading` 样式：大号 Quicksand 标题、页首居中宽度、底部 dotted divider，接近参考图的小点分页视觉。资源版本 bump 到 `style.css/app.js?v=1778242100`，同步主源、镜像与 `/tmp/aquarius-deploy-clean/tutor-openclaw-ui/`；`npm run check`、部署副本 `node --check`、`git diff --check` 通过；Browser reload 确认加载 `app.js/style.css?v=1778242100`。

## 2026-05-09 B.7-3 LaTeX title cleanup
- Harrison 截图反馈 B.7-3 页眉仍出现乱码标题，如 `f(t) = \sin...\frac...`。这是旧 session/自动拆页路径把公式块内容当成 `block.title`，页眉再忠实渲染导致的；即使 active cache 已有 H2，旧会话恢复仍可能带公式标题。
- 已在 `tutor-openclaw-ui/app.js` 修复：自动拆页的 `inferLessonChunkTitle()` 不再从 `.math-block` 抽公式作标题，而是优先用 H1/H2/H3，其次按正文关键词推断人话标题（colon notation、cube roots、index trap、sampled sinusoid）。新增 `isBadLessonPageTitle()`，显示前判定含 LaTeX 命令、花括号、数学分隔符等坏标题时，强制从正文重新推断。
- 资源版本 bump 到 `style.css/app.js?v=1778242400`，同步镜像与 `/tmp/aquarius-deploy-clean/tutor-openclaw-ui/`；`npm run check`、部署副本 `node --check`、`git diff --check` 通过；Browser reload 确认加载 `app.js/style.css?v=1778242400`。该修复对旧 localStorage/session 中已保存的公式标题也生效。

## 2026-05-09 B.8 render error and B.7-3 regeneration
- Harrison 截图反馈 B.8 显示 `Lesson render failed: inferLessonChunkTitle is not defined`，并要求 B.7-3 重新生成。B.8 问题根因是上一轮把 `inferLessonChunkTitle()` 定义在 `parseLessonKnowledgePoints()` 内部，但 `getLessonPageDisplayTitle()` 在外部调用，导致作用域错误；B.8 内容本身仍是 textbook-only prompt。
- 已把 `inferLessonChunkTitle()` 移到全局 lesson render helpers，删除内部重复定义；资源版本最终统一为 `style.css/app.js?v=1778243300`，同步主源、`workspace_tutor_related_full/tutor-openclaw-ui` 与 `/tmp/aquarius-deploy-clean/tutor-openclaw-ui`。`npm run check`、`node --check`、部署副本检查均通过；Browser reload 确认加载 `1778243300`，且无 `inferLessonChunkTitle is not defined` / `Lesson render failed`。
- 按“重新生成先删命中缓存”规则处理 B.7-3：备份并删除 active cache `workspace_tutor_related_full/tutor-materials/lesson-cache/b_7-3/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 到 `backup-before-regen-20260509150539/`，然后执行 `node ws-bridge.js --pregen-section "B.7-3" "B.7-3 Vector Operations" new` 强制重生。Agent A/Agent B 完整跑完，保存新 active cache，`skipped:false`。
- 新 B.7-3 验证：接口 `cached:true`，lesson 长度约 19362，H2 为 `Concepts In This Section`、`1. Creating row vectors with colon notation`、`2. Vectorized complex roots`、`3. MATLAB indexing is not function input notation`、`4. Building a time vector for a sampled signal`，没有 LaTeX 标题；已同步该 cache 到 `/tmp/aquarius-deploy-clean`。B.8 接口仍返回 `lessonLen:604`、`formulaAppendix:true`、`hasMathConstants:false`。

## 2026-05-09 Chapter 1 figure descriptions found
- Harrison 纠正：之前所有图片都添加过讲解总结，让我去找。已确认这些“图片讲解总结”不在 `section-figure-map-new.json`，而是在每页 OCR metadata：`workspace_tutor_related_full/tutor-materials/new-book-ocr/page-XXX.meta.json` 的 `figures[].description` 字段，同时有 `figures[].keywords` 和 `description_model`。
- 示例：`page-066.meta.json` 的 Figure 1.1 有 description 解释 transient finite-energy waveform vs continuing finite-power waveform；`page-080.meta.json` 的 Fig. 1.11 有 description 解释 analog/digital 与 continuous/discrete-time 四象限对比。
- 统计：新书 metadata 中共有 71 个 figure entries，68 个已有 description；Chapter 1 共有 59 个 figure entries，57 个已有 description。Chapter 1 目前只缺两个 description：`page-076.meta.json` 的 `page-076-unknown-1.png`（time reversal Fig. 1.8）和 `page-115.meta.json` 的 `page-115-fig__1_36-1.png`（translational mechanical elements Fig. 1.36）。两张实体图片均存在，可用 `add_figure_descriptions.py` 或手动补。
- 重要结论：以后判断“图片素材是否齐”不能只看 `section-figure-map-new.json` 和实体图片，还要检查 `new-book-ocr/*.meta.json` 的 `figures[].description`，因为生成/检索时这些讲解总结很关键。

## 2026-05-09 Missing Chapter 1 figure descriptions preview
- Harrison 要求打开两个缺 description 的 Chapter 1 图片看原因。已查看 `page-076-unknown-1.png` 和 `page-115-fig__1_36-1.png`。
- `page-076-unknown-1.png` 是 Fig. 1.8 time reversal：上图为原信号 `x(t)`，下图为 `phi(t)=x(-t)`，展示关于纵轴翻转；裁剪右下角只露出半个 `Figure`，且 metadata `fig_id` 为 `unknown`，可能是当时自动描述脚本漏过的原因之一。
- `page-115-fig__1_36-1.png` 是 Fig. 1.36 translational mechanical elements：质量块 M、弹簧 K、线性 dashpot B；裁剪包含页眉和正文开头，不是纯图裁剪，可能导致自动描述脚本当时失败或跳过。两图实体都存在，可以后续手动或用 `add_figure_descriptions.py` 补上 description。

## 2026-05-09 Fix missing Chapter 1 figure metadata
- Harrison 指出 `page-076` 第一张图的图名应为 `Figure 1.8 Time reversal of a signal.`，并要求修改图名和补 description；同时指出 `page-115` 那张图本来有图名，也要补描述。
- 已修改桌面真源、workspace 镜像和 `/tmp/aquarius-deploy-clean`：`page-076.meta.json` 中该图改为 `fig_id: Figure 1.8`、`caption: Figure 1.8 Time reversal of a signal.`、`crop_file: page-076-figure_1_8-1.png`，并添加 description/keywords/`description_model: manual-codex-2026-05-09`。实体图文件也从 `page-076-unknown-1.png` 重命名为 `page-076-figure_1_8-1.png`，并同步更新 `section-figure-map-new.json` 与 `FILE_LIST.txt`。
- 已修改 `page-115.meta.json` 中 Fig. 1.36：统一为 `fig_id: Figure 1.36`、`caption: Figure 1.36 Some elements in translational mechanical systems.`，补充 description/keywords/`description_model`，解释质量块 M、弹簧 K、dashpot B 及其 terminal relationships。
- 验证：所有相关 JSON parse OK；桌面与部署副本 Chapter 1 figure description 统计均为 `59/59`，missing 空；旧实体文件 `page-076-unknown-1.png` 已移除，旧文件名仅保留在历史记忆文本中，不影响系统读取。

## 2026-05-09 Chapter 1 generation readiness answer
- Harrison 问现在是否可以生成 Chapter 1 的所有讲解。已确认素材齐备：Chapter 1 有 44 个 section-page-map entries、121 个 figure refs、59/59 个 Chapter 1 figure descriptions，当前最新版 active cache `new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md` 数量为 0。
- 回答策略：可以生成，但应按整章生成规则执行：先判断/生成 `1` 的父章节 prelude，再生成叶子小节；像 `1.1/1.2/1.3/1.4/1.5/1.7/1.8/1.11` 这类带子节的父 section 不应重复生成成普通 lesson，除非其父节前言确实需要 parent_prelude；`1.6/1.9/1.10/1.12` 等无更细子节的 section 直接生成。建议并发 2，最多谨慎 3。

## 2026-05-09 Chapter 1 lesson generation complete
- Harrison 要求多任务并行生成 Chapter 1，并追问是否能“并发多点”。本轮先用 `/api/pregen/section` 生成 active variant，发现 runtime `/api/section` 不会在 cache miss 时生成，必须用 pregen endpoint 且 `force:true`。并发从 2 提升到 3；此前记忆中的并发 4 容易 `fetch failed`，因此没有直接开 4。
- 父章节检查：`1` 无 parent prelude；过程中 `1.5` 检测到并生成了 `lesson-cache/1_5/new__parent_prelude__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`。叶子小节目标共 35 个：`1.1-1` 到 `1.12`，不重复生成带子节的父 section 普通 lesson。
- API 生成结果：34/35 个叶子小节由模型成功写入 active cache；`1.12 Summary` 两次失败，原因是 OpenRouter HTTP 402 insufficient credits（请求 18000 tokens，余额不足）。已根据 OCR `page-133` 到 `page-135` 手动补写 `1.12` active cache，并加入合法 `kc-visual-plan` 隐藏视觉元数据，否则 `/api/section` 会把文件判为 stale/cache miss。
- 最终验证：`workspace_tutor_related_full/tutor-materials/lesson-cache` 中 Chapter 1 的 35/35 个 leaf active cache 全部存在，35/35 都带 `kc-visual-plan` 或 `kc-visual-meta`；本地接口验证 `1.12` 返回 `cached:true`、lesson 长度约 10k，`1.1-1` 也返回 `cached:true`。`1.12` 文件同步到 `/tmp/aquarius-deploy-clean` 对应 lesson-cache；生成日志在 `/tmp/chapter1-leaf-pregen-force.log` 与 `/tmp/chapter1-remaining-pregen-c3.log`。

## 2026-05-09 Chapter 2 and 3 OCR generation
- Harrison 要求生成 Chapter 2/3 OCR，并询问以前 OCR 是怎么生成的。追查脚本后确认：旧背景/新书 OCR 最早主要是“页面图 + OpenRouter 视觉模型”流程，相关脚本包括 `ocr_background_v3.py`（Gemini 视觉 OCR 双页图）、`ocr_gpt54_test.py`/`scan_pages_gpt54.py`（GPT-5.4 视觉识别/章节扫描）、`generate_new_book_meta.py`（Claude/Sonnet 从 OCR 文本生成 meta）。但当前 OpenRouter 已在 Chapter 1 生成时出现 HTTP 402 insufficient credits，因此本轮不再消耗模型额度。
- 已新增本地可复跑脚本 `workspace_tutor_related_full/tutor-materials/generate_chapter_ocr_local.py`：利用 `Linear Systems and Signals Third Edition.pdf` 的文本层，通过 `pdftotext -layout` 生成 `new-book-ocr/page-XXX.txt`，用 PyMuPDF 按旧规格渲染 `new-book-pages/page-XXX.png`（1800x2200），并用本地规则生成 `page-XXX.meta.json`。PDF 映射已确认：书页 N 对应 PDF 页 N+20；Chapter 2 为书页 `150-236`，Chapter 3 为书页 `237-329`，Chapter 4 从 `330` 开始。
- 已生成并同步 Chapter 2/3：`page-150` 到 `page-329` 共 180 页；现在 workspace 真源、桌面根副本、`/tmp/aquarius-deploy-clean` 都有 `new-book-ocr` 文本 329 个、meta 329 个、`new-book-pages` 图片 329 个。问题页保留 OCR/meta，但 section teaching map 会排除 problems。
- 已修 `build_new_section_map.py`：限制 section/subsection header 必须匹配当前 chapter，避免正文公式数字如 `0.8`、`6.5`、`7.8888` 被误识别成章节；Chapter 2/3 problem 页题号也不会污染教学 section map。已重建并同步 `section-page-map-new.json`、`section-page-map-display-new.json`、`section-page-anchor-new.json`、`section-figure-map-new.json`。
- 验证：本地 `/api/section` 对 `2.1` lesson 请求返回 `bookPages: page-150,page-151`；`3.1` lesson 请求返回 `bookPages: page-237,page-238,page-239,page-240`；`/new-pages/page-237.png` 返回 200。lesson cache 尚未生成是正常状态，当前完成的是 OCR/page image/meta/section map 层。

## 2026-05-09 Codex desktop takeover - tutor-agent-4 thread
- Harrison 在第四个 Codex Desktop 线程要求确认是否看到桌面 `tutor agent` 文件夹，并“读取记忆并接管”。当前线程 cwd 是 `/Users/chenghaoxiang/Documents/Codex/2026-05-09/tutor-agent-4`，真实项目仍位于 `/Users/chenghaoxiang/Desktop/tutor agent`。
- 已读取并确认：全局主记忆 `/Users/chenghaoxiang/Desktop/Memory/MEMORY.md`、全局今日记忆、`CODEX_PROJECT_GUIDE.md`、`README.txt`、`package.json`、项目主记忆 `workspace_tutor_related_full/project-memory/MEMORY.md`、最新 `2026-05-08.md` 与 `2026-05-09.md`，并快速查看 `tutor-openclaw-ui/app.js` 与 `ws-bridge.js` 的关键入口。
- 已运行 `npm run check`，`tutor-openclaw-ui/ws-bridge.js` 与 `tutor-openclaw-ui/app.js` 语法检查通过。当前接管结论：主工程是 `tutor-openclaw-ui/`，核心文件为 `index.html/app.js/style.css/ws-bridge.js`；教材/OCR/图片/cache 在 `tutor-materials` 和 `workspace_tutor_related_full/tutor-materials`；部署同步目录为 `/tmp/aquarius-deploy-clean`。
- 注意部署副本最新 Git 提交是 `f7af3af Fix Q&A assistant restore positioning`，但工作树保留大量未提交差异：今天后续 UI 修复、B.7-3/B.8 本地修复、Chapter 1 active cache、Chapter 2/3 OCR/page/meta/map 更新等都在其中，不能误删或 reset。后续继续遵守中文沟通、不出现韩语、每次写记忆、重新生成先删实际命中缓存、多例子/可视化优先、整章生成按父章节 prelude + 叶子小节执行。

## 2026-05-09 Learn Q&A right panel visual cleanup
- Harrison 根据截图要求：只去掉右侧提问框背景网格线，不影响左侧讲义/整体背景；中间空态星星图标太丑要换；底部 `Balanced` 框太长，需要调整布局。
- 已修改 `tutor-openclaw-ui/index.html/style.css`，并同步到 `workspace_tutor_related_full/tutor-openclaw-ui` 与 `/tmp/aquarius-deploy-clean`：右侧 `.learn-chat-col` 背景改为纯净浅色纸面，不再叠网格；空态图标从笨重星星换为更轻的圆形 compass/sparkle SVG；底部 composer 增加 container query，宽面板时一行展示，窄右栏时自动切成两行，`Balanced` 变为紧凑胶囊，避免输入框被压得过短。
- 资源版本 bump 到 `style.css/app.js?v=1778243300`；`npm run check` 和部署副本 `git diff --check` 通过。本地 Browser 打开 `B.4 Cramer's Rule` lesson 截图确认右栏网格已消失、图标已替换；随后补了容器宽度规则解决窄栏 composer 挤压问题。

## 2026-05-09 Remove Q&A empty-state icon
- Harrison 看截图后要求把右侧 Q&A 空态中间的图标直接删除。已从 `tutor-openclaw-ui/index.html` 删除 `.learn-chat-empty-icon` 节点，并在 `style.css` 末尾加 `.learn-chat-empty-icon { display:none !important; }` 兜底，防止旧 DOM/缓存里残留图标仍显示。
- 已同步桌面真源、`workspace_tutor_related_full` 镜像和 `/tmp/aquarius-deploy-clean`，资源版本 bump 到 `style.css/app.js?v=1778243700`。验证：三处 `index.html` 都不再有 `<div class="learn-chat-empty-icon">`，`npm run check` 与部署副本 `git diff --check` 通过。

## 2026-05-09 Chapter overview UI de-AI and collapsed summaries
- Harrison 反馈 B.1 overview 页面丑且“一眼 AI”：不希望出现表情符号；上课不是来看大段总结，summary 不应默认展开，要用户自己点开；整体排版需要优化。已使用 `frontend-design` 方向做 UI 收敛。
- 已改 `tutor-openclaw-ui/app.js/style.css/index.html` 并同步 `workspace_tutor_related_full` 与 `/tmp/aquarius-deploy-clean`：Chapter Map hero 不再渲染 emoji 图标；章节 summary 改为 `<details>` 默认折叠的 `Chapter summary` 按钮；小节卡片从整块 button 改为 article + `Start lesson` 按钮，默认只显示编号、标题和一句短 preview，完整 summary 放进 `View summary` 折叠区；卡片 emoji 改为 `01/02` 这种文本编号 marker。
- 同时用 CSS 覆盖 lesson overview 的 `Section Objective` / `Concepts In This Section` 卡片伪元素，去掉自动加的 `💡/🍀` 前缀。资源版本 bump 到 `style.css/app.js?v=1778243800`；`npm run check`、部署副本 `git diff --check` 通过。本地 Browser 打开 B.1 overview 验证：DOM 中显示 `Chapter summary +`、每个小节 `View summary +` 和 `Start lesson`，不再有 overview emoji。

## 2026-05-09 Chapter overview two-column map
- Harrison 进一步要求：大章和小节不要上下排列，左边应是大章，右边是小节；删除这些 overview 页的小节总结，不要展开按钮，因为点进每个小节第一页本来有总结；大章总结要保留；大章如果有讲解 prelude 也不要删除；每个小节缩小成小小的横向方框。
- 已改 `getOverviewSummaryHtml()` 和 `buildOverviewSubsectionCardsHtml()`：无 prelude 的父章节 overview 现在渲染为 `.chapter-overview-map` 两列布局，左侧 hero 保留大章标题、summary、路径/refs badge；右侧显示小节列表。小节卡片恢复为真正可点击 button，但只含 `01/02` 编号、标题和 `Start ->` 提示，不再渲染小节 preview、`View summary`、details 或展开区。
- 保留 prelude 逻辑：如果父章节有 `hasPrelude` 讲解，仍走原来的 `overview_lesson` 分页讲解，并把小节入口作为 `extraHtml` 放到讲解页下方，没有删除大章讲解。CSS 调整为桌面两列、窄屏回退单列，小节项为紧凑横条。资源版本 bump 到 `style.css/app.js?v=1778243900`；`npm run check`、部署副本 `git diff --check` 通过，三份 app.js 已确认同步一致且不再含 `View summary` 文案。

## 2026-05-10 Journal-style subsection rows and collapsible chapter summary
- Harrison 看 5月10日截图后要求：右边小节改成更手帐一些的风格；左边大章总结还是改成可以展开的形式。已改 `tutor-openclaw-ui/app.js/style.css/index.html` 并同步 workspace 镜像与 `/tmp/aquarius-deploy-clean`。
- 左侧大章 summary 重新包成 `<details class="chapter-overview-summary-details">`，默认只显示 `Chapter summary +`，展开后显示大章总结。右侧小节保持无 summary，只做可点击小节入口。
- 右侧小节视觉改成手帐便签条：虚线边框、淡纸色渐变、左侧细色条、编号章、顶部小胶带伪元素、轻微纸张阴影和 hover 轻微旋转；不使用 emoji。资源版本 bump 到 `style.css/app.js?v=1778280400`；`npm run check`、部署副本 `git diff --check` 通过。尝试本地 Browser 视觉刷新时页面回到登录页，因此本轮以静态渲染/同步/语法检查验证为准。

## 2026-05-10 Remove chapter overview route metadata box
- Harrison 截图指出大章卡片里的 `Study route / 2 paths / textbook refs / lecture first` 区域不要。已从 `getOverviewSummaryHtml()` 里删除 `.chapter-overview-side` 的渲染，移除 `refs` 和 `sectionCountLabel` 使用；CSS 里保留 `.chapter-overview-side { display:none !important; }` 作为旧缓存兜底。
- 资源版本 bump 到 `style.css/app.js?v=1778280500`，同步桌面真源、workspace 镜像和 `/tmp/aquarius-deploy-clean`。验证：三份 app.js 一致，overview 渲染里已无 `Study route` / `textbook refs` / `lecture first`，`npm run check` 与部署副本 `git diff --check` 通过。

## 2026-05-09 OpenRouter rerun OCR and Chapter 1 cache
- User asked to stop local fallback and rerun all OpenRouter-credit-failed work. Re-ran Chapter 1 section 1.12 lesson cache via ws-bridge `/api/pregen/section` force path; Agent A used `gpt-5.5`, Agent B used `anthropic/claude-sonnet-4.6`, output size 37,669 bytes at `workspace_tutor_related_full/tutor-materials/lesson-cache/1_12/new__aquarius_visual_latex_v2.aquarius_visual_latex_v2.en.md`, synced to `/tmp/aquarius-deploy-clean`.
- Added and used `tutor-materials/rerun_chapter_ocr_openrouter.py` to regenerate Chapter 2/3 OCR pages 150-329 through OpenRouter visual model `openai/gpt-5.4` from page PNG inputs. Validation: 180/180 metadata files have `metadata_model=openrouter:openai/gpt-5.4`, no missing pages, no failed log lines.
- Rebuilt `section-page-map-new.json` and display/anchor/figure maps. Confirmed mappings: 2.1=page-150..151, 3.1=page-237..240, 3.4=page-253..265, 3.8=page-280..298. Synced OCR/pages/maps to desktop main material dir and `/tmp/aquarius-deploy-clean`. Restarted ws-bridge on port 9000 (PID 48664), health shows new=329 pages and `/new-pages/page-237.png` returns 200.

## 2026-05-09 Syllabus sidebar completion
- Updated new-book syllabus in `tutor-openclaw-ui/app.js` from only Background/Chapter 1 to full Third Edition contents: Background plus Chapters 1-10, with Chapter 1 extended through 1.12 and Chapter 2/3 populated with OCR/map-backed sections/subsections. Source checked against the local Third Edition PDF contents.
- Synced updated `app.js` to `workspace_tutor_related_full/tutor-openclaw-ui` and `/tmp/aquarius-deploy-clean` copies. Reconfirmed map files (`section-page-map-new`, display, anchor, figure) are present in UI/workspace/deploy.
- Synced all 35 Chapter 1 new-book lesson caches to desktop main `tutor-materials` so workspace/main/deploy are consistent. Verified `/api/section` hits cache for `1.12 Summary`, `1.8-1`, `1.11-4`; Chapter 2/3 syllabus entries return correct bookPages via the new OCR map but still show cache miss until their lessons are generated.

## 2026-05-09 B.7-3 parser title/order fix
- Harrison reported B.7-3 still rendered with no first numbered point: page 1 title showed `B.7-3 Vector Operations`, and sometimes point 3 appeared after 4. Investigation showed the active cache was not stale: `/api/section` with `bookSource:"new"` returned `cached:true`, and markdown H2 order was `1. Creating row vectors`, `2. Vectorized complex roots`, `3. MATLAB indexing`, `4. Building a time vector`.
- Root cause was frontend parsing: `parseLessonKnowledgePoints()` treated the outer `# B.7-3 Vector Operations` H1 as a primary knowledge heading. Fixed `tutor-openclaw-ui/app.js` so primary lesson pages start from H2 knowledge headings only, and added `isSectionTitleHeading()` to discard section-title H1s from intro/remainder content. Result should make page 1 title `1. Creating row vectors with colon notation` and preserve 1/2/3/4 order.
- Also cleaned the regenerated B.7-3 cache by removing an accidental giant italic wrapper around the sampled-sinusoid subsection. Synced `app.js`, updated B.7-3 cache, and bumped `index.html` assets to `app.js/style.css?v=1778243700` across desktop source, workspace mirror, and deploy copies. Verification: `npm run check` passed; deploy `app.js` syntax checks passed; browser reload showed the app loading `app.js?v=1778243700`.

## 2026-05-09 Syllabus visual polish
- Adjusted `tutor-openclaw-ui/style.css` syllabus sidebar styling per user screenshots: chapter headings remain darker, section items use softer slate-blue, subsection items use lighter muted blue/teal. Removed the active section blue vertical pseudo-element by setting `.syllabus-section.active::before { content: none !important; }`. Synced style.css to workspace and deploy copies. Verified in in-app browser: active B.1 card shows no left blue bar and section hierarchy colors are visually separated.

## 2026-05-09 Homepage dynamic polish
- Used frontend-design guidance and referenced hyperknow.io interaction direction. Updated `tutor-openclaw-ui/index.html` landing comparison section from emoji/red-x/green-check poster style to a more product-like verification stream: unanchored answer vs Aquarius verification, with source-lock status, syllabus context, page/figure chips, animated evidence steps, scan glow, and progress meter.
- Added restrained homepage motion: hero copy/collage entrance, CTA glint, chat answer loading line, proof card breathing, source-lock pulse, scan sweep, and reduced-motion fallbacks. Synced `index.html` to workspace and deploy copies. Verified served HTML contains new proof-card content and HTML tag counts are balanced.

## 2026-05-09 Syllabus hover contrast fix
- User reported syllabus subsection hover/active was almost invisible on light blue sidebar background. Updated `style.css`: section hover now uses near-white card, darker text, clearer border and shadow; subsection hover/active now has explicit border, white background, darker teal/ink text, and subtle shadow. Synced to workspace/deploy.

## 2026-05-09 B.7-3 hard fix after user still saw wrong page
- Harrison said B.7-3 was still wrong. Follow-up found two additional issues: the section-title regex did not match dotted IDs like `B.7-3`, and old legacy `new__track:...v19/v20/v21` B.7-3 caches still existed in the active cache directory.
- Fixed `isSectionTitleHeading()` regex in `tutor-openclaw-ui/app.js` to match `B.7-3`; moved all old B.7-3 `new__track*.md` files into `backup-old-track-cache-20260509/` in workspace/deploy so fallback cannot serve them. Bumped assets to `1778243900`, synced app.js/index.html, and restarted ws-bridge on port 9000.
- Applied a hard content fix to the active B.7-3 cache: removed visible `# B.7-3 Vector Operations` plus overview/concepts block, so the first rendered heading is now `## 1. Creating row vectors with colon notation`. Verified via live `/api/section` that B.7-3 returns `cached:true`, no H1 headings, and H2 list exactly `1,2,3,4`.

## 2026-05-09 lecture panel expands when sidebars hidden
- User wanted the lecture/explanation area to expand after hiding either the left workspace sidebar or the right Q&A sidebar.
- Root causes: the Q&A width resizer could leave inline grid-template-columns after collapse, and .learn-explain-body still had an older max-width:680px cap while the lesson frame used a conservative max width.
- Fix: clear/reapply learn split when Q&A collapses/restores, guard applyLearnSplit while either panel is collapsed or on mobile, expose window.applyLearnSplit, dispatch resize after left sidebar hide/show, remove the 680px explanation cap, and widen lesson frames to 1600px in collapsed-sidebar states.
- Verification: node --check tutor-openclaw-ui/app.js and git diff --check passed in a clean worktree.
- Pushed commit: dfc652f Expand lecture panel when sidebars hide.

## 2026-05-10 Q&A full-panel polish when lecture hidden
- User said after hiding lecture/explanation, the Q&A box should fill the available panel instead of staying as a small centered card, and asked for more texture/quality using frontend-design.
- Root cause: compact Q&A empty-state overrides still capped `.learn-chat-empty-state` at `max-width:360px`; only the column expanded, not the empty-state card.
- Fix: added `explain-collapsed:not(.chat-collapsed)` scoped CSS to make Q&A empty state full width, add paper/grid texture, tape accent, soft layered shadows, restored prompt chips in the full-panel state, and widened/polished the follow-up composer. Bumped assets to `1778280200`.
- Verification: npm run check passed; deploy clean diff-check passed; in-app browser verified hidden lecture state shows full-width Q&A panel.
- Pushed commit: 2210e9f Polish Q&A full panel state.
## 2026-05-10 Q&A full-panel constrained to 75 percent
- User disliked the previous full-width hidden-lecture Q&A empty state and asked for the box to occupy only a centered 75% of the full panel, with text keeping the original non-fullscreen size.
- Fix: in the `explain-collapsed:not(.chat-collapsed)` CSS, changed empty card and follow-up composer to `width: min(75%, 1160px)`, centered them, reduced min-height/padding, restored title to `22px`, copy to `13px`, and shrank prompt chips to compact sizing. Bumped assets to `1778371000`.
- Verification: npm run check passed; deploy clean diff-check/node syntax passed; local browser CSS probe confirmed title font size is 22px.
- Pushed commit: 2b8e75c Constrain full Q&A empty state.
