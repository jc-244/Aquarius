- 2026-05-03: Harrison 指出 Matrix Algebra 页面里 identity matrix 更适合直接用 LaTeX 展示，而不是强行调 Wikimedia/Wikipedia 静态参考图；同时确认新的视觉优先级规则：如果维基百科/Commons 有真正高解释价值的图片或动图（如 transpose 的行列互换演示），优先使用；如果概念本质是定义型、结构型、符号型对象（如 identity matrix / unit matrix），则应允许系统自动判定“不调图，直接用 LaTeX 更清楚”。已在 `tutor-openclaw-ui/ws-bridge.js` 中落地三层修复：1) 全局 visual policy prompt 新增“definition-first concept prefers LaTeX”规则；2) `web_search_image` / `generate_image` 调度前加入 identity matrix / unit matrix 的 LaTeX 优先启发式，直接跳过外部图；3) 对命中缓存的 lesson 做运行时规范化，把已有的 identity matrix Wikimedia 静态图替换为 LaTeX 单位矩阵块，确保旧缓存页面刷新后也能立即变正确。
- 2026-05-03: Harrison 要求把 Tutor Agent 的三条模型链路统一到“自己的 GPT API”：Agent A 继续用 `gpt-5.5`，Agent B 改用 `gpt-5.4`，图片链路 `gptimage2` 也改走同一套 OpenAI 兼容 API，而不是继续混用 OpenRouter。已修改 `tutor-openclaw-ui/ws-bridge.js`：新增 `OPENAI_BASE_URL/OPENAI_API_BASE` 支持，`Agent B` 从 OpenRouter 切到 OpenAI chat completions，`Agent A` 保持 OpenAI 通道；同时修改外部图片脚本 `~/.agents/skills/image-animation-generation/scripts/gen_image.py`，将文字模型改为 `gpt-5.4`、图片模型改为 `gpt-5.4-image-2`，并改读 `OPENAI_API_KEY` + `OPENAI_BASE_URL`。静态语法检查已通过，但实际联调测试目前失败，根因是项目实际加载到的环境里没有 `OPENAI_API_KEY`（只看到了 `OPENROUTER_API_KEY`），因此直接请求返回 401/缺 key，尚不能确认用户自定义 GPT API 的真实 base URL 和 image endpoint 兼容性。
- 2026-05-03: Harrison 进一步澄清要接的是 Codex 当前使用的 `quan2go` 配置，而不是官方 `api.openai.com`。已核实本机现成配置：`~/.codex/config.toml` 指向 `base_url = "https://capi.quan2go.com/openai"` 且 `wire_api = "responses"`，`~/.codex/auth.json` 中存在 `OPENAI_API_KEY`。实测发现这条链路不是普通 OpenAI chat-completions 兼容接口：直接请求 `/chat/completions` 返回 404；改打 `/responses` 虽命中路径，但返回 `400 not a codex request`，说明 quan2go 当前对请求体/调用上下文有 Codex 专用约束，不能直接替换 Tutor Agent 现有的通用 HTTP 调用层。为避免把项目主链路弄坏，我已把 `ws-bridge.js` 与 `gen_image.py` 回退到此前可工作的版本。当前结论：若要真切到 quan2go，必须专门做一层 `responses` wire API 适配，并弄清它要求的 codex request schema / headers / image输出格式；不能只改 base_url + model 名称。
