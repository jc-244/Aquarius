## 2026-05-16 Localhost 9000 not found after directory rename
- Harrison reported `localhost:9000` showing `Not found` after the project directory cleanup/rename.
- Diagnosis: port 9000 still had an old Node process started before the rename, with command path `/Users/chenghaoxiang/Desktop/tutor agent/tutor-openclaw-ui/ws-bridge.js`. Its `/health` endpoint still worked, but root `/` returned 404 because the old static root no longer matched the renamed `app/` layout.
- First attempted a plain `npm start` restart; it served `/` correctly but did not persist reliably after the shell command ended.
- Final fix: updated the existing macOS LaunchAgent at `/Users/chenghaoxiang/Library/LaunchAgents/ai.openclaw.tutor-ui.plist` so it runs `/Users/chenghaoxiang/Desktop/tutor agent/app/ws-bridge.js` with working directory `/Users/chenghaoxiang/Desktop/tutor agent`, then reloaded and kickstarted it with `launchctl`.
- Verification: `launchctl print gui/501/ai.openclaw.tutor-ui` shows state `running`, program arguments point to `app/ws-bridge.js`, `http://127.0.0.1:9000/` returns HTTP 200 with Aquarius HTML, and `/health` returns ok with old=104 and new=329 indexed pages. Current listener observed as Node PID 12476 on port 9000.

## 2026-05-16 GitHub collaborator invitation
- Harrison asked how to let a co-developer edit the GitHub repo. Clarified that sending the public repo link only lets them view; editing requires inviting their GitHub account as a collaborator.
- Current repo is `https://github.com/MrParamecium/Aquarius`, now using `main` as the default branch with the organized `app/`, `materials/`, `workspace/`, `docs/`, and `tools/` structure.
- Harrison navigated to `Settings -> Collaborators` at `https://github.com/MrParamecium/Aquarius/settings/access`; the correct next step is to click `Add people`, enter the co-developer's GitHub username or email, select the account, and grant `Write` permission so they can push code.
- The old temporary branch `desktop-tutor-agent-20260513` was deleted after its contents were merged into `main`; only `main` remains on the remote.

## 2026-05-16 Tutor agent handoff takeover
- Harrison asked whether Codex could see the Desktop `tutor agent` folder, read memory, and take over.
- Confirmed the active project is `/Users/chenghaoxiang/Desktop/tutor agent`; the projectless Codex workspace under Documents was empty.
- Read `README.md`, `PROJECT_STRUCTURE.md`, `workspace/memory/MEMORY.md`, `workspace/memory/2026-05-16.md`, and `workspace/memory/2026-05-15.md` to restore context.
- Current project state: clean git branch `main...origin/main`, canonical runtime paths are `app/`, `materials/`, `workspace/`, `docs/`, and `tools/`; app entry is `app/ws-bridge.js`; `npm run check` checks `app/ws-bridge.js` and `app/app.js`.
- Key continuing constraints remembered: do not casually delete Chapter 2 crops/metadata, use Claude Sonnet 4.6 for lesson generation, and append every conversation summary into the date memory file.

## 2026-05-16 Homepage attachment comparison panel
- Harrison reported that when uploading an image or attachment from the homepage question box, the app generated an explanation but the uploaded source disappeared, leaving nothing to compare against.
- Implemented a homepage answer comparison view: when the main question includes attachments, the answer screen now keeps a snapshot of those attachments and opens a default left-side `Attached Source` panel with the explanation on the right.
- Added a topbar `对照` toggle, close button, attachment tabs for multiple files, image preview rendering, PDF object/iframe preview rendering, and a draggable desktop resizer using `--attachment-source-width`; narrow screens stack the attachment panel above the explanation.
- Updated send state so a homepage attachment alone enables the send button, and made `sendQuestion` accept an explicit source (`main`/`followup`) so attachment-only follow-up sends are not misclassified.
- Verification: `npm run check` passed, `git diff --check` passed, localhost `/health` and `/` returned HTTP 200, and browser console showed no current errors after loading the app.

## 2026-05-16 References panel moved and restyled
- Harrison showed a screenshot where content/web sources appeared as a large ugly box near the bottom of the generated answer and asked for it to move to the top, be nicer, and default collapsed under the name `References`.
- Moved `sourcesSection` above `answerContent` inside the main answer scroll so references appear before the explanation instead of after it.
- Reworked the sources UI into a compact `References` accordion with a count summary (`N sources · X content · Y web`), default collapsed on new answers and restored historical answers, and expandable only when clicked.
- Restyled the references body/cards to remove the thick framed look: lighter single-line header, subtle border/shadow, smaller source cards, uppercase Content/Web labels, and cleaner horizontal rails.
- Verification: `npm run check` passed, `git diff --check` passed, and browser reload of `http://127.0.0.1:9000/` showed no console errors.

## 2026-05-16 Attached source image click-to-zoom
- Harrison asked for the uploaded problem image in the left `Attached Source` comparison panel to enlarge when clicked.
- Updated image attachments in the comparison panel to render as a clickable zoom button with a small hover hint, opening a large modal preview with title, backdrop close, and close button.
- Reused the project's existing modal visual language but added attachment-specific classes and styling (`attachment-image-modal`, `attachment-image-stage`, etc.).
- Also synchronized `References` summary counts from the actual rendered book/web source arrays so the header does not stay at `0 sources` when source cards are present; References remains default collapsed after generation and history restore.
- Verification: `npm run check` passed, `git diff --check` passed, and browser reload of `http://127.0.0.1:9000/` showed no console errors.

## 2026-05-16 Recent sessions preserve uploaded images
- Harrison reported a bug where entering a saved Recent conversation made the originally submitted uploaded image disappear.
- Diagnosis: main Q&A recent sessions only persisted history, book pages, and web sources; the homepage attachment snapshot in `activeMainAttachmentSources` was never stored.
- Added `cloneAttachmentSourcesForStorage()` and included main-session `attachments` in recent conversation snapshots and saved session records.
- Updated `loadHistoricalSession()` for main sessions to restore saved attachments into the `Attached Source` comparison panel, defaulting it open when attachments exist and clearing it only when the saved session has none.
- Existing old Recent entries cannot recover images that were never saved before this fix, but all newly saved Recent entries after this change should preserve uploaded images/PDFs.
- Verification: `npm run check` passed, `git diff --check` passed, and browser reload of `http://127.0.0.1:9000/` showed no console errors.

## 2026-05-16 Generic document attachments
- Harrison clarified that attachments should not be limited to image/PDF; Word and other documents should also be openable.
- Removed the `accept="image/*,.pdf"` restriction from main, follow-up, learn, and popover attachment inputs; attach button titles now say image/PDF/Word/other document.
- Expanded attachment typing in `app.js` to `image`, `pdf`, `document`, and `file`, with file-type chips (`DOC`, `PPT`, `XLS`, `TXT`, `FILE`) and generic document cards in the left comparison panel.
- Non-image/PDF files are preserved in the comparison panel and Recent storage with `dataUrl`, `mimeType`, and size; the UI provides an `Open file` link/download for Word and other documents.
- API payloads now filter model-readable attachments to image/PDF only, so Word files are not falsely treated as parsed by the model. Attachment-only generic-file prompts tell the user the file is openable in the comparison panel but its content was not parsed unless pasted or screenshotted.
- Added Esc close support for the attachment image modal.
- Verification: `npm run check` passed, `git diff --check` passed, and browser reload of `http://127.0.0.1:9000/` showed no console errors.

## 2026-05-16 Attachment image misclassified as file
- Harrison showed that a `pasted-image.png` was appearing as a generic `FILE` card with an `Open file` button instead of image preview.
- Root cause: the new generic attachment classifier treated saved semantic `type: "image"` as if it were a MIME type, so it did not match `image/*` and fell through to generic `file`.
- Fixed `getAttachmentType()` to first recognize semantic types (`image`, `pdf`, `document`, `file`), then fall back to `data:image/...`, `data:application/pdf`, MIME type, and filename extension.
- Added a fallback so old/wrongly saved entries with `type: "file"` but `dataUrl` beginning with `data:image/` or image extensions recover as image previews.
- Fixed `getAttachmentMimeType()` so semantic `type: "image"` is not returned as a MIME string; it now prefers `mimeType`, raw MIME-looking types, data URL MIME, then inferred defaults.
- Verification: `npm run check` passed, `git diff --check` passed, a quick Node classifier check returned `image/image/image/document`, and browser reload showed no console errors.

## 2026-05-16 Attachment whitelist and Word preview
- Harrison rejected the generic `Open file`/download behavior for non-image documents and clarified that only images, PDFs, and Word should be accepted; other document types should be unsupported rather than uploaded.
- Reversed the generic-document attachment behavior: main, follow-up, learn, and popover file inputs now whitelist image/PDF/Word `.docx`, and upload handling rejects other file types with a clear alert instead of saving them or creating a download/open link.
- Added browser-side `.docx` text extraction from `word/document.xml` using ZIP central-directory parsing and `DecompressionStream`, then renders Word files as a left-side `Word preview` text panel with no download/open button.
- API payloads now include extracted Word text as a `document` attachment context, so the model can actually use uploaded `.docx` content; image and PDF behavior stays unchanged.
- Kept the recent-session image recovery fix by normalizing old `type: "file"` image/PDF entries before rendering, so saved pasted images no longer become generic file cards.
- Verification at 2026-05-16 13:58 CST: `npm run check` passed, `git diff --check` passed, localhost browser reload showed no new JS errors, attach button titles only mention image/PDF/Word, all app file inputs have the image/PDF/.docx accept whitelist, and page text no longer contains `Open file`.

## 2026-05-16 PDF attachments not actually read by model
- Harrison showed a homepage answer where a PDF was visible in the left comparison panel, but the model answered that it could not see the full PDF and instead referenced unrelated textbook/search pages.
- Root cause: backend `generateExplanation()` only appended the placeholder `(PDF content will be analyzed by the model)` for PDF attachments; no PDF text or rendered PDF pages were actually sent to the model.
- Fixed `app/ws-bridge.js` to decode PDF attachment data URLs, write them to a temp file, run `pdftotext -layout -enc UTF-8`, and include extracted PDF text in the model prompt as `[Attached PDF text: filename]`.
- Added fallback for scanned/non-selectable PDFs: if extracted text is too short, backend runs `pdftoppm` on the first pages and sends rendered page PNGs as vision attachments, so the model can inspect the PDF image pages instead of hallucinating from textbook context.
- Increased `/api/ask` JSON body limit from the old 2 MB string accumulator to a 35 MB buffered limit so normal PDF base64 payloads can reach the backend.
- Fixed follow-up behavior: follow-up questions now merge the current left-side comparison attachments with any newly attached follow-up files, so a user can ask "can you see my PDF?" without re-uploading the same PDF.
- Verification: `npm run check` passed, `git diff --check` passed, LaunchAgent service restarted successfully, `/health` returned ok, and a synthetic PDF containing "Attachment Sentinel lotus capacitor checkpoint 731" was sent through `/api/ask`; the model answered with the PDF phrase, proving the attachment content reached the model.

## 2026-05-16 Attachment-first textbook retrieval
- Harrison objected that PDF uploads should not completely skip textbook context because uploaded PDFs are likely related to course materials; the correct behavior is PDF-first, textbook-assisted.
- Revised the prior attachment-only behavior. Frontend now marks attachment requests as `attachmentFirst` and omits stale current book/web context from the request, but the backend still uses extracted attachment text to retrieve relevant textbook pages.
- Added `prepareAttachmentContext()` so PDF/Word extraction happens before retrieval and is reused during answer generation, avoiding duplicate PDF extraction.
- Added `selectBooksFromAttachmentText()` to generate retrieval keywords from both the uploaded PDF/Word text and the user's question. The attachment is the material, while the user's question is the focus; vague user text like "这个" is resolved from attachment content instead of being used alone.
- Web search is now controlled by the existing UI toggle even for attachment-first questions, but search query generation is driven by extracted attachment text rather than vague wording like "这个"; this keeps the web tool useful without causing unrelated search noise.
- Textbook context is included only when its score against attachment text clears a threshold, so weak matches do not force unrelated content into the answer.
- Updated prompt wording: attachments are the material, the user's question defines the focus, and matched textbook/web context is supporting explanation only.
- Verification: `npm run check` passed, `git diff --check` passed, LaunchAgent restarted, and a synthetic PDF request with prompt "这个" logged `Attachment-first mode: selected ... textbook pages from attachment text`.

## 2026-05-16 Homework workspace feature
- Harrison asked for a homework feature where each homework problem can be added separately, explained, and then used for follow-up Q&A.
- Added a new Sidebar `Homework` entry and a full `homeworkView` beside Course Tracker and Mistake Notebook.
- Built the Homework workspace with a saved problem list, per-problem title/status/body editor, local save state, explanation panel, and Q&A panel scoped to the selected problem.
- Added localStorage persistence under `aquariusHomework.v1`, create/select/delete/save flows, and status labels (`Todo`, `Working`, `Explained`, `Done`).
- Wired `Generate Explanation` to the existing `/api/ask` path with web search enabled by default and a prompt that treats the pasted homework problem as the primary material.
- Wired follow-up Q&A so each question sends the current problem text plus existing explanation/history, ensuring the model sees both the homework and the user's actual question.
- Styled the workspace as a dense three-column study desk matching the current Aquarius paper/tool aesthetic, with responsive two/one-column fallbacks.
- Verification: `npm run check` passed and `git diff --check` passed. Browser automation reached the app but was blocked by the first-run personalization overlay before full click-through verification; static syntax and integration checks passed.

## 2026-05-16 HW folder deployment
- Harrison clarified Homework should not be manually created in the UI; it should be preloaded from a project HW folder.
- Created project-level `HW/HW1` and Harrison replaced the rough generated crops with final screenshots named `01.png` through `06.png`.
- Added backend `/api/homework` in `app/ws-bridge.js` to scan `PROJECT_ROOT/HW/<set>/` directories, build HW sets from image files, and expose each image both as `/homework-assets/<set>/<file>` and as a data URL for model attachment use.
- Added `/homework-assets/` static route for project HW images.
- Updated the Homework frontend to use `HW set -> problems` from `/api/homework`, with a HW selector, refresh buttons, read-only problem title/body, image preview cards, per-problem status/explanation/Q&A progress stored locally, and no manual HW/problem/image creation.
- Confirmed `HW1` currently loads 6 problems from `HW/HW1/01.png` through `06.png`.
- Verification: `npm run check` passed, `git diff --check` passed, LaunchAgent restarted, `/api/homework` returned 1 set / 6 problems / data URLs present, and `/homework-assets/HW1/01.png` returned HTTP 200 image/png.

## 2026-05-16 Homework compact reader redesign
- Harrison rejected the first three-column Homework layout and asked for a much smaller left question-number selector, the full problem fixed at the top, explanation below it, and Q&A as a bottom-right small bubble.
- Reworked `homeworkView` markup to use a compact rail (`HW` selector, refresh, count, number-only problem buttons), one scrollable main column, a sticky top problem dock, an explanation section below, and a hidden Q&A drawer opened by a floating `Q&A` bubble.
- Updated Homework rendering so the problem list only shows numbered buttons with status dots, switching problems closes the Q&A drawer, problem images render as large click-to-zoom previews without remove/open-file controls, and empty explanation text no longer says to paste manually.
- Added Q&A bubble/close button bindings in `app/app.js`.
- Added CSS override for the compact Homework reader: 76px rail, sticky problem dock, contained large problem image, explanation card below, and fixed bottom-right Q&A bubble/panel.
- Verification: `npm run check` passed, `git diff --check` passed, `/api/homework` still returned HW1 with 6 problems and data URLs, `/homework-assets/HW1/01.png` returned HTTP 200, and browser verification showed 6 problem buttons, a 76px rail, `position: sticky` problem dock, loaded image, visible Q&A bubble, and an opening Q&A panel.

## 2026-05-16 Homework compare split layout
- Harrison then rejected the top-fixed Homework reader and asked to mimic the main Compare layout instead: problem/source on the left, explanation on the right.
- Kept the compact HW/question-number rail, but changed Homework detail content to a side-by-side split: left pane contains the problem title/status/actions and scrollable problem image source; right pane contains the explanation as its own scrollable reading area.
- Changed the problem dock back from `position: sticky` to `position: relative`, with a left/right grid and a divider between source and explanation.
- Kept the Q&A as a bottom-right bubble/panel, now positioned inside the Homework workspace instead of fixed to the whole viewport.
- Verification: `npm run check` and `git diff --check` passed; browser verification after entering guest mode showed `homeworkView` visible, 6 problem buttons, grid columns around `360px 478px`, left problem pane and right explanation pane side by side, problem image loaded, and Q&A bubble positioned absolute.

## 2026-05-16 Learn page default Q&A bubble
- Harrison asked for the Learn/lecture page to default to full-screen explanation with only a bottom-right Q&A bubble, and to restore the left lecture / right Q&A split only after clicking the Q&A bubble.
- Changed Learn state defaults so `isLearnChatCollapsed` starts true, lesson rendering calls `minimizeLearnQaToBubble()`, and normal panel focus no longer writes `data-panel-focus="normal"` because old CSS hid the Q&A bubble whenever the attribute existed.
- Added Q&A topbar controls in the split panel: a square button to shrink Q&A into the existing draggable floating window and a minus button to minimize back to the Q&A bubble.
- Reworked popover header controls to icon buttons: square restores the sidebar split, minus minimizes to the bubble.
- Updated Q&A behavior: clicking the bubble opens/restores the right Q&A panel; shrinking opens the movable popover; minimizing hides the panel/popover and shows the bubble.
- Added CSS locks so collapsed Learn Q&A truly hides the right sidebar/resizer, lets the lecture fill the page, and forces the Q&A bubble visible despite older `.hidden` and collapsed-panel rules.
- Verification: `npm run check` and `git diff --check` passed. Browser testing reached the B.1-1 Learn page and exposed the old `data-panel-focus`/CSS conflict; the final fix removes that conflict and static checks confirm the new control bindings and visibility locks are present.

## 2026-05-16 Codex takeover after memory read
- Harrison asked Codex whether it could see the Desktop `tutor agent` folder, read memory, and take over.
- Located the active project at `/Users/chenghaoxiang/Desktop/tutor agent` and read `README.md`, `PROJECT_STRUCTURE.md`, core `workspace/memory/MEMORY.md`, plus the 2026-05-16 and 2026-05-15 memory files.
- Confirmed current runtime context: app entry `app/ws-bridge.js`, LaunchAgent-backed localhost port 9000, canonical directories `app/`, `materials/`, `workspace/`, `docs/`, `tools/`, and project-level `HW/HW1` for homework screenshots.
- Verified `npm run check` passes and `http://127.0.0.1:9000/health` returns ok. Current git worktree has ongoing uncommitted changes in `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, today memory, and new `HW/` content.
- Ready to continue from the latest Homework compare split layout and attachment/PDF/Word work without restarting from scratch.

## 2026-05-16 Main workspace shell flattened
- Harrison pointed out the homepage shell looked wrong because the left sidebar and right main area were separated as two framed cards; he wanted one shared surface divided only by a single line, with no outer boxes.
- First tried a quick final CSS override, but Harrison correctly objected that stacking overrides would create messy CSS. Removed that temporary `FINAL HOME SHELL FLATTENING` override and changed the original shell styling source around the main `.app`, `.main`, `.sidebar`, and `.toc-sidebar` rules instead.
- Updated the source shell styles so `.app` has no padding/gap, keeps the journal grid background, `.main` has no border/radius/shadow, and `.sidebar`/`.toc-sidebar` have no card border/radius/shadow; only `.sidebar` keeps a 1px right divider.
- Verification: `npm run check` passed, `git diff --check` passed, and browser visual check of the workspace showed sidebar and main as one continuous surface with only the vertical divider line.

## 2026-05-16 Desktop tutor agent takeover confirmation
- Harrison asked whether Codex could see the Desktop `tutor agent` folder, read the memory, and take over.
- Located `/Users/chenghaoxiang/Desktop/tutor agent`, plus the Desktop backup folder and testing manual.
- Read core project memory and handoff docs: `workspace/memory/MEMORY.md`, today's `workspace/memory/2026-05-16.md`, yesterday's `workspace/memory/2026-05-15.md`, `README.md`, `PROJECT_STRUCTURE.md`, and `docs/sync-policy.md`.
- Current state confirmed: active app path is `app/`, bridge is `app/ws-bridge.js`, server is LaunchAgent-backed on `localhost:9000`, `/health` is ok with old=104 and new=329 indexed pages, and `npm run check` passes.
- Current git worktree has existing uncommitted work in `app/app.js`, `app/index.html`, `app/style.css`, `app/ws-bridge.js`, today's memory file, and untracked `HW/`; treat these as active user/project work and do not revert.

## 2026-05-16 Similar tutor agent design research
- Harrison asked to find tutor agents similar to HyperKnow and DeepTutor to study their design.
- Searched current web/product sources and selected relevant AI tutor / study copilot examples: Tutro, OpenMAIC, Kira AI Tutor, Cognaa, superTA, Khanmigo, NotebookLM Learning Guide / LearnLM, ChatGPT Study Mode, MathGPT.ai, Open TutorAI, and OmniTutor.
- Main design patterns worth studying for Aquarius: source-upload-first learning flows, side-by-side source/explanation comparison, proactive tutor memory, Socratic/anti-cheating behavior, quiz/flashcard generation, multimodal/narrated lessons, multi-agent classroom simulation, teacher/parent observability, and persistent learning plans.

## 2026-05-16 Sidebar icon rail collapse
- Harrison showed TeachBetter.ai's sidebar behavior and asked for Aquarius to behave similarly: when the left workspace sidebar collapses, it should still leave a narrow rail visible with icons only.
- Updated `app/style.css` so `--sidebar-width-collapsed` is 76px instead of 0, the collapsed sidebar remains visible/clickable, text labels and accordion panels are hidden, navigation/settings icons are centered in 48px buttons, the Aquarius logo stays visible, and a small blue edge tab peeks out from the right edge.
- Updated `app/app.js` so the menu toggle is a real toggle: expanded -> rail, rail -> expanded, with `aria-expanded` and title text synced.
- Added `title` attributes to sidebar navigation/settings buttons so icon-only mode still has hover labels.
- Verification: `npm run check` passed, `git diff --check` passed, and `localhost:9000/health` remained ok. Browser plugin reached the app but was blocked by the login/onboarding flow for full visual clicking; CSS/JS checks confirm the rail rules override the older zero-width collapse rules because they appear later in the stylesheet.

## 2026-05-16 Sidebar minimal hover cleanup
- Harrison rejected the sidebar's hover/active styling because it showed oval pill outlines and yellow highlight marks; he asked for a simpler gray-to-black behavior.
- Removed the dashed pill styling from the `Workspace` label and disabled the yellow underline pseudo-element on `.sidebar-link`.
- Added a final sidebar-specific minimal nav lock in `app/style.css`: sidebar links/settings stay transparent with no border, no pill, no shadow, no translate, and only change color from muted gray to near-black on hover/active.
- Kept the icon rail behavior intact; collapsed icon buttons also remain transparent/minimal instead of showing rounded pills.
- Verification: `npm run check` passed, `git diff --check` passed, and `localhost:9000/health` remained ok.

## 2026-05-16 Remove lesson outer oval frame
- Harrison pointed to the large outer rounded frame around Learn lecture content and said it makes the explanation smaller; he asked to remove it.
- Identified the wrapper as `.lesson-page-frame`, which was adding max-width, padding, border, rounded corners, white background, and shadow around the generated explanation cards.
- Updated `app/style.css` so `.lesson-page-frame` is full width, transparent, borderless, shadowless, and has zero padding; kept the actual inner generated teaching cards intact.
- Added a final EOF lock to override older full-screen lesson rules that re-applied max-width to collapsed-chat Learn pages.
- Verification: `npm run check` passed, `git diff --check` passed, and `localhost:9000/health` remained ok.

## 2026-05-16 Remove workspace grid/dot background
- Harrison asked to delete the grid and dot textures behind the sidebar and Learn explanation area.
- Added a final `app/style.css` background cleanup lock that removes `background-image` from `html`, `body`, `.app`, `.main`, sidebars, topbar, Learn panels, answer panels, and feature views.
- Set workspace/Learn surfaces to clean white, with only very subtle white layering on sidebar/topbar; also disabled the dark-mode background image variants.
- Verification: `npm run check` passed, `git diff --check` passed, and `localhost:9000/health` remained ok.

## 2026-05-16 Sidebar settings icon style match
- Harrison asked for the Settings gear in the sidebar footer to match the design language of the other sidebar icons instead of using the emoji-style blue circular badge.
- Replaced the `⚙️` emoji in `app/index.html` with a line-style SVG gear using `stroke=currentColor`, matching the rest of the sidebar icon set.
- Simplified `.sidebar-settings-icon` in `app/style.css` to an 18px transparent inline icon container and removed the circular background, border, shadow, and dark-theme badge override.
- Verification: `npm run check` passed, `git diff --check` passed, and browser visual check showed the Settings icon aligned with the other sidebar line icons.

## 2026-05-16 Main Q&A follow-up history bug
- Harrison reported that on the main Q&A/Compare answer page, asking a follow-up made the previous question/answer disappear visually.
- Root cause: main follow-up requests already sent `tutorState.chatHistory.slice(-8)` to `/api/ask`, but the frontend always replaced `answerContent` with the latest explanation after the response.
- Added `renderMainConversationThread()` in `app/app.js` and use it for main follow-up loading, success, abort, error, and Recent-session restore when a session has multiple turns.
- Added `.main-chat-*` styles in `app/style.css` so preserved Q&A history renders as a readable conversation thread while first-turn explanations keep the original document layout.
- Confirmed the current PDF/attachment answer pipeline: upload/compare panel -> `/api/ask` with attachments and question -> PDF text/page-image extraction -> attachment-first textbook retrieval -> optional web search -> final explanation with history, sources, attachments, and user profile prompt.
- Verification: `npm run check` passed and `git diff --check -- app/app.js app/style.css` passed. Browser opened localhost successfully; direct helper smoke test was limited because app internals are module-scoped rather than exported on `window`.
