# B.6 Prep Notes

## Target

- Section family: `B.6 Vectors and Matrices`
- Current cache generation family: `aquarius_std_v1`

## Known subsection titles

- `B.6-1 Some Definitions and Properties`
- `B.6-2 Matrix Algebra`

## Preview status

From `new-book-previews.json`, the following preview entries exist:

- `B.6 Vectors and Matrices`
- `B.6`
- `B.6-1 Some Definitions and Properties`
- `B.6-1`
- `B.6-2 Matrix Algebra`
- `B.6-2`

## Current page-map status

From `app/section-page-map-new.json`, currently visible mappings are:

- `b.6` -> `page-037` to `page-042`
- `b.6-1` -> `page-037` to `page-038`
- `b.6-2` -> `page-038` to `page-041`

## Existing cache status

Old lesson-cache files exist for:

- `b_6-1`
- `b_6-2`

These are older versioned caches (`v12`, `v19`, `v20`, `v21`) and do **not** count as `aquarius_std_v1`.

No `aquarius_std_v1` cache files are prepared yet.

## Product implications

1. The third-edition `B.6` family is currently just:
   - `B.6`
   - `B.6-1`
   - `B.6-2`
2. We can safely prepare `B.6-1` and `B.6-2` first.
3. `B.6` as a whole can be treated as a valid overview wrapper for the two mapped subsections.

## Teaching expectations for B.6

Because this section family is about vectors and matrices:

- formulas are important, but geometric intuition also matters
- visuals should likely be at least `static`
- some subsections may benefit from `interactive` demos if matrix action, transformation, or structure comparison is central
- quiz count should likely be `normal` to `high` depending on subsection density

## Recommended first move

Start with:

1. `B.6-1`
2. `B.6-2`
