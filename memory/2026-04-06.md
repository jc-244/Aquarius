# 2026-04-06 记忆

- 2026-04-06 15:21: 删除了 memory/2026-04-04.md（国家植物园 JK 约拍 + 记忆测试，非项目内容）。
- 2026-04-06 15:43: Harrison 提出 Tutor Agent 的 RAG 检索缺陷：background-ocr 里只有纯文字，没有 metadata（标题、关键词、知识点），导致用关键词查 "linear system" 这类知识点时无从下手，且书的目录本身也很差。他的想法：为每一页额外生成一份 metadata（页面内容摘要+关键词），以提升检索精度。待讨论后实施。
- 2026-04-06 15:46: Harrison 新增硬性规则：每次对话无论什么话题，都必须先分析合理性 + 说明打算怎么做，但先不动，等他命令再执行。
- 2026-04-06 15:47-15:48: Harrison 要求先完成 metadata 方案第一步，并希望后续把 OCR 提取出的子节加入 Tutor Agent 左侧目录树。已提出方案：先从 OCR 文本提取 `B.X-Y` 子节标题到 `subsections.json`，再把前端左侧目录从大节平铺改成可折叠树形结构。
- 2026-04-06 15:48-15:56: 新建 `tutor-materials/extract_subsections.py`，扫描 `background-ocr/page-*.txt` 并输出 `background-ocr/subsections.json`；扩展正则支持纯文本、Markdown 粗体、三级标题三种子节标题格式，并用 `current_subsection` 把后续页面归到最近子节。首轮共提取 12 个子节，但缺失 `B.6-1/B.6-2/B.7-3~B.7-6`，确认问题主要来自 OCR 文本本身漏识别，不是子节提取脚本逻辑错误。
- 2026-04-06 15:56-16:01: Harrison 认为 OCR 识别图片文字不准确，要求改用 `gemini-3.1-pro-preview` 重跑，并最终要求不是只补缺页，而是把 Background 全部页面都重跑。已评估合理，并给出全量重跑 OCR + 重新抽取子节的计划。
- 2026-04-06 16:01-16:10: 基于旧脚本 `ocr_background.py` 新建 `tutor-materials/ocr_background_v2.py`，模型改为 `google/gemini-3.1-pro-preview`，prompt 强化要求大节/小节标题单独成行且不带 markdown。首次全量跑很快失败：前几页内容被错误覆盖成目录/残缺文本，后续大量页面触发 403；v2 结果不可用。
- 2026-04-06 16:15: Harrison 确定 thinking/reasoning 模型永久使用 Claude Opus 4.6（alias: opus）。
- 2026-04-06 16:20: Harrison 说明 OpenRouter 刚充值，并指出更关键的问题：metadata 应按“书页”粒度而不是按 PDF/PNG 图片粒度，因为一张扫描图里实际上含左右两页书页。这个判断被确认正确；按图片粒度做 metadata 会把两个知识点混在一起，严重影响 RAG 检索精度。
- 2026-04-06 16:20-16:28: 新建 `tutor-materials/ocr_background_v3.py`，改成让模型对每张 PNG 分别输出 `=== LEFT PAGE ===` 与 `=== RIGHT PAGE ===`，再拆成独立书页文件写入 `background-ocr-v3/book-XXX.txt`。先做单张测试确认左右页拆分正常、标题格式干净，再按 Harrison 要求只试跑前 4 张图片，成功生成前 8 个书页文件。
- 2026-04-06 16:34-16:38: Harrison 要求编号从 `book-000` 开始而不是 `book-001`。已确认合理，并将已有文件整体前移 1 位，同时把 v3 脚本里的 `BOOK_PAGE_START` 调整为 0。重命名后已有 `book-000 ~ book-021` 共 22 个文件，表示此前运行过的 11 张图结果被保留。
- 2026-04-06 16:39-17:09: Harrison 要求继续跑完 Background，并询问是否包含 Chapter 1。已说明当前 `background-pages/` 目录只覆盖 Background，对应图片尚未包含 Chapter 1；因此只能继续跑剩余 Background 页面。中途 Harrison 又要求删除旧的 `tutor-materials/background-ocr/`（gemini-2.5 产物），在确认后已移到废纸篓，之后统一以 `background-ocr-v3/` 作为新目录。
- 2026-04-06 17:08-17:09: Harrison 发现新的双页 OCR 结果也存在较大问题：部分图片只识别了上半页/一页，另一些产生 0 字节文件。排查后确认根因是 v3 脚本过度依赖 `=== LEFT PAGE === / === RIGHT PAGE ===` 格式；当模型不严格遵守输出结构时，解析逻辑会写出空文件或漏页。虽然提出了增强 fallback 与只重跑空文件的修复思路，但在 Harrison 要求下，所有 OCR 任务已先全部停止。
- 2026-04-06 17:15: Harrison 判断 `gemini-3.1-pro-preview` 在双页扫描 OCR 上可能存在模型级问题（经常只识别上半页、漏下半页），要求先暂停这一方向，后续再布置新任务。
- 2026-04-06 17:16: Harrison 要求替换 capi 的 GPT 密钥；已更新配置并重启生效。后续若再涉及 capi 配置或 Tutor Agent reasoning 模型，应记住用户最新偏好：thinking/reasoning 统一优先使用 Claude Opus 4.6，而当前主会话实际模型仍是 `capi/gpt-5.4`。
- 2026-04-06 17:19: Harrison 再次确认当前运行模型；当前会话实际模型是 `capi/gpt-5.4`。已说明若要把默认/当前会话切到 Claude Opus 4.6，需要单独修改配置或 session override。
- 2026-04-06 17:20: Harrison 询问今天的事项是否都已写入记忆。检查发现当日记忆文件只记录了部分事项，OCR 重跑/停止、background-ocr-v3 试跑问题、旧 background-ocr 删除、capi key 更新等还未完整补记；已如实告知。
- 2026-04-06 17:21: Harrison 追问为什么没有按要求每次都完整写入记忆。已承认是执行失误：长链路 OCR/配置操作中没有在每次回复后立刻补记，而且 pre-compaction flush 还错误地把整段内容重复追加，导致当日记忆文件出现重复标题与重复条目。
