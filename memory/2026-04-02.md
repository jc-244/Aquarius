- 2026-04-02 22:22: Harrison 检查记忆文件，发现昨天后半段没有及时记录，要求立即补全并重申规定：每次对话必须立即写入当日记忆文件，不能等到被问到才写。已补全 2026-04-01.md 的缺失部分。
- 2026-04-02 22:25: 讨论如何保证记忆规则被执行。决定直接修改 AGENTS.md，在"Write It Down"章节下新增强制规则：每次回复后必须立即写入 memory/YYYY-MM-DD.md，无例外。规则已写入 AGENTS.md。
- 2026-04-02 22:27: Harrison 要求拆解 OpenClaw harness 架构。读了本地文档（architecture.md, agent-loop.md, delegate-architecture.md），给出完整拆解：Gateway（总控WS守护进程）→ Agent Loop（串行运行）→ pi-agent-core（底层LLM运行时）→ Plugin Hooks（拦截点）。重点：Tutor Agent 的 RAG 逻辑最佳注入点是 `before_prompt_build` hook，用户点击小节 → hook 读 sections.json → 注入 system prompt → Opus 教学。
