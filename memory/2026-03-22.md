# 2026-03-22

## OpenClaw 模型供应商映射（用户明确要求）
- 用户反复确认并要求严格执行：
  - `gpt5.3codex` / `gpt5.4` → `capi`
  - `kimi` → `moonshot`
- 该映射为本轮排错的硬约束，后续改动不得混用前缀。

## 本轮排错关键发现
- 网关日志出现明确冲突报错：
  - `model not allowed: moonshot/gpt-5.3-codex`
  - `model not allowed: capi/kimi-k2.5`
- 说明冲突不在“用户意图”，而在“会话层/控制台提交的 model 字符串前缀被拼错”，导致 provider 与 model family 交叉错配。

## 已执行动作（仍未完全闭环）
- 多次核对并调整 `~/.openclaw/openclaw.json` 的默认模型与 fallback。
- 进行过 `openclaw gateway restart` 与模型/状态检查。
- 增补过 aliases（含 `gpt5.3codex`、`gpt5.4`、`kimi`）用于减少名称歧义。
- 尽管如此，用户在 control-ui 侧仍报告 Kimi 报错，问题疑似在 control-ui 会话 patch 的模型值组装逻辑，而非 provider 配置本身。

## 用户交互偏好（本轮）
- 用户希望“直接改、快速推进”，不希望只停留在解释。
- 用户要求先指出冲突点再继续修复；在确认后要求持续修复直到不报错。
