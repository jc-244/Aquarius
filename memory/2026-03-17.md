
### Tutor Agent Demo Progress (2026-03-17)
- User requested a tutor agent to generate algorithm visualisations (BFS, DFS, Dijkstra, Fourier Transform).
- We established a dual-track strategy:
  1. Python scripts (NetworkX, Matplotlib, Manim) for step-by-step animations.
  2. OpenRouter API (`google/gemini-3.1-flash-image-preview`) for static concept images.
- Created AgentSkill `image-animation-generation` with `SKILL.md` defining these two tracks.
- Created `tutor-openclaw-ui` static web frontend with `index.html`, `style.css`, and `app.js`.
- Configured ngrok and LocalTunnel to expose the backend.
- ENCOUNTERED BLOCKER: The web UI uses HTTP `fetch` to `/v1/chat/completions` but OpenClaw Gateway natively exposes WebSocket on port `18789`. 
- Built a Node.js `ws-bridge.js` proxy on port 9000 to translate HTTP POST to OpenClaw's WS RPC (`agents.run` / `sessions.spawn`), but user still faced Network Errors ("Load failed") communicating through ngrok. We need to debug the bridge and CORS issues to finally connect the UI to OpenClaw.
