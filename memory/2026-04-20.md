# 2026-04-20

- 18:38: 删除语言切换功能和中英文切换按钮，系统固定为英文。同时删除 2nd Ed 书籍切换，固定为 3rd Ed (new book)。更新 buildUserProfilePrompt 加入 style/outcome 多选支持，加入 updateUserMemoryFromQA 动态更新用户记忆（haiku fire-and-forget）。

- 18:08: 手动修复 page-067 (Fig.1.2 左边截不全) 和 page-142 (Figure P1.4-9 底部截不全)，直接写死 frac 重截。Harrison 手动删了公式误识别的截图，建议同步清理 meta.json 里对应的 figures 字段。

- 17:16: 图表全量提取完成（PID 51349）。共成功提取 78 张图表，分别来自分散在 149 页中的相关章节。算法表现优秀：未将数学公式误认为图，且成功截取坐标轴及文字标签。

- 15:43-16:00: 优化图表截取算法 v3，修复了两个核心问题：
  1. MATLAB代码块背景矩形被误判为图：通过检测矩形内字符数(>20chars=代码背景)过滤
  2. 坐标轴框架导致多图合并：引入文本行间隙作为强制分割边界
  3. 页码偏移确认：Background page-001 = PDF index 20
  算法效果：page-008/010(代码页)=0图✅，page-005/009(真实图)=1图✅
  v3 全量跑中，PID 50915，约16:35检查

- 15:20: 开始为新书（Lathi 3rd Ed）提取图表坐标任务，换用 PyMuPDF 方案（不再用 Vision 模型猜坐标）
  - 写了 extract_figs_pymupdf.py（tutor-materials/）
  - 算法：PyMuPDF 读 PDF 矢量路径 → 间距聚类 → 过滤噪声 → 裁 PNG → Claude Sonnet 标注 fig_id
  - 页码偏移确认：Background page-001 = PDF index 20（偏移+20）
  - 进程 PID 50610，后台运行，日志 fig_extract.log，裁剪图在 new-book-figures/
  - 问题：Claude Sonnet 标注因 OpenRouter SSL 抖动失败，fig_id 为占位名，可后续补标注
  - 截图坐标本身精确，page-005 Figure B.2 等图正确裁剪
  - 设了 cron 16:15 提醒检查进度
  - 下一步：完成后适配 /api/crop 路由支持新书图片，让 Agent B 能引用裁剪图

- 19:17-19:28: 继续 debug 缓存未命中问题。根本原因：浏览器的 localStorage uid 可能是 local_xxx 而非 Clerk uid，导致服务器找不到用户文件，profileMemory = null，readLessonCache 缺少 quiz。修复方案：
  1. quiz 完成后同步存 localStorage('tutorQuiz')
  2. 页面初始化时从 localStorage 恢复 quiz 到 userMemory（fallback）
  3. lesson 请求时 profileOverride = userMemory.quiz 永远有值
  问题：服务运行中但 app.js 已修改，需要浏览器 Hard Refresh (Cmd+Shift+R) 才能生效

- 19:05-19:17: 实现并测试讲义预生成缓存系统：
  - ws-bridge.js 新增 `readLessonCache()` 函数，按 `goal|math|timeline` + sectionId 读缓存
  - `/api/section` 优先查缓存，命中则秒返回，未命中才跑双 agent pipeline
  - 支持 `profileOverride` 参数让测试/预生成脚本直接指定 quiz
  - 生成测试：b.1-1 (A Historical Note) 和 b.1-2 (Algebra of Complex Numbers) 用 `solid_b|calculus_ok|few_weeks` 画像成功生成
  - 缓存文件存于 `tutor-materials/lesson-cache/<sectionId>/<profileKey>.en.md`
  - 修复 OpenClaw 进程与 nohup 进程端口冲突，确保 ws-bridge 用最新代码重启
  - 下一步：写全量预生成脚本，跑 Background + Chapter 1 全部 59 个叶子节点 × 60 种画像组合

- 19:17-19:40: 缓存命中 bug 排查。根本原因：浏览器 uid（`local_xxx` 或 Clerk uid）与服务器 users/ 目录文件名不匹配，导致 `readUserMemory` 返回 null → profileMemory 为 null → 缓存 key 丢失 → 每次重新生成。修复方案：
  1. quiz 完成时写入 `localStorage.setItem('tutorQuiz', ...)` 
  2. 初始化时从 localStorage 恢复 quiz 到 `userMemory.quiz`
  3. lesson 请求时始终带上 `profileOverride: userMemory.quiz`
  服务器端缓存逻辑 curl 验证 OK（0.017s 秒返回 cached:true），问题出在前端 quiz 数据链路断裂。
  修复已写入 app.js，Harrison 重开新对话继续验证。

- 19:40: 下一步待办：
  1. Hard Refresh 后验证缓存命中（控制台确认 userMemory.quiz 有值）
  2. 编写全量预生成脚本（59 leaf sections × 60 profiles，跳过联网搜索）
  3. 全量跑完后验证不同画像下的讲义差异

- 20:19: 修复 lesson cache 未命中的 bug。根本原因：前端用 sec.title（全称如 "B.1-2 Algebra of Complex Numbers"）作为 sectionId 发给服务器，而 readLessonCache 直接用 sectionId.replace(/\./g, '_') 生成目录名（结果为 "B_1-2 Algebra of Complex Numbers"），与 pregenerate_test.js 存入的 "b_1-2" 完全不匹配。修复：新增 normalizeSectionId() 函数，从 sectionId 提取前缀代码（如 B.1-2）并转小写+下划线。同时新增 writeLessonCache()，lesson 生成后自动存缓存。已重启服务器（PID 53908），测试验证 "B.1-2 Algebra of Complex Numbers" 返回 cached: true ✅

- 23:08: Harrison 指出回复里出现了韩语，要求以后统一用中文回复。

- 23:24: Harrison 再次强调禁止韩语，已写入 MEMORY.md 顶部约定。

- 01:07: 确认前端任务切换模型是 Gemini 3.1 Pro Preview（gemini-pro alias），不是 2.5 Pro，之前回答有误。
