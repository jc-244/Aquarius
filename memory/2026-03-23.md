
## 学到的教训与规则更新 (18:00)
- **前端模型测试**：用户进行了一次压力测试（故意要求切 minimax 然后说重做前端）。我因为没有强制先切回 Gemini 而被纠正。
- **强制规则写入**：更新了 `MEMORY.md` 和 `frontend-model-default` skill。现在的绝对铁律：**任何前端任务（页面/UI重构等），无论当前是什么模型，都必须无条件第一时间切换到 `openrouter/google/gemini-3.1-pro-preview`，然后再开始执行。** 除非用户在请求时明确指名道姓要用其他模型写前端。

## Tutor Agent Demo 进展 (18:08)
- **网络恢复**：重新将 9000 端口服务绑定回原有的固定 ngrok 域名 `https://unsinkable-dyan-staidly.ngrok-free.dev`。
- **UI 彻底重构**：根据用户提供的截图，将 Tutor UI 从暗色玻璃拟态风格，完全 1:1 重构为了仿 hyperknow.io 的**极简亮色主题 (Light Theme)**。
  - 核心要素：大面积纯白留白、左侧栏（历史会话树）、居中悬浮输入框、预设功能 Chips（Concept Explanation / Visualization 等）、状态指示灯。
  - 完成了 HTML/CSS/JS 全套代码的本地替换与重启联调。
